# Review — add-speak-in-simple-terms-output-style

Task: [`ai-agents/tasks/done/add-speak-in-simple-terms-output-style.md`](../tasks/done/add-speak-in-simple-terms-output-style.md)
Plan: [`ai-agents/plans/add-speak-in-simple-terms-output-style.md`](../plans/add-speak-in-simple-terms-output-style.md)
File(s) under review: `claude/scaffold/universal-rules.md` (source); root `CLAUDE.md`, root `AGENTS.md` (generated by `claude/fkit-claude-init.sh`)
Status: in-review

**Round 1 verdict: ⚠️ Changes requested — 2 defects (none blocking), 3 low.** Both reviewers ran
(reviewer pass + Codex adversarial via `codex exec`). Coverage complete.

**Out of scope, not reviewed:** tasks 61, 65, 67, 68 — `claude/agents/fkit-coder.md`,
`claude/skills/fkit-status/*`, `claude/skills/fkit-task-*`, `test/dashboard-contract.test.js`,
`claude/scaffold/ai-agents/**`, and the `ai-agents/` doc edits belonging to those tasks.

## Reviewer findings

| #  | Round | Sev    | file:line | Claim |
|----|-------|--------|-----------|-------|
| R1 | 1     | medium | `ai-agents/tasks/backlog/add-speak-in-simple-terms-output-style.md:45-58`, `ai-agents/sprints/sprint-2.md:94` | The brief's "What to build" and the sprint row still assert the **disproven four-file approach** ("4 files — CLAUDE.md + AGENTS.md × dogfood + scaffold"). The owner's correcting ruling is recorded **only** in the plan file. A later reader — or agent — re-derives the wrong plan and could "restore" the two missing scaffold copies, reintroducing hand-edited generated files. |
| R2 | 1     | medium | `claude/fkit-claude-init.sh:318`, `:339-343` | **Rules-block size budget is 84% consumed and has zero test coverage.** Emitted block = 3438B of `RULES_MAX=4096`; this bullet consumed 464B, leaving **658B**. Overflow is `exit 1` *before* either root file is merged and before the agent/skill refresh — i.e. **every `fkit` launch in every consuming project dies**. No test in `test/` references `RULES_MAX`, `universal-rules`, or `begin-rules`. Not a defect of this change; a budget/guard gap this change materially advanced. |
| R3 | 1     | low    | `claude/scaffold/universal-rules.md:35` | **Raised by both reviewers.** Inserting the new bullet at `:30-34` makes "Speak in simple terms" the nearest antecedent of the singular "**This preference** does not apply where a role or procedure prescribes the form of the output". The carve-out can now read as scoping only the new bullet, weakening it for the concision/placement cluster. Pre-existing looseness, worsened by insertion position. Cheap fix: pluralize to "These preferences do not apply…". |
| R4 | 1     | low    | `claude/scaffold/universal-rules.md:31-32` | The load-bearing-term enumeration ("a filename, a marker, an ADR, a status value") carries **no "illustrative, not exhaustive" disclaimer** — unlike its sibling at `:38`, which explicitly has one. A load-bearing term outside the list (a verdict line, `/fkit-task-done`, "hop N of 2", a severity label) could read as fair game to simplify. `:35` catches the *prescribed-form* cases but not load-bearing terms appearing in ordinary prose. |
| R5 | 1     | low    | `ai-agents/plans/add-speak-in-simple-terms-output-style.md:29-33` | The plan's blockquote is **not verbatim**: it says "use it and gloss it once." — the shipped text says "use it and gloss it once, **in a few words**." The plan is the record of what was built; a reader diffing plan against source sees a mismatch. |

### Verified as correct — no finding (recorded so they are not re-checked)

- **The brief was factually wrong; the coder's premise holds.** `claude/scaffold/CLAUDE.md` and
  `claude/scaffold/AGENTS.md` contain **zero** `## Output style` sections. They carry a bare, adjacent
  marker pair (`claude/scaffold/CLAUDE.md:59-60`, `claude/scaffold/AGENTS.md:12-13`) that init fills at
  install time. The owner's one-file ruling was the correct call; the brief's approach would have
  hand-edited generated regions.
- **Preference, not a smuggled rule.** Placement verified by line position: the bullet sits at
  `universal-rules.md:30-34`, inside `## Output style` (`:12`), after the yielding preamble (`:14-18`),
  outside the hard-rules block (`:1-10`); in the root files inside the markers and after the hard-rules
  section. Its absolutes ("never about content", "never replaces a precise term") **restrict the
  preference itself** rather than imposing new obligations, and match the register of the sibling
  bullet at `:23` ("Never drop a failing test…"). Codex reached the same conclusion independently.
- **Overlap with "Concision is not omission" is deliberate, not redundant.** The two guard the same
  seam against **different excuses** — brevity vs simplicity. Frontier-move, not a defect.
- **The `⛔ Cancelled` → "stopped" hazard is closed in text.** `:31-32` names "a status value"
  explicitly; `:33-34` forbids softening a failure and replacing a precise term with a vague one.
  Sufficient as written. Residual risk is behavioral and unprovable — matches the coder's disclosed
  limitation.
- **Codex-side reach holds for a *consuming* project — verified by execution, not by reading.** A
  greenfield sandbox init produced **both** `CLAUDE.md` and `AGENTS.md` containing the preference
  exactly once (`claude/fkit-claude-init.sh:447-459`).
- **Idempotency — could not be broken.** Four greenfield runs: byte-stable, one marker block and one
  occurrence per file. Brownfield attack (pre-existing files, **CRLF** line endings, a marker **quoted
  inline in prose**, and a file with **no trailing newline**): exactly one occurrence each, stable
  across two further runs.
- **Byte-identity across the three real homes** confirmed pairwise: `universal-rules.md:30` ==
  `CLAUDE.md:79` == `AGENTS.md:65`.
- **Init collateral — tracked surface clean.** `ai-agents/README.md` and
  `ai-agents/knowledge-base/conventions/status-report-format.md` are content edits belonging to tasks
  67/68, **not** init writes: both root copies still differ substantially from their scaffold
  counterparts, proving convergence stayed additive. *Gitignored surface, disclosed:* the init run at
  18:40:26 refreshed `.claude/` with the then-current in-flight sources of other tasks, and
  `.claude/agents/fkit-coder.md` is now stale relative to `claude/agents/fkit-coder.md` (edited 18:47,
  task 61). Not task 62's defect — recorded so the "no collateral" claim is complete rather than
  merely true of tracked files.

## Coder response

_(coder-owned — reviewer does not write here)_

**Round 1 — coder verdicts, 2026-07-18.** Every finding verified against the code before acting.
**All 5 CORRECT, none disputed, all 5 resolved.** Three needed an owner ruling (R1, R2, R4) and were
relayed **verbatim**; the owner ruled on all three the same day.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | **CORRECT** | Defect (record) | **Owner ruled (b): keep the brief as the historical record, add a pointer.** A dated `⚠️ CORRECTION` block now heads the disproven section, states what was actually verified in the tree, names the ruling, and links the plan — with the original text preserved as *what the author believed at scoping time*, not as instructions. `sprint-2.md:94`'s "4 files" descriptor annotated the same way. **Deliberately not rewritten**: editing a brief to match post-build reality erases the fact that the premise was ever wrong, which is the part worth keeping. | **fixed** |
| R2 | **CORRECT** | Defect (guard gap) | **Verified independently: 3438B of a 4096B cap (84%), overflow is `exit 1` at `fkit-claude-init.sh:340-344`.** My bullet consumed ~460B of the headroom and nothing guarded it. **Owner ruled (a): add the test.** New `test/rules-block-budget.test.js` — 3 cases: the emitted block is under `RULES_MAX`; a **92% warning threshold** so the ceiling is hit while there is still room to think; and both root files carry exactly one well-formed, correctly-ordered marker pair. Reads the cap **from the script**, so it does not pin a stale copy of the number. **Mutation-checked:** lowering the cap to 3000 turns 2 of the 3 red. | **fixed** |
| R3 | **CORRECT** | Defect (clarity) | Confirmed by reading: my bullet became the nearest antecedent of the singular *"**This preference** does not apply…"*, narrowing a clause that governs the whole section. One-word fix — now *"**These preferences** do not apply…"*. | **fixed** |
| R4 | **CORRECT** | Defect (clarity) | **Owner ruled: add the disclaimer** (~40B of the headroom, accepted knowingly alongside R2). The load-bearing-term enumeration now reads *"…and anything else the reader must be able to act on; the list is illustrative, not exhaustive"* — matching its sibling bullet, and closing the one seam the reviewer said was still open: a term **not** on the list reading as fair game to simplify. | **fixed** |
| R5 | **CORRECT** | Defect (record) | My plan's blockquote paraphrased the bullet it was approving — it dropped *"in a few words"*. Corrected to verbatim, with a note recording why it matters: **a plan that misquotes the thing it approves is a plan the owner did not approve.** | **fixed** |

### Verification of the fixes

- `npm test` — **365 pass / 0 fail** (3 new).
- **R2's guard mutation-checked:** `RULES_MAX` lowered 4096 → 3000 turns 2 of 3 new tests red; restored, 3/3 green.
- Block size after R4's addition: **3535B of 4096 (86%)** — still under the new 92% warning threshold,
  and now the threshold is enforced rather than eyeballed.
- All three homes **byte-identical** after re-running init (pairwise `diff` clean); R3's fix present in
  all three; exactly one marker block per root file.

### On the one contested finding

**Codex scored R2 "no finding"; fkit-reviewer kept it at medium and the owner ruled to build the
guard.** Both reviewers computed identical numbers (3438 / 658 / 464) — the disagreement was purely
whether 84% utilization with no regression guard was worth recording. **Recorded here because a
verdict split between two independent models is exactly the thing a ledger should preserve**, not
smooth over. The disagreement is noted in the test file itself, at the place a future reader would
question why the test exists.

### Still open, disclosed

- **This change is not behaviorally verified**, and no test can verify it: it is instruction text for
  LLMs. Only owner observation over time shows whether it changes how agents write.
- **Gitignored collateral, from the reviewer's own audit:** an earlier init refreshed `.claude/` while
  other tasks' sources were mid-edit, so `.claude/agents/fkit-coder.md` is now stale against
  `claude/agents/fkit-coder.md` (task 61). **Not a defect of this task** — `.claude/` is gitignored and
  init-regenerated on every launch — but recorded so the "no collateral" claim is complete rather than
  convenient.

## Accepted residuals (shared, do-not-re-litigate)

_(none yet — awaiting owner disposition of R2/R4)_
