# Plan — 0248: Update the docs for the structure-check capability

> Approved by the owner via `AskUserQuestion`, live `fkit lead` session, 2026-08-07 — verbatim
> selections: plan **"Approve (Recommended)"**; open question 1 ruled **"Fix here (Recommended)"**
> (edit D is in scope: fix the two now-false "read-only `/fkit-heal`" claims in
> `claude/README.md` and `claude/agents/fkit-producer.md`); open question 2 ruled **"File separate
> task (Recommended)"** (architecture.md §9.1's stale suite count is NOT absorbed — a separate
> small brief will be filed by a producer after this task closes). Plan authored by a spawned
> fkit-coder (`/fkit-plan-task`), presented by the fkit-lead driver, written to this file by the
> driver in the approval turn (fkit-sprint-ship-loop §Durable artifacts).

**Task:** `ai-agents/tasks/backlog/0248-update-the-docs-for-the-structure-check-capability/brief.md`
**Docs only — no behavior change, no commit, no wiki-vault write, no task-file move.**

## Ground truth verified on disk (2026-08-07)

- Deps 0242–0246 all in `ai-agents/tasks/done/`. **0247 landed too**: `structure_notice()` at `claude/fkit-claude.sh:373-488` (one stderr line, runs on every launch incl. `FKIT_SETUP_ONLY`, points at `/fkit-heal`, suppressed by `ai-agents/.fkit-accepted-drift`); the intent file exists at `ai-agents/.fkit-accepted-drift`, seeded with this repo's 9 known dogfood-drift paths. Per the brief's verification-step-4 rule, the notice is documented **as landed**, not "as filed".
- Shipped surface to describe: ADR-039 (companion licence; ADR-015 unchanged for the unattended path); `claude/structure-spec.md` (0243); `claude/structure-manifest.tsv` + `bin/generate-structure-manifest.mjs` + `npm run generate:manifest` (0244); the producer-owned `/fkit-heal` skill — `check.sh` (deterministic verdicts, presented verbatim; outcome vocabulary incl. `wiki-routed` → ADR-005; refusals fail loud) + `repair.sh` propose/apply (0245/0246); five `test/structure-*.test.js` suites; prove-red mutation 15 covers notice removal.
- ADR-039 v1 scope wording to stay consistent with (the SKILL.md carries it verbatim): consent-gated **replacement of untouched-stale fkit-authored files only — no move, no rename, no delete**; owner-edited files report-only with diffs; owner present, explicit approval of the **exact enumerated list** via `AskUserQuestion`; consent **never stored**; apply-time freshness re-check; never silent.
- `architecture.md` was already partially updated by 0245: §3/§4.2 say "26 skills"; the §4.2 producer row includes `heal` — but its parenthetical reads "read-only structure check — task 0245", **stale post-0246**.
- The scaffold's only convergence/content-drift prose is `claude/scaffold/ai-agents/README.md` § "How fkit keeps this folder up to date" (scaffold `CLAUDE.md`/`AGENTS.md` don't describe convergence). Its live twin `ai-agents/README.md` has the same section in adapted wording. The pair is on the dual-home parity **exception list** as audience-adapted (`test/dual-home-parity-exceptions.mjs`, `README.md` entry) — both homes get the edit, each in its own register; **no byte alignment** (aligning would violate the ADR-027/0186 audience-adapted kind).
- The manifest generator hashes **the working tree too** (`bin/generate-structure-manifest.mjs` `workingTreeFiles()`; structure-manifest test A: "INCLUDING the one this working tree is about to [ship]") — so the scaffold edit + regen in the same uncommitted change keeps `npm test` green. Generator requires full git history; this checkout is full.

## Edits

### A. `ai-agents/knowledge-base/architecture.md`

1. **§3 repository tree** — add the two new install-share files under `claude/` (`structure-spec.md` — what should exist and what each path means; `structure-manifest.tsv` — every shipped blob per path) and widen the `bin/release.mjs` line to a `bin/` entry also naming `generate-structure-manifest.mjs`.
2. **§4.2 producer skill row** — update the `heal` parenthetical from "read-only structure check — task 0245, ADR-039" to name check **+ consent-gated repair** (tasks 0245/0246, ADR-039). (Row membership untouched — 0250's `/fkit-task-brief` omission is a different file, `claude/scaffold/CLAUDE.md`, and stays out of scope.)
3. **§7 Key flows — new flow "7 — Structure conformance"**, appended after "6 — Release", covering, in shipped-state terms:
   - the determination layer in the install share: `structure-spec.md` decides *what should exist and what each path means* (class-annotated, wiki-routing notes); `structure-manifest.tsv` decides *touched-or-not* — every shipped blob per path, rename-aware across the scaffold's three historical homes plus the current working tree, CRLF-normalized, regenerated by `npm run generate:manifest`;
   - the producer-owned `/fkit-heal` check: deterministic script verdicts (`check.sh`), presented verbatim, outcome vocabulary incl. `owner-edited` (report-only with diffs), `wiki-routed` (repair is `fkit-wiki`'s exclusively, ADR-005), loud refusals; read-only in every check branch;
   - the repair path and its consent model, ADR-039 v1 scope verbatim-consistent (untouched-stale fkit-authored replacement only; no move/rename/delete; owner present; explicit `AskUserQuestion` approval of the exact enumerated list; consent never stored; apply-time freshness re-check; never silent);
   - the launch notice (0247, **landed**): one stderr line on divergence, every launch incl. setup-only; suppression via the tracked intent file `ai-agents/.fkit-accepted-drift` (sibling of `.fkit-keep-out`; silences the launch notice only — `/fkit-heal` still reports those paths in full);
   - governing records: ADR-015 **unchanged and in force** for the unattended launch path; ADR-039 the companion licence; test surface: the five `structure-*` suites + their prove-red mutations.

### B. `README.md` (repo root)

Extend the **"Staying current"** paragraph (or one short sibling paragraph beside it): a launch also tells you — one line — when your project's fkit-managed structure diverges from what the installed version ships; you run `/fkit-heal` in a producer session to see per-file verdicts and repair; repair is **in-session, consent-gated, diffs in view, applies only the exact list you approve — never silent**, and never moves, renames, or deletes anything; deliberate divergence is recorded in `ai-agents/.fkit-accepted-drift`.

### C. Scaffold docs pointers (the audience-adapted pair)

1. `claude/scaffold/ai-agents/README.md` — in "How fkit keeps this folder up to date", after the "Your edits are never 'corrected'" bullet (and referencing the keep-out section's neighborhood): the sanctioned path — the launch notice names diverging paths; `/fkit-heal` in a producer session shows what diverged and offers the consent-gated repair (untouched-stale files only, never a move/rename/delete, never silent); deliberate drift goes in `ai-agents/.fkit-accepted-drift`.
2. `ai-agents/README.md` (live twin) — the same pointer in its adapted register, at its "Content drift is not fixed" bullet.
3. **`npm run generate:manifest` in the same change** — the scaffold README is manifest-hashed; regen works from the working tree, no commit involved.

### D. The two stale "read-only `/fkit-heal`" claims — RULED IN SCOPE (open question 1)

- `claude/README.md:47` — producer row: `heal` "(read-only structure check)" → name check + consent-gated repair;
- `claude/agents/fkit-producer.md:41-43` — "/fkit-heal (the **read-only** structure-conformance report … it repairs nothing until the consent-gated repair phase ships" → the repair phase shipped (0246); rewrite to the landed consent-gated shape.
Minimal, docs-prose only, canonical `claude/` sources — never `.claude/` copies.

## Explicitly not touched

- `ai-agents/wiki-vault/` — 0249, `fkit-wiki` only.
- `claude/scaffold/CLAUDE.md` producer row — 0250 owns it.
- architecture.md §9.1's "eight `node --test` contract suites" — **19 on disk**; pre-existing staleness wider than this capability. **RULED: separate task (open question 2)** — not absorbed here.
- `claude/structure-spec.md`, the heal skill, any script — no behavior change.

## Sequencing

1. architecture.md (A1→A3). 2. Root README (B). 3. Scaffold pair + manifest regen (C). 4. D (ruled in scope). 5. Verification.

## Verification

1. Spot-check every v1-scope sentence written against ADR-039/SKILL.md wording: untouched-stale only, no move/rename/delete, never silent, consent never stored, apply-time re-check.
2. Root README names the check and consent-gated repair; grep the touched docs — nothing describes a silent auto-update.
3. `npm test` green — specifically `structure-manifest.test.js` test A against the regenerated manifest (run it once *before* regen to watch it fail — proves the regen was load-bearing) and `dual-home-parity.test.js` (README pair stays on the exception path).
4. Manifest diff inspection: only `ai-agents/README.md` row(s) for the scaffold change — no unrelated churn. If 0250 lands first, its `CLAUDE.md` rows will already be present; regen on top is additive, no conflict.
5. Every claim cites shipped state — 0247 described as landed (notice + intent file), nothing described as "filed" that shipped.
6. `git status --porcelain` — nothing under `ai-agents/wiki-vault/`.

## Edge cases / risks accounted for

- **Audience-adapted pair:** the two README edits must NOT be byte-aligned — differing wording is the recorded design (0186/ADR-027 third kind); parity test enforces via exception, so no test asserts alignment either way.
- **Manifest regen environment:** generator hard-fails on a shallow clone; this checkout is full. Keep LF endings (manifest is CRLF-normalized, but a content edit is a content edit).
- **Dogfood self-reference:** `ai-agents/README.md` is itself listed in `.fkit-accepted-drift` and is `owner-edited` class in this repo's own check — editing it further changes no verdict and no test hashes the live copy.
- **Structure-spec drift test (0243):** content-only edits, no file added/removed from the scaffold inventory — stays green.
- **Flow numbering:** new architecture.md flow appended as "7 —"; §8+ section numbers are unaffected (flows are numbered within §7, not as sections).

## Open questions — RULED by the owner 2026-08-07 (see header)

1. **Include the two stale "read-only `/fkit-heal`" fixes (edit D)?** Recommendation: yes, include here. **RULED: "Fix here (Recommended)".**
2. **architecture.md §9.1 claims "eight `node --test` contract suites"; disk has 19.** Recommendation: do not absorb — file a separate small task. **RULED: "File separate task (Recommended)"** — the driver routes the filing to a producer after this task closes.
