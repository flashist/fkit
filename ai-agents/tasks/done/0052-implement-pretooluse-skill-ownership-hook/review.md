# Review — implement-pretooluse-skill-ownership-hook

Task: ai-agents/tasks/backlog/implement-pretooluse-skill-ownership-hook.md (task 43)
Design: ai-agents/knowledge-base/decisions/adr-018-pretooluse-skill-ownership-hook-replaces-consult-skills-exception-list.md
File(s) under review: claude/skill-ownership-hook.sh (new), claude/skills-for-role.sh (new),
claude/fkit-claude.sh, claude/README.md, claude/skills/fkit-team/SKILL.md, claude/scaffold/CLAUDE.md,
test/skill-ownership-hook.test.js (new), test/launcher-contract.test.js — uncommitted working tree.
Status: in-review

## Reviewer findings

| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1     | high | claude/skill-ownership-hook.sh:12-20; ADR-018:78-79 | The hook uses `exit 2` instead of ADR-018 Decision 3's explicit, adopted mandate ("an explicit `hookSpecificOutput.permissionDecision` ... Never a bare exit code standing in for the decision"). The stated justification — that a hook emitting `{"hookSpecificOutput":{"permissionDecision":"deny",...}}` on stdout with exit 0 is "silently ignored" — does not hold up: I independently reproduced, live, against the actual installed Claude Code binary (2.1.211), that this exact JSON shape with exit 0 **does** block the tool call, for both a `Bash` call and a real `Skill` call (the `dataviz` skill). Codex's independent pass converged on the same conclusion by reading the binary/docs, hypothesizing the coder's test likely omitted the required `hookSpecificOutput.hookEventName:"PreToolUse"` field (my own decompilation of the binary's `pXr()` function confirms that field is required for the JSON path to register a deny). `exit 2` does also work correctly (confirmed by the passing test suite and by tracing the same binary code), so this is **not a live functional defect** — but it means ADR-018 (accepted, dated today) now inaccurately describes what shipped and why, based on debunked evidence. There may still be a legitimate reason to prefer `exit 2` (it is a strictly simpler, less schema-fragile fail-closed path — the JSON route fails **open** on a schema/validation mismatch, per the same binary trace), but that is a different justification than the one currently written, and needs an explicit owner call either way. |
| R2 | 1     | high | test/prove-red.sh:67-68; test/launcher-contract.test.js (Group B, test 8) | `test/prove-red.sh` — the launcher-contract suite's mutation-testing hard gate (task 23 / ADR-014, "a test that has never failed has not been tested") — is broken by this diff. I ran it directly (`sh test/prove-red.sh`) and it fails even at step **0b, the unmutated-copy baseline** (which is supposed to rule out "red for the wrong reason"): all 7 per-role Group-B test-8 assertions plus test 11 go red against ANY copy of `claude/`, mutated or not. Root cause: the new Group B test 8 hardcodes the expected hook `command` string using the real checkout's absolute path (`REPO` from `test/harness.mjs`), but `prove-red.sh` deliberately runs the suite against full copies of `claude/` under a temp dir via `FKIT_LAUNCHER` — a copy legitimately generates its own (different, but correct-for-that-copy) absolute path, which now never matches the hardcoded expectation. Separately, and independent of the above: Mutation 1 (`prove-red.sh:68`) edits `skills_for_role()`'s reviewer entry via a `sed` targeting `fkit-claude.sh`, but that function moved to `skills-for-role.sh` in this diff — the sed is now a no-op against the copy, so even if the path issue were fixed, this specific mutation would no longer prove what it claims to. Not caught by `npm test` (`prove-red.sh` isn't wired into it) — that's why it shipped unnoticed. Needs a coder fix to `prove-red.sh` and/or the Group B test before this is safe to merge. |
| R3 | 1     | medium-high | test/skill-ownership-hook.test.js:50-93 | The exhaustive per-role × per-skill ownership matrix the old Group B owned (7 roles × 21 skills) is not fully replicated here. Coverage is representative sampling (reviewer owns 2 skills, coder owns 1, producer/architect get one negative case each, universal skills get full per-role coverage) — not exhaustive. There is no test asserting, e.g., that `wiki` cannot invoke `fkit-plan-task`, that `producer` cannot invoke `fkit-wiki-ingest`, or that `adversarial-reviewer` owns only `fkit-adversarial-review` and nothing else. Since `skills_for_role()` is the sole source of truth for a security boundary, a future accidental mis-scoping of a skill to the wrong role has real gaps where nothing would catch it. (Raised independently by the Codex pass, X4; verified by reading the actual test file.) |
| R4 | 1     | medium | claude/fkit-claude.sh (build_settings, hook wiring); ADR-018 §Consequences | `disableAllHooks` is a real, documented Claude Code settings key (confirmed present in the installed binary's strings) that disables **all** hooks process-wide from any settings source. Since this lockdown is now entirely hook-based, a project or user settings file (including a `.claude/settings.local.json` the operator already controls, per this repo's own file) can turn off the *entire* role-ownership gate with one key — a materially different, single-point-of-failure risk profile than the old per-skill `skillOverrides` off-list, which required touching many entries individually. Not discussed anywhere in ADR-018's Consequences / Residual risks. This does not defend against a hostile third party either way (it requires the operator's own settings, the same actor the tool already serves) — but it is a genuine change in fragility that isn't currently written down. Worth recording, not necessarily blocking. (Raised independently by the Codex pass, X1, at "critical" — I assess it lower given the threat-model boundary is unchanged, but it is real and undocumented.) |
| R5 | 1     | medium | claude/README.md:28-30; claude/scaffold/CLAUDE.md:16; claude/fkit-claude.sh:460 | Three concrete, verified spots of leftover stale text directly contradict the corrected hook-based explanation this same diff adds elsewhere in the *same files*: (a) `claude/README.md:28-30` still says the lock is "`--settings` with `skillOverrides`" that turns a skill `"off"`, "hidden from the `/` menu **and** unrunnable" — untouched by this diff, and now contradicted a few lines later by the updated "skill lockdown" section that correctly describes the hook and says foreign skills stay **visible**. (b) `claude/scaffold/CLAUDE.md:16` still says "every other role's skills turned off" — untouched, contradicted by the corrected paragraph the diff adds further down the same file. (c) `claude/fkit-claude.sh:460` prints, to every user at every session launch, `"locked: only the <role>'s skills exist here"` — literally false now that ADR-018 Decision 5 makes foreign skills visible-but-blocked, not nonexistent. The task's own item 4 asked for exactly this class of correction in `fkit-team/SKILL.md` and `scaffold/CLAUDE.md`; the fix in scaffold/CLAUDE.md is real but incomplete within its own file, and README.md needed the same correction but wasn't in the task's named list. (Raised independently by the Codex pass, X6; verified by reading all three files directly.) |

## Coder response

Every finding verified independently before acting — not deferred to the reviewer's or Codex's say-so.

| # | Verdict | Defect / Frontier | Action | Status |
|---|---------|--------------------|--------|--------|
| R1 | CORRECT, and worse than I initially treated it | Defect (ADR/code mismatch) | **Re-verified R1's core factual claim myself, live, before touching anything**: retested the JSON-on-stdout route with `hookEventName` included — confirmed it DOES block the call. My original test omitted that field; that omission is exactly the schema-fragility this now documents. Corrected the two in-repo comments (`claude/skill-ownership-hook.sh` header, `test/skill-ownership-hook.test.js` header) that stated the debunked "silently ignored" claim. Consulted `@fkit-architect`, who correctly identified this as a live contradiction between shipped code and ADR-018 Decision 3's adopted text ("never a bare exit code"), not a documentation-only fix, and declined to record any ADR edit without owner sign-off. **Owner decision (2026-07-16): implement the JSON route**, matching Decision 3 as literally written. Done: `claude/skill-ownership-hook.sh`'s `deny()` now emits `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"..."}}` on stdout with exit 0, for every deny path; `allow()` unchanged (bare `exit 0`, empty stdout). **No ADR-018 edit was needed** — the ADR already mandated JSON verbatim; it was the code that had diverged, so switching the code resolves the contradiction without touching the ADR. Updated `test/skill-ownership-hook.test.js` throughout: every deny assertion now parses stdout and pins `hookEventName`/`permissionDecision` (`assertDeny()`), not just an exit code. **Caught a real regression of my own while doing this**: since allow and deny now share exit code 0, the matrix's allow-branch and every other allow assertion had to be strengthened to also require empty stdout (`assertAllow()`) — without that, a role wrongly stripped of an owned skill would still show as "passing" (exit 0), just via the deny path instead of the allow path, silently defeating the matrix's whole purpose. Confirmed via `sh test/prove-red.sh`: red-then-green cycle at the correct named assertion (`reviewer × fkit-review`) both before and after this fix. Full suite 285/285 green. | ✅ done, verified |
| R2 | CORRECT | Defect (mine) | Root cause was two-fold: (a) Group B test 8 hardcoded this checkout's absolute hook path instead of deriving it from whichever launcher (`LAUNCHER`/`dirname`) is actually under test — fixed in `test/launcher-contract.test.js`. (b) Mutation 1's `sed` target didn't move when `skills_for_role()` moved to `skills-for-role.sh` — fixed by retargeting the mutation at the new file and adding `FKIT_SKILL_OWNERSHIP_HOOK` (mirroring `FKIT_LAUNCHER`'s pattern) so `test/skill-ownership-hook.test.js` can be redirected at a mutant copy. Re-ran `sh test/prove-red.sh`: now passes, and reds at the correct named assertion (`reviewer × fkit-review`), not just "some failure." | ✅ done, verified |
| R3 | CORRECT | Defect (coverage gap) | Added an exhaustive 7-role × 21-skill hard-coded matrix to `test/skill-ownership-hook.test.js` (mirrors the old Group B's `OWNED`/`UNIVERSE` philosophy per ADR-014 §5 — hard-coded, not derived from `skills_for_role()`). 147 new matrix assertions; full suite now 285/285. | ✅ done, verified |
| R4 | CORRECT | Frontier (documentation gap, not a new hole) | Agree with the reviewer's own severity assessment — same threat-model boundary (operator's own settings either way), genuinely undocumented. **Owner decision (2026-07-16): yes, add it.** Consulted `@fkit-architect`, who verified the claim (grepped repo, confirmed no prior mention in ADR-018; corroborated by both the reviewer's and Codex's independent findings), and added a 5th bullet to ADR-018's "Residual risks / re-raise only if" list (Consequences section) — an in-place documentation addition to the ADR's own residual-risks section, not a new ADR or a Decision-reopening edit. Confirmed present on disk. | ✅ done, verified |
| R5 | CORRECT | Defect | Fixed all three: `claude/README.md:28-30`, `claude/scaffold/CLAUDE.md:16` (both corrected to describe the hook, not the retired off-list), and `claude/fkit-claude.sh:460` (the per-launch printed message — "...are runnable here" instead of "...exist here", matching Decision 5's visible-but-blocked reality). | ✅ done, verified |

### A note on R1's severity, stated plainly
The architect's read is right and I should have caught the ADR-contradiction framing myself before consulting — I initially treated R1 as "a stale comment," and it's actually "shipped code that violates an adopted ADR mandate." That's a materially different thing, and it's exactly the kind of gap `fkit-process-stateful-review`'s "verify every claim, don't defer" discipline exists to catch — in this case the architect caught it, not me, which is itself worth naming rather than smoothing over.

## Accepted residuals (shared, do-not-re-litigate)
- **R1** — resolved by owner decision (2026-07-16): implementation switched to the JSON deny route,
  matching ADR-018 Decision 3 as written. No ADR edit was required. Settled — not to be re-raised.
- **R4** — resolved by owner decision (2026-07-16): `disableAllHooks` residual-risk bullet added to
  ADR-018's Residual risks list by `@fkit-architect`. Settled — not to be re-raised (per that bullet's
  own re-raise condition: only if the kill-switch becomes reachable by someone other than the
  operator).

All five findings (R1–R5) are now ✅ done and verified. Round 1, closed.

---

## Report

**Verdict: ⚠️ Changes requested — 5 findings (0 live-runtime security holes; 2 high needing resolution before merge, 3 medium).**

### Reviewers run
- **Lead pass (me):** full read of the diff, the ADR, the task file, and the actual `skills_for_role()` mapping; ran the full `npm test` suite (138/138 green); additionally ran **live, empirical reproductions against the real, installed Claude Code binary** (`claude -p`, no `--dangerously-skip-permissions`) to check the single most load-bearing claim in the diff — see R1. Also directly executed `test/prove-red.sh`, which is not part of `npm test` — see R2.
- **Codex adversarial pass:** ran via `codex exec --sandbox read-only`. Completed (second attempt; first hit the tool's default 2-minute Bash timeout — reran with a longer one per the skill's own guidance). Findings: X1–X6 (see below for disposition of each).

### Disposition of the Codex findings
- **X1** (disableAllHooks, "critical") → recorded as **R4**, reassessed to **medium** (same threat-model boundary as before; genuinely undocumented fragility change, not a new hole against a hostile third party).
- **X2** (JSON-deny claim likely based on an invalid test) → recorded as **R1**; independently reproduced live by me, not just accepted on Codex's say-so.
- **X3** ("tests never exercise Claude loading the settings / real agent_type") → I closed most of this gap myself during this review: I ran a real, live `claude -p` end-to-end test with the actual `claude/settings.json`-style wiring and the actual `dataviz` skill, in both allow and deny configurations, and it worked as designed. I did **not** get a full live cross-role consult chain to trip the hook (the model correctly declined, on its own judgment, to bypass its role's self-restraint when I tried to force it as a "test harness" — which is itself good behavior, but means that specific path is still only unit-tested, not live-tested end-to-end). Not elevated to its own row; noted here as a partial mitigation of a real but now-narrower gap.
- **X4** (matrix coverage) → recorded as **R3**, verified by reading the test file directly.
- **X5** (prove-red.sh mutation 1 sed no longer matches) → folded into **R2**, which is broader: I found the *unmutated baseline* also fails, not just the mutation.
- **X6** (stale docs) → recorded as **R5**, verified by reading all three cited files directly (all three checked out true).

### A note on how this review was conducted
Invoking my own `fkit-stateful-review` skill via the `Skill` tool failed in this session with `Error: Skill fkit-stateful-review is disabled for model invocation in skillOverrides settings` — the **exact** pre-fix error this task exists to eliminate. I traced this: it is **not** evidence against the diff. It's an artifact of my own review environment — `.fkit/settings/*.json` are gitignored, regenerated only by the launcher/init script, and this session's copy still holds the **old**, pre-diff `skillOverrides` blob (confirmed by reading it directly). I followed the `fkit-stateful-review` procedure manually instead (reading the skill file directly, since I retain `Read` access to it) rather than deferring or skipping steps.

### Suppressed as settled (not re-raised)
- **Menu visibility of foreign skills** (ADR-018 Decision 5) — accepted cost, not re-raised.
- **Narrow, no-`jq` extraction approach itself** — I adversarially constructed a payload designed to smuggle a decoy `"skill"` key through the `args` field ahead of the real one; it correctly **failed closed** (denied), because JSON's own escaping of embedded quotes breaks the naive `grep` pattern's literal match before it can produce a false *allow*. Not a defect — noted for completeness, not raised as a finding.
- **Coder's disclosed non-actions** (task file not moved/status not updated, no commit) — correct, not this role's call either.

### Convergence call
This is Round 1 — nothing here re-litigates a prior round or an existing accepted residual (there were none). **Recommend: act, not closeout.** R2 (broken mutation-testing hard gate) and R1 (ADR/implementation mismatch on the single most load-bearing design fact) both warrant a fix or an explicit, recorded owner decision before this merges; R3–R5 are real but lower-urgency cleanup. None of the findings show the hook failing to enforce role ownership at runtime — the mechanism itself works, live-verified.

## Questions for the owner (I have no channel to you directly — the coder relays these)

1. **R1** — Given the JSON-deny mechanism does work (independently reproduced), do you want (a) ADR-018 amended to record the corrected empirical finding and an explicit case for keeping `exit 2` anyway (its fail-closed robustness against a future schema mismatch), or (b) the implementation switched to match the ADR's current mandate? Either is defensible; the current state (silently diverging code + ADR) is not.
2. **R2** — Should `prove-red.sh` be fixed as part of closing out this task (it's the direct consequence of this diff), or tracked as a fast-follow? Given it's the project's own "a test that has never failed has not been tested" hard gate for this exact suite, I'd recommend treating it as in-scope, but that's the owner's call on sequencing.
3. **R4** — Is the `disableAllHooks` single-kill-switch fragility worth a line in ADR-018's residual risks, or is it considered out of the threat model (operator-controlled settings) and not worth recording?
