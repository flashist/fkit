# Review — 0412

Task: ai-agents/tasks/done/0412-let-the-read-only-board-reader-serve-another-projects-ai-agents-tree-root-flag/brief.md
File(s) under review: bin/fkit-board.mjs, test/board-root.test.js (new), README.md (board section) — working tree vs HEAD
Status: closed-out
Coverage: both reviewers measured (round 3) — Codex (`codex-cli 0.152.0`, exit 0, foreground under a 560 s cap) ran `node --check bin/fkit-board.mjs` (passed), `resolveRoot` against fkit's own tree (resolved) and `node --test test/board-root.test.js` (1 passed, 8 blocked by its sandbox's `EPERM` on `mkdtemp`); the Claude reviewer ran `npm run test:unit` (1012/1012), `test/board-root.test.js` (9/9), 7 mutation probes on a scratch copy of the reader (5 caught, 2 survived — R11), and 9 `--bench --root` fixture probes (unreadable `tasks/done`, `sprints/`, `sprints/cancelled`, a child that is a file, a broken child symlink — all refused; a read-but-not-search dir reproduces R10). Phase-2 close-out (not a review round): the README-only delta was checked by the Claude reviewer alone — no Codex pass, per the skill's Step 6 (phase 2 skips the review passes).

> **Owner rulings in force for this ledger (2026-09-23, selected option text via `AskUserQuestion`, relayed by `fkit-lead` — not the owner's own prose):** always fkit's own `dashboard.sh` (target's copy never runs; accepted downside: board and target's `/fkit-status` may disagree while the target is stale); collision = warn only, as a separate `warnings` array on `/api/check`, `ok` unchanged; plan approved as written. These are not re-raised below.

## Reviewer findings

| #  | Round | Sev  | Location | Claim |
|----|-------|------|----------|-------|
| R1 | 1 | low | bin/fkit-board.mjs:590 (with `arg()` at :577) | Duplicate `--root` is not refused: `arg()` takes the first occurrence, so `--root A --root B` serves A and never validates B, and `--root A … --root` (trailing, bare) slips past the "needs a path" refusal because `arg()` already found A. Banner names A and nothing falls back to fkit's own tree, so the blast radius is small — but it is the same "silently ignored flag" class the `--root=` / trailing-`--root` guards exist to stop. Raised by both reviewers; reproduced by both. Defect. |
| R2 | 1 | low | test/board-root.test.js:305 | `assert.equal(check.ok, check.problems.length === 0)` is a tautology — it restates the server's own `ok` expression (bin/fkit-board.mjs:548) and cannot fail. With `problems` checked only for the words `claimed by` (:306), a collision implementation that pushes a differently-worded entry into `problems` and flips `ok` would pass. Measured: the R6 fixture yields `problems: []`, so `ok === true` and `problems` deep-equal `[]` are assertable. Raised by both. Defect (test). |
| R3 | 1 | low | test/board-root.test.js:215 (fake at :62-69) | R4/R8 is titled "never run" but proves only "its answer is never used": the fake `dashboard.sh` has no side effect, so an implementation that probed the target's copy first and fell back to fkit's own on a missing marker would pass. The current code does NOT do this (only two spawn sites, bin/fkit-board.mjs:314 and :633, both take `home`'s dashboard; :621 only stats the target's copy) — this is regression protection for ruling 1's headline invariant, not a live bug. A fake that leaves a sentinel would pin it. Codex X3 (Codex: medium; mine: low). Defect (test). |
| R4 | 1 | low | test/board-root.test.js (whole file) vs bin/fkit-board.mjs:672, :173, :161 | Mutation probes that SURVIVE the new suite: (a) aiboard's sibling default anchored to the target (`root: home` → `root` at :672) — every test passes `--aiboard`, so the "tools come from fkit's checkout" rule is pinned for `dashboard.sh` only, not for aiboard; (b) dropping `resolve()` on a relative `--root` (:173) — fails loud at the probe, so low; (c) `ai-agents/tasks` present as a FILE passes validation (:161 `!== 'dir'` → `=== 'missing'` survives) and would serve an empty board. Caught: target dashboard, `--root=` refusal, warnings on `/api/check`, warnings into problems, the `note` line. Defect (missing test). |
| R5 | 1 | low | bin/fkit-board.mjs:417 (`cacheKey`) | ⚠️ PRE-EXISTING (0411 code, unchanged by 0412). The key is per-board task counts + board-file count + max mtime, so renaming a board file (mtime preserved, count unchanged) leaves the WHOLE snapshot stale — reproduced: `done/sprint-4b.md` → `done/sprint-04.md` on a live reader kept listing `sprint-4b.md` and reported `warnings: []`; a fresh reader reports the S-004 collision. 0412's new warnings inherit this. The comment at :414-416 discloses the task-folder rename case only, not board files. Codex X2 (Codex: medium; mine: low — needs an mtime-preserving rename, cleared by any later edit or a restart). Defect, out of 0412's scope — disposition is the owner's. |
| R6 | 1 | low | test/board-root.test.js:132-141 | R1 counts every `^\d{4}-` entry in the live tree's three boards, but the reader counts only DIRECTORIES holding a `brief.md` (bin/fkit-board.mjs:227-229). In this dogfooded repo, a task folder caught mid-creation (folder made, brief not yet written) by a concurrent agent turns R1 red for a reason unrelated to `--root`. Zero such folders today (checked). Flake risk, not a wrong result. Defect (test robustness). |
| R7 | 2 | low | bin/fkit-board.mjs:147-162 (`resolveRoot`, `kind()` = `statSync(p).isDirectory()`) with :219 (`listTaskDirs`, `if (!existsSync(dir)) continue;`) | `resolveRoot` checks only that `ai-agents/tasks` and `ai-agents/sprints` are directories, not that they can be READ — yet its refusal says "not a readable fkit project tree". Reproduced: a target whose `ai-agents/tasks/` is mode 000 passes validation, `existsSync(tasks/backlog)` then returns false, and `--bench --root` exits 0 reporting `corpus: 0 tasks` (served: an empty board, silently) — the "silent wrong answer" class the `--root` guards exist to stop. With `tasks/backlog/` itself mode 000, `readdirSync` throws (crash under `--bench`, a 500 per poll when serving). Needs a permission-broken tree, so rare. Raised by Codex (X2, medium — its failure mode was the 500; the silent-empty case is mine). Defect (edge). |
| R8 | 2 | nit | test/board-root.test.js:405-430 (R7 test: `try { … } finally { await new Promise((r) => server.close(r)); rmSync(ui.dir …) }` then a second `try` that holds `rmSync(fx …)`) | The fixture `fx` is removed only in the SECOND try/finally; a failed assertion inside the first try propagates out of its `finally` and skips the second, leaking the temp fixture. Only on a red run; every other test cleans up in a single `finally`. Raised by Codex (X3). Defect (test hygiene). |
| R9 | 2 | nit | bin/fkit-board.mjs:587 (`argv.filter((a) => a === '--root').length > 1`) and :592 (`argv.some((a) => a.startsWith('--root='))`) | Both guards scan RAW argv tokens, so another flag's VALUE that is literally `--root` or starts with `--root=` (e.g. `--aiboard --root --root <tree>`) is refused. Fails SAFE — exit 2 with a named cause, never a wrong tree; `./--root` works around it. Inherited from round 1's `--root=` guard; the R1 fix extends the same shape. Raised by Codex (X1, low). No change recommended — recorded so it is not re-found. Defect (cosmetic, fail-safe). |
| R10 | 3 | low | bin/fkit-board.mjs:169-178 (`resolveRoot` probe: `readdirSync(join(flag, rel))`) with :243-246 (`listTaskDirs`: `try { … statSync(brief) … } catch { continue; }`) | The R7 probe proves a directory can be LISTED, not SEARCHED. A board dir with read but no search bit (e.g. mode 644) passes, then every per-entry `statSync` fails and is swallowed, so its tasks vanish silently. Reproduced: `tasks/backlog/` at 644 → `--bench --root` exit 0, `corpus: 2 tasks` (3 in the tree). Same silent-wrong-answer class R7 closed; needs an odd permission on a directory, so rare. Coder disclosed it in round 2 as outside the approved R7 fix (owner ruling *"Fix: check it can be read"*). Defect (edge) — disposition is the owner's. |
| R11 | 3 | low | test/board-root.test.js:244-283 (unreadable-tree test, `cases` = `ai-agents/tasks`, `tasks/backlog`, `sprints/done`) | The test pins 3 of the 7 directories the probe walks. Mutation probes that SURVIVE: the task-board probe narrowed to `['backlog']` (drops `tasks/done`, `tasks/cancelled`) and the sprints probe narrowed to `['done']` (drops `sprints/cancelled`) — each would let an unreadable such dir through to an `EACCES` crash / 500 per poll. Also unpinned: `ai-agents/sprints/` itself, a child that is a FILE (`ENOTDIR`), a broken child symlink (`ENOENT`). The code is correct today — all of these measured refused, exit 2. Codex X1 (Codex: medium; mine: low — the probe is one data-driven loop, so a survivor needs a deliberate narrowing). Defect (missing test). |

**Round 2 — fixes verified (not trusted):** R1, R2, R3, R4 (a/b/c), R6 each confirmed in the code and by a mutation probe on a scratch copy that reintroduces the defect and turns the named test red: dup-guard removed → R3 red; collision flips `ok` → R6 red; target copy spawned before the fall-back → R4/R8 red (sentinel); aiboard default anchored to target → R9 red; `resolve()` dropped → R9-relative red; `!== 'dir'`→`=== 'missing'` → R3 red. Also caught: dashboard taken from the target (4 tests red), warnings emptied (R6 red), warning names made absolute (R6 red), note line dropped (R4/R8 red). No regression found from the fixes. R6's remaining window (a whole task created between R1's count and its `--bench` run) is inherent to counting a live tree and is not re-raised.

**Round 3 — fixes verified (not trusted):** R7 — code read at bin/fkit-board.mjs:164-179; mutation probes on a scratch copy: whole probe removed → unreadable-tree test red; sprints probe removed → red; children not recursed → red; failure swallowed → red; no-flag path probed too → red (5 caught; the 2 survivors are R11). Fixture probes: unreadable `tasks/done`, `sprints/`, `sprints/cancelled`, a child that is a FILE (`ENOTDIR`), a broken child symlink (`ENOENT`) → all exit 2 naming the dir; no-flag `--bench` on fkit's own tree unchanged (413 tasks, 11 boards). Behaviour change beyond R7, accepted as sound: a broken board symlink was silently skipped at HEAD and is now refused. R8 — one outer `finally` at test/board-root.test.js:450-479 owns both temp dirs; read and confirmed. R9 — accepted residual, not re-raised. No regression found. **Noted, not a row:** README's `--root` paragraph says the reader exits "naming what is missing" and that the tree must hold both dirs — it does not mention the new `unreadable:` refusal (doc nit). An unreadable `brief.md` crashes `--bench` (exit 1, loud) on any tree, `--root` or not — pre-existing 0411 read path, not silent, not a 0412 row.

**Phase 2 close-out (2026-09-23, owner dispositions relayed by `fkit-lead`, selected option text via `AskUserQuestion`):** R10 → accepted residual; R11 → accepted residual; README note → fixed by the coder before close. The coder's *Accepted residuals* entries for R10 and R11 were read and match the rulings and the code (probe `names = readdirSync(join(flag, rel))`, `listTaskDirs`' `} catch { continue; }`, the test's 3-entry `cases`) — kept as written, not duplicated. README check: the new sentence (board section, *"A tree whose boards cannot be read (a permission error) is refused the same way, naming the unreadable directory."*) matches `resolveRoot` — the `unreadable:` refusal throws `ROOT_UNRESOLVED`, `main()` prints it and returns 2, and the message says it does not fall back; no machine paths in the README diff. Nit, not a row: the same refusal also fires for a child that is a file (`ENOTDIR`) or a broken symlink (`ENOENT`), which "(a permission error)" does not name — the parenthetical reads as the common case, not a limit. Source check: `bin/fkit-board.mjs` and `test/board-root.test.js` last modified before the README edit and the round-3 processing, and every round-3 anchor (`resolveRoot` probe, `listTaskDirs` catch, the test's `cases`) reads as cited — no source change since round 3. No open finding: R1–R4, R6–R8 fixed; R9–R11 accepted; R5 deferred to 0413. Ledger closed out.

**Round 3 — disproven / suppressed:** none disproven; R5 and R9 suppressed as settled (see *Accepted residuals*); no round-3 finding re-litigates an owner ruling.

**Disproven / not recorded as rows:** none. **Suppressed as settled:** R5 (see *Accepted residuals*); no round-2 finding re-litigated an owner ruling.

**Out of scope, noted only:** the startup probe's `spawnSync` (bin/fkit-board.mjs:640, `const probe = spawnSync('bash', [dashboard, 'select-active', …`) passes no `timeout`, while the comment above `DASHBOARD_TIMEOUT_MS` (:72-77) says both call sites are guarded. Pre-existing at HEAD, unchanged by 0412, and 0412 runs fkit's own `dashboard.sh`, so no new hang path. Not a 0412 row.

**Evidence note for the coder's claim "prove-red 40 mutations green":** true, and irrelevant to 0412 — `test/prove-red.sh` carries no board-reader mutation (grep: its only `board` hits are dashboard/mover mutations). The reviewer's own 9 probes above are the only mutation evidence for this diff.

## Coder response

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT (sev: low — agreed; banner names the served tree, nothing falls back to fkit's) | Defect | Verified: `arg()` (bin/fkit-board.mjs:577) takes the first `--root`; the trailing check keyed on `arg()` (now :597) so `--root A … --root` passed. **Fixed:** a second `--root` now exits 2 — bin/fkit-board.mjs:584-591 (`argv.filter((a) => a === '--root').length > 1` → *"--root given more than once … does not fall back"*), before the `--root=` / trailing checks. Tests: two new R3 cases, test/board-root.test.js:209-210 (`--root A --root B`, `--root A --root`). Mutation probe (guard removed) → R3 red. | ✅ done |
| R2 | CORRECT | Defect (test) | Verified: test/board-root.test.js (old :305) restated the server's own `ok: problems.length === 0`. **Fixed:** pinned to values — `assert.equal(check.ok, true)` + `assert.deepEqual(check.problems, [])`, test/board-root.test.js:390-391 (replaces the tautology and the `claimed by`-only check). Mutation probe (collision pushes a differently-worded entry into `problems`) → R6 red. | ✅ done |
| R3 | CORRECT (sev: low — agreed; no live bug: the only spawn sites take `home`'s dashboard) | Defect (test) | **Fixed:** the fake `dashboard.sh` now `touch`es a sentinel beside itself (test/board-root.test.js:71-76, helper `fakeRan` :81); R4 asserts it absent after `--bench` (:247) and after serving (:273). Mutation probe (run the target's copy first, then fall back to fkit's) → R4/R8 red. R7's byte-level hash also covers the sentinel. | ✅ done |
| R4 | CORRECT (all three survivors confirmed as gaps) | Defect (missing test) | **Fixed, test-only:** (a) new test *R9 aiboard sibling default* (test/board-root.test.js:316) — target sits in `<tmp>/proj` with a stub at `<tmp>/aiboard/aiboard/web/index.html`; the reader must name fkit's sibling default (banner if it exists on the machine, error if not) and never the target's; `makeFixture` gained a `dir` option (:47). (b) new test *R9 relative --root* (:337) — `--bench --root <relative path>` exits 0 and the tree line prints the resolved path. (c) new R3 case `ai-agents/tasks` as a FILE (:192-194, :204). Mutation probes: `root: home`→`root` → R9a red; `resolve(flag)`→`flag` → R9b red; `!== 'dir'`→`=== 'missing'` → R3 red. | ✅ done |
| R5 | CORRECT (reproduced by reading: bin/fkit-board.mjs:417 `cacheKey` = per-board counts + board-file count + max mtime; a board-file rename that keeps mtime moves none of them) | Defect — pre-existing (0411), out of 0412's scope | **No code change** (owner ruling 2026-09-23: *"Separate backlog task"*; `cacheKey` not touched). Deferred to a follow-up task, filed separately. | won't fix in 0412 — deferred to follow-up task (owner ruling) |
| R6 | CORRECT (flake risk, not a wrong result) | Defect (test robustness) | **Fixed:** R1 now counts a `NNNN-` entry only if it is a directory holding a `brief.md` — the reader's own rule (bin/fkit-board.mjs:227-229) — test/board-root.test.js:142-153. | ✅ done |
| R7 | CORRECT (sev: low — agreed; needs a permission-broken tree) | Defect (edge) | Reproduced both cases before the fix: `ai-agents/tasks/` mode 000 → `--bench --root` exit 0, empty corpus; `tasks/backlog/` mode 000 → `EACCES … scandir` crash, exit 1. **Fixed:** `resolveRoot` now LISTS every directory the reader walks — `ai-agents/tasks/` + each of `backlog/done/cancelled` it holds, `ai-agents/sprints/` + `done/`/`cancelled/` — with `readdirSync` (already imported; `node:fs` list and `spawnSync(` count unchanged) and refuses a failure as `unreadable: <dir>/ (<code>)` under the same "does not fall back" message — bin/fkit-board.mjs:164-186. Test: new *R3: an unreadable --root tree …* (test/board-root.test.js:244), three chmod-000 cases (`tasks`, `tasks/backlog`, `sprints/done`), skips if chmod does not block reads (root). Mutation probes, 4/4 caught (see worklog). | ✅ done |
| R8 | CORRECT | Defect (test hygiene) | Verified by reading: a red assertion in the crawl `try` left through its `finally` and skipped the second `try` holding `rmSync(fx …)`. **Fixed:** R7 test now has ONE outer `finally` removing both temp dirs; the server is closed in an inner `finally` before the byte comparison — test/board-root.test.js:451-477. Forced-red check (crawl assertion, then byte assertion): fixture dirs leaked 0 both times. | ✅ done |
| R9 | CORRECT (reproduced: `--aiboard --root --root <tree>` → exit 2, "given more than once") | Frontier (fail-safe cosmetic; raw-argv scan) | **No code change** — owner ruling 2026-09-23, selected option *"Accept as residual"*. Recorded under *Accepted residuals*. | won't fix (frontier) |
| R10 | CORRECT (verified by reading: the probe only lists — bin/fkit-board.mjs:171 `names = readdirSync(join(flag, rel))` — while `listTaskDirs` swallows a per-entry `statSync` failure, :246 `} catch { continue; }`; so a read-but-no-search dir lists, then drops its tasks. Sev: low — agreed; needs an odd directory mode. Coder had disclosed it in round 2) | Frontier (limit of the owner's "check it can be read" fix; permission edge) | **No code change** — owner ruling 2026-09-23, selected option *"Accept as residual — A rare permission mistake, outside your 'check it can be read' ruling. Raise it again only if a real project hits it."* Recorded under *Accepted residuals*. | won't fix (frontier) |
| R11 | CORRECT (verified by reading: the test's `cases` list, test/board-root.test.js:245-249, holds `ai-agents/tasks`, `tasks/backlog`, `sprints/done` — 3 of the 7 dirs the probe walks: `tasks` + `TASK_BOARDS` (bin/fkit-board.mjs:53) + `sprints` + `done`/`cancelled`. Code correct today — reviewer measured all refused. Sev: low — agreed) | Frontier (test depth vs. a one-loop, data-driven probe) | **No code change** — owner ruling 2026-09-23, selected option *"Accept as residual — The code is correct for all 7, and the check is one loop over a list of folders."* Recorded under *Accepted residuals*. | won't fix (frontier) |

**Coder notes, round 1:**
- **Suppressed as settled:** none — no finding matches an accepted residual or a settled ADR; none re-litigates the three owner rulings in the header.
- **Regression / oscillation check:** none. R1's guard runs before the `--root=` and trailing checks and changes no single-`--root` path; the no-flag path is untouched.
- **R5 status wording:** the skill's status vocabulary has no "deferred" value; the cell says what happened rather than force-fit `won't fix (frontier)` (it is a defect, not a tradeoff) or `blocked`.
- **Full `npm test` after the fixes:** exit 0 — unit 1011/1011 pass, 0 fail, 0 skipped (1009 + 2 new R9 tests); `prove-red.sh` 40/40 mutations red, "hard gate PASSED". Six targeted mutation probes on the reader (one per fix) all caught — listed in the worklog.
- **Ready for reviewer round 2.** Header `Status:` left at `in-review` (R5 is deferred, not closed).

**Coder notes, round 2:**
- **Suppressed as settled:** none — R7–R9 match no accepted residual, ADR or header ruling.
- **Regression / oscillation check:** none. R7's probe runs only under `--root` (the no-flag path returns before it) and only after the `missing:` checks pass, so every round-1 R3 case keeps its message. Behaviour change beyond the two reproduced cases, stated: a `--root` tree where `tasks/<board>` or `sprints/done|cancelled` is a FILE, not a directory, is now refused as `unreadable: … (ENOTDIR)` — at HEAD the reader crashed on it (`readdirSync` of a file).
- **Known limit, not covered:** a directory with read but no search (`x`) bit lists fine, so it passes; the reader then drops its entries silently (per-entry `statSync` is caught). Rarer than R7's cases; not in the approved fix; not a row.
- **R9 wording:** classed Frontier here, not the reviewer's "Defect (cosmetic)" — the refusal is the fail-safe side of scanning raw argv, and the owner ruled to keep it.
- **Full `npm test` after the fixes:** exit 0 — unit 1012/1012 pass, 0 fail, 0 skipped (1011 + 1 new); `prove-red.sh` 40/40 red, "hard gate PASSED".
- **Ready for reviewer round 3.** Header `Status:` left at `in-review` (R5 still deferred to 0413).

**Coder notes, round 3:**
- **Suppressed as settled:** none — R10 and R11 match no accepted residual, ADR or header ruling. R10 is the limit the coder named in round 2 (*"Known limit, not covered"*), not a re-litigation of R7.
- **Regression / oscillation check:** n/a — no code change this round.
- **Reviewer's README note (not a row) — done.** Owner ruling 2026-09-23, selected option *"Yes — A one-line docs fix the coder makes before the task is closed, so the README matches what the code does."* README's `--root` paragraph (board section, after *"never falls back to fkit's own tree"*) now adds: *"A tree whose boards cannot be read (a permission error) is refused the same way, naming the unreadable directory."* No source changed.
- **Accepted residuals R10, R11 added by the coder** — the skill's Step 6 has the coder record an owner-confirmed tradeoff, as with R9 in round 2.
- **Header `Status:` left at `in-review`**, not set to `closed-out`: the README line is new and unreviewed. Every finding R1–R11 is now done, accepted, or deferred to 0413 — ready for the reviewer to check the README line and close out.

## Accepted residuals (shared, do-not-re-litigate)

<!-- Entries are added only once the OWNER approves treating a finding as a settled tradeoff. -->

- R5 board-file rename leaves the reader's snapshot stale — What: 0412 ships without touching `cacheKey` (bin/fkit-board.mjs, `function cacheKey(taskDirs, boardFiles)`); the fix is task 0413 (`ai-agents/tasks/backlog/0413-make-the-board-readers-snapshot-cache-notice-renames/`, depends on 0412) · Why (structural): pre-existing 0411 code, outside 0412's `--root` scope; owner ruling 2026-09-23, selected option *"Separate backlog task"* (via `AskUserQuestion`, relayed by `fkit-lead`); fixing it here would widen 0412's diff into the cache contract that 0411's suite pins · Re-raise only if: 0413 is cancelled without a fix, or a 0412 change touches `cacheKey` or the warnings path in a way that makes a stale snapshot worse than at HEAD.
- R9 `--root` guards scan raw argv, so another flag's value spelled `--root` / `--root=…` is refused — What: the duplicate-`--root` and `--root=` guards in `main()` (bin/fkit-board.mjs, `argv.filter((a) => a === '--root')` and `argv.some((a) => a.startsWith('--root='))`) check every token, not only flag positions; such a value exits 2 with a named cause; `./--root` works around it · Why (structural): the reader has no argv parser — `arg()` is an `indexOf` lookup — so telling a flag from a value would need one; the refusal fails SAFE (never a wrong tree, never a fall-back), and the case is unlikely; owner ruling 2026-09-23, selected option *"Accept as residual"* (via `AskUserQuestion`, relayed by `fkit-lead`) · Re-raise only if: the reader gains a real argv parser, a real path or flag value that begins with `--root` is reported, or a guard is changed so the collision serves a tree instead of refusing.
- R10 a board directory that can be listed but not searched passes the `--root` check, and its tasks vanish silently — What: `resolveRoot`'s probe (bin/fkit-board.mjs, `names = readdirSync(join(flag, rel))`) proves each board dir can be LISTED, not searched; `listTaskDirs` then drops every entry whose `statSync` fails (`} catch { continue; }`), so a dir with `r` but no `x` bit serves a short board with exit 0 · Why (structural): outside the owner's R7 ruling *"Fix: check it can be read"*; needs a hand-broken directory mode, so rare; closing it would mean a per-entry stat probe or turning the swallowed `statSync` into a refusal, which widens the reader's read path; owner ruling 2026-09-23, selected option *"Accept as residual — A rare permission mistake, outside your 'check it can be read' ruling. Raise it again only if a real project hits it."* (via `AskUserQuestion`, relayed by `fkit-lead`) · Re-raise only if: a real project hits it (a board served short because of a directory mode), or the probe or `listTaskDirs`' per-entry catch is changed.
- R11 the unreadable-tree test pins 3 of the 7 directories the probe walks — What: test/board-root.test.js, test *R3: an unreadable --root tree …*, `cases` = `ai-agents/tasks`, `tasks/backlog`, `sprints/done`; `tasks/done`, `tasks/cancelled`, `sprints/`, `sprints/cancelled`, a child that is a file and a broken child symlink are not pinned (all measured refused by the reviewer) · Why (structural): the probe is one data-driven loop over `TASK_BOARDS` and `['done', 'cancelled']`, so a survivor needs a deliberate narrowing of those lists; owner ruling 2026-09-23, selected option *"Accept as residual — The code is correct for all 7, and the check is one loop over a list of folders."* (via `AskUserQuestion`, relayed by `fkit-lead`) · Re-raise only if: the probe stops being one loop over those lists (per-directory special cases), or a regression lets an unpinned directory through.
