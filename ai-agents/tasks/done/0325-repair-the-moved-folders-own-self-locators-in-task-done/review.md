# Review — 0325

Task: `ai-agents/tasks/done/0325-repair-the-moved-folders-own-self-locators-in-task-done/brief.md`
File(s) under review: `claude/skills/fkit-task-done/SKILL.md` — working tree vs `HEAD` (`493cecd`). Round 1: +58/−2. Round 2 (after the R1–R7 fixes): +75/−4, five hunks — step 4 (`:116–119`, `:127–129`); step 5 new "The moved folder's OWN self-locators" bullet (`:190–249`); step 5 "Then prove it." (`:290–292`); step 7 report bullet (`:319–325`). Round 3 (after the R8 fix): +80/−4, five hunks — step 4 (`:116–119`, `:127–129`); step 5 self-locator bullet (`:191–253`; the three R8 sentences at `:222–224`, `:228–231`); "Then prove it." (`:294–297`); step 7 report bullet (`:323–330`; R8 sentence at `:325–326`). Out of surface: this folder's `plan.md` / `worklog.md` (read for context only).
Status: closed-out

*Round 2 (reviewer re-verify, 2026-08-26). Reviewers run: fkit-reviewer own pass + Codex adversarial pass (`codex exec --sandbox read-only`, codex-cli 0.145.0, exit 0) — full coverage, both completed. All seven Round 1 fixes re-read at their new lines; the seven-occurrence count re-measured at `git show c45ec3d` (`plan.md:5`, `:117`, `:188`; `review.md:3`, `:26`, `:86`, `:98`; the full-folder-name grep returns only `plan.md:5` and `review.md:3`; `0188` still under `backlog/`). R1, R3, R4, R5, R6, R7 verified closed. R2 closed for the count, the row, and the grep-scope sentence, but the new `:227` sentence conflicts with the pre-existing `:222–223` — recorded as R8 (new row) rather than re-opening R2. Codex's other new item (`:204` "by role, never by string" vs `:228–229` "an elided self-path … is not a locator") was checked against the corpus and not recorded: every elided own-path in a header block (13 lines across 12 `done/` ledgers) sits in a `File(s) under review:` value, Q2-frozen either way, and an elision does not "name a path to a file" (`:207`), so `:228–229` follows from the role prong; no live locator is affected.*

*Round 3 (reviewer re-verify of the R8 fix only, 2026-08-26). Reviewers run: fkit-reviewer own pass + Codex adversarial pass (`codex exec --sandbox read-only`, codex-cli 0.145.0, exit 0; prompt `.fkit/tmp/0325-r3-adversarial-prompt.md`, output `0325-r3-codex-out.txt`) — full coverage, both completed. Scope: the three sentences `:222–224`, `:228–231`, `:325–326`; settled items not re-opened. R8 verified closed: the freeze list now has one membership rule in all three places (a step 4 grep return, or an entry met while reading the header block for locators) and one no-hunt boundary ("nothing hunted for beyond the grep and the header block", `:230–231`); the elided `File(s) under review:` self-entry that R8 turned on is listed under every sentence; a below-header pointer enters only as a grep return (it cannot be met in the header block), so the R2(b) no-hunt boundary is not reopened and `:251–253` ("not a list step 7 asks you to compile") still holds — rows 3–7 of the `0250` table are neither grep returns nor header-block entries. Stays inside Q2 ("frozen and listed"): every `File(s) under review:` self-entry sits in the header block, which the producer always reads, so it is always met and always listed. One observation, raised by both reviewers, NOT recorded as a row: `:228–229` enumerates the header-block addition as "any `File(s) under review:` self-entry", while step 7 `:326` says "one met in the header block" — a strict superset. The gap is an elided self-path met in the header block that is not part of a `File(s) under review:` value; re-measured this round: 12 elided own-path lines in the header blocks of `done/` record files, all 12 inside a `File(s) under review:` value (`0103:9`, `0132:10`, `0147:6`, `0150:5`, `0161:7`, `0162:6–8`, `0174:8` are continuation lines of that field), zero outside it. No sentence tells the producer NOT to list such an entry; frozen under every reading; report completeness only; no live instance. Verdict: incompleteness of an enumeration, not a contradiction — severity info, below the row bar; disposition (leave as noted / open R9 / record as residual) returned to the owner as NEEDS-DECISION. `Status: closed-out` stands.*

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | low | `claude/skills/fkit-task-done/SKILL.md:213–216` — *"Not in a ledger's `File(s) under review:` line … both fail the header-block test above"* | **Wrong reason for a right outcome.** A `File(s) under review:` line sits *inside* the header block (`done/0250-…/review.md:4`, above the first `## ` at `:22`), so it passes the position prong of the test at `:203–205`; it fails the *asserts-nothing* prong. A cold reader who was just told *"That is the whole test"* meets a contradiction. Outcome (frozen + listed in step 7) is faithful to owner ruling Q2 (`plan.md` §4); only the stated reason is off. Wording defect. |
| R2 | 1 | low | `SKILL.md:222` — *"six occurrences, 2 repair / 4 freeze"*; `:216–219` — *"Step 4's folder-name grep already surfaces every literal self-hit"*; `:305–310` step 7 reason list | **Worked-example claims the repo does not back, and a step 5 ↔ step 7 mismatch.** (a) At close `0250`'s folder held **seven** `backlog/` lines, not six: `git show c45ec3d` (post-repair) still shows 5 (`plan.md` 2, `review.md` 3), and `review.md:26` — findings row R1 citing `` `ai-agents/tasks/backlog/0188-…/brief.md:54` `` — is the seventh; `0188` is still in `backlog/`, so it is the same freeze class as rows 5–6 (correct `backlog/`, another task) and the rule freezes it correctly, but the table is presented as exhaustive. (b) The step-4 grep (full folder name) returns only rows 1–2; rows 3–6 (elided `0250-...`, bare `tasks/backlog/`) are never surfaced, yet *"The role rule gets all six"* + step 7's *"every other self-hit from step 4 you classified as evidence"* leave a cold producer unsure whether to hunt for and list rows 3–6. (c) Step 7's reason phrases (*"captured output / dated measurement / plan text / another task's path"*) omit the two Q2 cases (`File(s) under review:` line, below-heading pointer) that `:215–216` says *"go in the step 7 freeze list"*, while *"another task's path"* is almost never reachable as a full-folder-name self-hit. Raised by both reviewers. |
| R3 | 1 | low | `SKILL.md:203–205` — *"a named field … whose value is a path to a file inside this same folder, and which asserts nothing"* | **Literal reading freezes real composite locators.** `done/0160-…/review.md:3` reads `Task: 0160 — `` `…/backlog/0160-…/brief.md` `` and `done/0218-…/worklog.md:8` reads `**Plan:** `` `…/backlog/0218-…/plan.md` ``, approved by …` — the value is not *a path* and the second one asserts an approval. `plan.md` §2 edge cases 4 and 6 promise both are covered ("re-point the path, keep the ID"); the shipped text never says so — only *"change nothing else on the line"* at `:197–198` hints at it. Safe direction (missed repair, no corruption), but it is the gap between plan and text. Raised by Codex; verified against the corpus. |
| R4 | 1 | low | `SKILL.md:204` — *"header block (above the first `## ` heading)"* | **Undefined when a record has no `## ` heading.** Measured: `done/0191-…/plan.md` uses `###` only; `done/0246-…/review.md` has no heading at all (2 of the `done/` record files). Either reading — nothing is header, or the whole file is — is defensible; the second would repair a body-prose pointer Q2 froze. Neither file carries a path locator today, so no live instance. Raised by Codex; verified. Whether to extend the Q2 definition is the owner's call — see NEEDS-DECISION in the Round 1 report. |
| R5 | 1 | low | `SKILL.md:126–127` — pre-existing *"Every hit it returns is handled in step 5; none is discarded. A reference you found and did nothing about is a link you broke."* | **Now contradicts the new freeze class.** A full-folder-name self-hit that is evidence (e.g. a `File(s) under review:` self-entry, a findings row naming the own brief) is correctly left byte-identical, so "did nothing about = a link you broke" is false for it. Blast radius small: the sentence defers to step 5, whose ⛔ paragraphs govern and whose "report both lists" makes a frozen hit *handled*, not discarded — so Codex's "high" is not supported. Wording defect. |
| R6 | 1 | low | `SKILL.md:118–119` — *"…the top-level `plans/` `worklogs/` `reviews/` directories no longer exist — **including the moved folder's own record files**, which point at themselves"* | **Clause attached to the wrong parent.** Read cold, "including the moved folder's own record files" lists things that *no longer exist*. The intended parent is "the plans, worklogs and review ledgers live **inside** the task folders" three lines up. Wording defect. |
| R7 | 1 | low | `SKILL.md:222` — *"`0250`'s folder as it stood at close"* (first mention of `0250` in the file) | **Bare ID on first mention.** [`conventions/durable-citation-anchors.md`](../../../knowledge-base/conventions/durable-citation-anchors.md) §"Citing a task": *"Write `` `NNNN` (`folder-slug`) `` on the first occurrence in a file, and bare `` `NNNN` `` after."* The reader is sent to a folder they have no name for. The six `:NNN` coordinates are each paired with a site + fragment, which satisfies §"Never cite a line number naked" (note `:86` / `:98` fragments start on `:85` / `:97` — recoverable via the fragment). Convention defect, low. |
| R8 | 2 | low | `claude/skills/fkit-task-done/SKILL.md:222–223` vs `:227` (and `:321–322`) | **The R2 fix introduced a contradiction about what the step 7 freeze list holds.** `:222–223` (pre-existing, Q2-faithful) says a `File(s) under review:` self-entry and a below-header forwarding pointer "both stay frozen **and go in the step 7 freeze list**" — unconditionally. The new `:227` says the two step 7 lists "are the grep's own returns, nothing hunted for beyond them", and step 7 `:321–322` asks only for "every other self-hit **from step 4**". The two disagree exactly where the Q2 case is *not* a grep return: an elided `File(s) under review:` self-entry. That shape is common — `done/0162-…/review.md:6–8` (`ai-agents/tasks/backlog/0162-.../plan.md` …), `done/0151-…/review.md:4`, `done/0253-…/review.md:4`, `done/0177-…/review.md:5`, `done/0132-…/review.md:10`, `done/0174-…/review.md:8` — so a cold producer closing a task with that ledger shape gets "list it" from `:222` and "the list is grep returns only" from `:227`. Blast radius: report completeness only — the entry is frozen under both readings, no file is corrupted. Q2's own wording ("frozen and listed in the report") favours listing what you meet; reconciling within that ruling (e.g. "the grep's returns, plus any Q2 case you meet on the way — never a sweep") is the coder's call, not an owner question. Raised by Codex (as "R2 partly closed"); verified against the corpus; severity re-derived to low. |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (wording) | Verified: `done/0250-…/review.md:4` `File(s) under review:` is above the first `## ` (`:22`), so it passes position and fails *role*. Reworded the ⛔ sentence (`SKILL.md:219–223`): the `File(s) under review:` line "sits in the header block, but its job is to record *what was reviewed* … so it fails the role prong"; the below-heading pointer "fails the position prong". Outcome (frozen + step 7 list, Q2) unchanged. Severity low — blast radius one sentence a cold reader trips on. | ✅ done |
| R2 | CORRECT | Defect (wording/spec) | (a) Counted at `c45ec3d`: five `backlog/` lines survive post-repair (`plan.md:117`, `:188`; `review.md:26`, `:86`, `:98`) + the two repaired locators (`plan.md:5`, `review.md:3`) = **seven at close**, not six; `0188` is still under `backlog/`. Table now says "seven occurrences, 2 repair / 5 freeze" (`:231–232`), row 7 added (`:242`: `review.md:26`, findings row, another task's path → freeze), trailer → "rows 5–7 … all seven" (`:244–249`). (b) Second ⛔ (`:225–229`) now says the grep surfaces "every self-hit that spells the full folder name", the two step 7 lists "are the grep's own returns, nothing hunted for beyond them", and a bare `tasks/backlog/` joins the elided form as not-returned; trailer adds "Only rows 1–2 are step 4 hits … rows 3–7 show what the rule says if you meet them, not a list step 7 asks you to compile" (`:247–249`). (c) Step 7 reason list (`:322–324`) adds `findings row`, `` `File(s) under review:` line ``, `pointer below the header block`; kept "another task's path" (a line may name both). No existing row's repair/freeze verdict changed. | ✅ done |
| R3 | CORRECT | Defect (spec gap vs `plan.md` §2 cases 4, 6) | Verified both composites on disk: `done/0160-…/review.md:3` `Task: 0160 — `` `…/backlog/0160-…/brief.md` `` (first `## ` at `:14`) and `done/0218-…/worklog.md:8` `**Plan:** `` `…/backlog/0218-…/plan.md` ``, approved by …` (first `## ` at `:16`). Definition (`:204–213`) now reads "whose value *names* a path … and whose job is to say where that file is", then names both forms: "The path need not be the whole value — `Task: 0160 — …/brief.md` carries an ID before it and `**Plan:** …/plan.md, approved by …` a clause after it; both are locators: re-point the path, keep the ID and the clause byte-identical." Safe direction before (missed repair); now matches the plan. | ✅ done |
| R4 | CORRECT | Defect (undefined case) — **owner ruled 2026-08-26: "Extend the definition (Recommended)"** | Verified: `done/0191-…/plan.md` has `###` only; `done/0246-…/review.md` has no heading (the only two such record files under `done/`). Definition (`:204–207`) now: header block = "above the first heading of level 2 or lower (`## `, `### `, …), or, when the file has no such heading, the leading field block it opens with (the run of `Key:` lines under the `# ` title)". The ⛔ sentence now says "below the header block" (`:222`), not "below the first `## `". Q2's header-block-only reach unchanged. Ruling recorded as a residual below. | ✅ done |
| R5 | PARTIALLY CORRECT | Defect (wording) | Not a strict contradiction: a self-hit classified as evidence and reported in step 7 *is* handled in step 5 and not discarded, so `:127`'s first sentence stays true — Codex's "high" is unsupported, as the reviewer says. The cold reading of "did nothing about" as "left byte-identical" is real, though. Added one parenthetical (`:128–129`): "(a self-hit you classified as evidence and *reported* frozen in step 7 is handled, not \"nothing\")". Pre-existing line, step 4, same file; no meaning change. | ✅ done |
| R6 | CORRECT | Defect (wording) | Confirmed the clause hung off "directories no longer exist". Re-parented (`:116–119`): "… live **inside** the task folders (`plan.md` / `worklog.md` / `review.md`) — **including the moved folder's own record files**, which point at themselves; step 5 rules that self case — and briefs cross-link each other; the top-level … directories no longer exist". Same words, new order, +1 line. | ✅ done |
| R7 | CORRECT | Defect (convention) | Checked [`conventions/durable-citation-anchors.md`](../../../knowledge-base/conventions/durable-citation-anchors.md) §"Citing a task": "on the first occurrence **in a file**" — no document-type exclusion, and `claude/skills/fkit-team/SKILL.md:40` already uses the form in skill text, so it binds here. First mention (`:231`) now `` `0250` (`fix-the-scaffold-producer-row-fkit-task-brief-omission`) ``; the prose mentions of `0324` and `0188` I author (`:244–246`) carry their slugs too. Quoted fragments in the table cells left as quoted (`:220` `0250-...` is an elided path, not a citation). | ✅ done |
| R8 | CORRECT | Defect (wording) | Verified: `:222–223` sent both Q2 cases to the step 7 freeze list unconditionally; the R2-fix sentence at `:227` said the lists are "the grep's own returns, nothing hunted for beyond them"; step 7 `:321–322` asked for step 4 hits only. They disagree exactly on an elided `File(s) under review:` self-entry — confirmed on disk at all six cited sites (`0151:4`, `0253:4`, `0177:5` spell `<NNNN>-…/`; `0162:6–8`, `0132:10`, `0174:8` spell `<NNNN>-.../`), and a header-block count finds 7 `done/` ledgers with the `…` form alone. Reconciled inside Q2 ("frozen **and listed**"), three sentences, no new search: (1) first ⛔ (`:222–224`) — "both stay frozen, and each one you *meet* — as a step 4 grep return, or while reading the header block for locators — goes in the step 7 freeze list"; (2) second ⛔ (`:228–231`) — the two lists are "the grep's own returns, plus any `File(s) under review:` self-entry the header block showed you while you read it for locators (an elided `0250-.../plan.md` there is not a grep return, but you met it) — nothing hunted for beyond the grep and the header block"; (3) step 7 (`:325–326`) — "every other self-hit — a step 4 return, or one met in the header block — you classified as evidence". The "elided self-path … is not a locator" sentence (`:231–233`) untouched. Regression check vs R2(b): the no-hunt boundary is now named explicitly (grep + header block), so the hunt ambiguity R2(b) closed does not reopen; a below-header pointer the grep did not return is still not sought. Severity low — report completeness only; the entry is frozen under every reading. | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- Owner rulings 2026-08-26 (`plan.md` §4), recorded here so later rounds do not re-open them: **Q1 "No test (Recommended)"** · **Q2 "Header block only (Recommended)"** — a self-locator is a named field above the first `## ` heading; body-prose forwarding pointers and `File(s) under review:` self-entries are frozen and listed in the report · **Q3 "Follow-up brief (Recommended)"** — `fkit-task-cancelled` mirror is brief `0342`. Re-raise only if: a live instance shows the header-block line freezing a genuine locator (Q2) or repairing evidence.
- **R4 header-block extension** (owner ruling 2026-08-26, relayed by the driver, verbatim label **"Extend the definition (Recommended)"**) — What: the header block is "above the first heading of level 2 or lower, or the whole leading field block when the file has none"; Q2's header-block-only reach is unchanged · Why (structural): two `done/` record files (`0191` `plan.md` — `###` only; `0246` `review.md` — no heading) had no defined header block, and the rejected reading (the whole file is the header) would have repaired body-prose pointers Q2 froze · Re-raise only if: a live record's leading field block runs into prose that carries a self-path the extension now repairs but Q2 meant frozen.
