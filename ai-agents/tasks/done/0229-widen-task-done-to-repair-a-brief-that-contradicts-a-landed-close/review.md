# Review — 0229

Task: 0229 — [brief](./brief.md)
File(s) under review: `claude/skills/fkit-task-done/SKILL.md` (working tree vs HEAD, +32/−5 after the Round-1 fixes — Round 1 reviewed +28/−5: step-1 label `One exception:` → `First exception:`; the new "Second exception: the contradicted-close repair" bullet with four must-nevers and the "any other value ⇒ plain stop" closer; two step-7 clauses)
Status: closed-out

Round 1 — 2026-08-27. Reviewers run: fkit-reviewer own pass + Codex adversarial pass (`codex exec --sandbox read-only`, codex-cli 0.145.0, exit 0 — **full coverage**). Settled inputs honoured and not re-raised: owner rulings Q1–Q5 (plan §4), "Ship 0229 standalone", ADR-033 §1/§5, ADR-034 (own-record findings → residuals). Line numbers below are claims against the working-tree revision read on 2026-08-27, paired with quoted fragments per `conventions/durable-citation-anchors.md`.

Round 2 — 2026-08-27. **Verdict: ✅ Ready to merge — no new defects; converged.** Reviewers run: fkit-reviewer own pass + Codex adversarial pass (`codex exec --sandbox read-only`, codex-cli 0.145.0, exit 0, "NO NEW FINDINGS" — **full coverage**). Scope: the step-1 second-exception bullet + the two step-7 clauses only (hunks `@@ -81`, `@@ -85 +85,26`, `@@ -311`, `@@ -315,2`). No new rows: nothing novel found. Re-verification of the Round-1 outcomes:
- **R1 fix closes the T3 trap** (`SKILL.md:90-93` *"**"For this task" means the row's Brief cell links this task's folder** — a prose mention, a quoted specimen row, or another task's row that merely cites this folder in its description is not a landed close"*). Walked by reading: `0014` — folder-name grep under `ai-agents/sprints/` returns `backlog.md:239` (Brief cell links `0296`) and `:244` (Brief cell links `0301`), both leading cell `🔲 Backlog`, neither Brief cell links `0014`'s folder → no landed close → must-never 1 → stop. `0021` — `sprints/done/sprint-1.md:45` `| ✅ Done | 9 | … | [..](../../tasks/done/0021-build-fkit-reconnect-tooling/brief.md) |`; `0041` — `sprint-1.md:40` `| ✅ Done | 4 | … | [..](../../tasks/done/0041-fix-claude-agents-md-placeholder-text/brief.md) |` — one hit each, plain `✅ Done`, Brief cell links the task's own folder → fires; brief (currently `🔲 Backlog` at `brief.md:13` in all three) resolves to plain `✅ Done` for `0021`/`0041` only. Also checked: no plain `✅ Done` row under `sprints/` carries a stale `tasks/backlog/` href for a folder now in `done/` (grep, zero hits), so "links this task's folder" has no path-form ambiguity to bite on today.
- **R2 fix holds** (`SKILL.md:98-101`): must-never 1 now reads *"never fire when no board row reads plain `✅ Done`"* and names the agent-closed-row case with its own stop (*"A row reading `✅ Done (agent-closed — not owner-verified)` beside an open-work brief is also not this branch's case: stop, and report what the row reads"*). Q2 outcome unchanged — no fire; the stop message now describes that case correctly.
- **R3 residual recorded correctly**: the *Accepted residuals* entry carries What / Why (structural, with the rejected one-word widening) / owner ruling 2026-08-27 "Record as residual (Recommended)" / Re-raise only if. The three sentences `:225` *"(the re-run and owner-upgrade paths must be no-ops here)"*, `:292` *"the file you just moved into `done/`"*, `:316` *"Only the brief just moved is touched"* are byte-identical to HEAD (`:200`/`:267`/`:291` there; `diff` of the three lines → empty). Neither re-raise condition is met.
- **Both directions on the changed lines:** no new ambiguity found ("Brief cell" is the table's last column — `backlog.md:103` header `| Status | Priority | Task | Brief |`, same shape in `sprint-1.md`; a body `**Status:**` line or a `➡️ Moved` row is not a status-table row per step 5); no contradiction with the first exception (`:82-85`, unchanged, still gates on `✅ Done (agent-closed — not owner-verified)` and must-never 3 routes that value there), with the "Resolve the status value FIRST" table (owner ⇒ plain `✅ Done`), with step 4's grep (`:148`, the exception narrows it to `ai-agents/sprints/`, recursive, `sprints/done/` included), with step 5's landing branch (`:300-301` "Reads anything else … Replace it with `✅ Done`"), or with step 7 (`:336-337`, `:341-343` name the exception and the value read). Re-run after a repair: brief begins `✅ Done` → closer's plain stop (`:110`). Spawned producer: "you are an agent" (`:66`) + must-never 2 (`:102-103`) → stops. No naked `:NNN` in added text (`git diff -U0 | grep '^+' | grep -E ':[0-9]+'` → empty). Mirror `diff claude/skills/fkit-task-done/SKILL.md .claude/skills/fkit-task-done/SKILL.md` → identical. `node --test test/*.test.js` → 774 pass, 0 fail.
- **Suppressed as settled (not re-raised):** Q1–Q5 (plan §4); the Q3 residual (ship-loops' "one exception" prose, for `0135`); R3's disposition.
- **Convergence:** Round 2 surfaced nothing new; every remaining observation is either a verified Round-1 fix or a recorded residual. Recommend closeout — `Status: closed-out` stands.

## Reviewer findings
| #  | Round | Sev    | file:line | Claim |
|----|-------|--------|-----------|-------|
| R1 | 1     | medium | `claude/skills/fkit-task-done/SKILL.md:88-90` — *"run step 4's grep now, restricted to `ai-agents/sprints/` … find a status-table row for this task whose leading cell reads plain `✅ Done`"* | **Raised by both reviewers.** The fire test's discriminator — "for this task" — is left implicit. Step 4's grep keys on the folder name `<NNNN>-<slug>`, which also appears **inside other tasks' row descriptions** (live: `0014`'s folder name is hit only via `0296`'s row at `ai-agents/sprints/backlog.md:239`, a `🔲 Backlog` row; `0296`'s own text records this exact loose-match trap as "T3"). A cold owner-present reader who takes "grep hit + line is a status-table row + leading cell `✅ Done`" as the test would fire on a foreign task's `✅ Done` row that merely cites this task — writing an owner-verified `✅ Done` with no landed close, which is what must-never 1 (`:96`) forbids. The restricted grep also reaches `ai-agents/sprints/reviews/` (two ledgers exist), where a quoted board row could match. **No live specimen today**: scanned all 81 plain `✅ Done` rows under `ai-agents/sprints/` — none names, in its description cell, another task whose folder sits in `done/` with an open-work brief; `sprints/reviews/` has no `✅ Done`-leading row. Latent, not firing on `0021`/`0041`/`0014`. Direction: state the discriminator — the row's **Brief (last) cell links this task's folder** — and say a hit that is prose, a quoted row, or another task's row is not a landed close. |
| R2 | 1     | low    | `claude/skills/fkit-task-done/SKILL.md:96-97` — *"never fire when no board row reads `✅ Done` — … a close starts from `backlog/` through the ordinary path; stop and say so"* | Must-never 1 says "reads `✅ Done`" where the fire sentence (`:90`) says **plain** `✅ Done`, and the closer's "a value that already begins `✅ Done`" (`:105`) is about the **brief's** value, not the board row's. The Q2 case (board row `✅ Done (agent-closed — not owner-verified)` + open-work brief) is therefore refused only by the word "plain" at `:90`; must-never 1's rationale ("a close starts from `backlog/`") misdescribes that case (folder already in `done/`, board already says agent-closed), so the stop message a cold reader would give is wrong for it. Not a re-litigation of Q2 — the outcome (no fire) is honoured; only the wording of the restriction and its stop message are loose. Direction: "no board row reads **plain** `✅ Done`", plus one clause: an agent-closed row beside an open-work brief stops and reports what the row reads. (Worklog decision 2 attributes coverage of this case to the closer — own record, ADR-034 — noted, not a row.) |
| R3 | 1     | low    | `claude/skills/fkit-task-done/SKILL.md:221` — *"(the re-run and owner-upgrade paths must be no-ops here)"*; `:287-288` — *"the file you just moved into `done/`"*; `:313` — *"Only the brief just moved is touched here"* | Pre-existing wording that the first exception already ran through; the second exception now opens a third no-move path through steps 4–7 and none of these three sentences names it. The self-locator no-op clause holds generically ("a locator that already names `done/` is left byte-identical"), and Codex judged the skip-move instruction overrides "just moved" sufficiently — I agree it does not misfire (`0021`/`0041`/`0014` folders hold only `brief.md`, so there are no self-locators to touch at all). Recorded because the plan (§Step 5) asserted `:221` "is also true of this path — left byte-identical": true in effect, but the parenthetical's enumeration is now incomplete. Direction: optional one-word widening ("the re-run, owner-upgrade and repair paths"), or leave for `0135`, which will re-edit this branch. |

Disproven / checked-and-fine (not rows — so the coder is not asked to chase them):
- **Identity gate** — `:86` *"If you are the **owner**"* and `:98-99` must-never 2 (*"a producer **spawned** to close is an agent (ADR-033 §5) and stops here exactly as it does on the first exception"*) use the same gate words as the first exception (`:82`, `:85`); the "Resolve the status value FIRST" table (`:61-68`, *"If you are unsure which you are, you are an agent"*) binds both. No reading found under which a spawned producer fires it. Both reviewers.
- **`sprints/done/` reach** — `:89` names it explicitly; step 4's grep is recursive (`:145`, `:148`). `0021` → `sprints/done/sprint-1.md:45` `| ✅ Done | 9 | …`; `0041` → `sprint-1.md:40` `| ✅ Done | 4 | …`; folder-name grep returns exactly one hit each, both plain `✅ Done`, Brief cell links the task → fires, resolves plain `✅ Done`.
- **`0014`** — folder-name grep under `ai-agents/sprints/` returns one hit, `backlog.md:239`, leading cell `🔲 Backlog` (another task's row) → no plain `✅ Done` row → must-never 1 → stops. (This is also the R1 hazard's shape, with a leading cell that happens not to be `✅ Done`.)
- **Agent-closed brief** (e.g. `tasks/done/0024-…/brief.md:13` reads `✅ Done (agent-closed — not owner-verified)`) — "a value that already begins `✅ Done`" (`:105`) is not the second exception's case; first exception (owner) or stop (agent). Must-never 3 holds.
- **`➡️ Moved` row / prose mention** — leading cell `➡️ Moved …` ≠ `✅ Done`; step 5 `:186` still forbids flipping it. A prose mention is not a status-table row per step 5's definition (`:170`).
- **ADR-033** — no new value written (owner-present resolves plain `✅ Done` per `:63`); agent-closed → plain upgrade left to the first exception (`:100-102`); no move (`:103`); `skills-for-role.sh`, hook, `test/skill-ownership-hook.test.js` untouched (`git diff --stat`).
- **Step 5 landing branch** — `:300-301` *"Reads anything else (`🔲 Backlog`, …)? Replace it with `✅ Done`"* resolves plain for the owner; board row already `✅ Done` → no-op edit; idempotent on re-run (brief then begins `✅ Done` → plain stop).
- **Step 7 clauses** (`:332-333`, `:337-338`) — consistent with step 5; report can name the exception and the value read.
- **`fkit-task-cancelled` step 1** — `done/` → "confirm with the owner"; no exception; no contradiction introduced.
- **No naked `:NNN` added** to the skill text (`git diff -U0 | grep '^+' | grep -E ':[0-9]+'` → empty). `ADR-033 §5` is a section cite.
- **Mirror** — `diff claude/skills/fkit-task-done/SKILL.md .claude/skills/fkit-task-done/SKILL.md` → identical.
- **`0021`/`0041`/`0014` still drifted** — briefs read `🔲 Backlog`; untouched by this diff, as the brief requires.

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (latent) | Verified: step 4's grep keys on the folder name; `0014`'s two `sprints/` hits are foreign `🔲 Backlog` rows (`backlog.md:239` `0296`, `:244` `0301`); `0021`'s row (`sprint-1.md:45`) has the folder link in its last cell. Severity **re-assigned low** (not medium): no live specimen, owner-present only, needs a foreign plain `✅ Done` row citing a done/-foldered open-work brief. Fixed anyway — one clause, no outcome change for `0021`/`0041`/`0014`. Applied at step 1 (`SKILL.md:90-93`, *"**"For this task" means the row's Brief cell links this task's folder** — a prose mention, a quoted specimen row, or another task's row that merely cites this folder in its description is not a landed close. If such a row exists, do **not** stop"*). Applied under the sprint-ship-loop standing approval: verified CORRECT, mechanical, inside the approved rule's intent. | ✅ done |
| R2 | CORRECT | Defect (wording) | Verified: must-never 1 dropped "plain" and its rationale fit only the no-row case. Severity low (Q2 outcome was already honoured by `:90`; only the stop message was wrong). Applied (`SKILL.md:98-101`): *"**never fire when no board row reads plain `✅ Done`** — with no row at all there is no landed close: … A row reading `✅ Done (agent-closed — not owner-verified)` beside an open-work brief is also not this branch's case: stop, and report what the row reads"*. Q2 outcome unchanged (no fire). Worklog decision 2's attribution corrected in the Round-1 worklog entry (own record, ADR-034 — no row). Same standing-approval basis as R1. | ✅ done |
| R3 | CORRECT | Frontier (plan-scoped deferral) | Claim accurate: `:225` *"(the re-run and owner-upgrade paths must be no-ops here)"*, `:292` *"the file you just moved into `done/`"*, `:316` *"Only the brief just moved is touched here"* do not name the repair path; holds in effect (skip-move instruction governs; `0021`/`0041`/`0014` folders hold only `brief.md`, no self-locators). **No edit this round**: the owner-approved plan settles exactly this point (§Step 5 "no edit"; old `:200` "left byte-identical") and `0135` re-edits this branch, so a widening here is outside the standing approval (ADR-019/032: outside-plan ⇒ stop). Owner ruled 2026-08-27 (live `AskUserQuestion`, relayed by the driver): "Record as residual (Recommended)" — entry added below. | won't fix (frontier) |

## Accepted residuals (shared, do-not-re-litigate)
- **Ship-loops' "one exception" prose + `:78-82` / `:283-286` cites** (`fkit-sprint-ship-loop/SKILL.md:293-299`, `fkit-task-ship-loop/SKILL.md:192-193`) — What: left as written · Why (structural): owner ruling Q3 2026-08-27 "Leave; record for 0135 (Recommended)"; the count claim "one exception" is now false, but each loop's **conclusion** — *no agent can repair this; it is the owner's* — stays true because the second exception is owner-only too · Re-raise only if: a loop's conclusion becomes false (e.g. an agent-runnable repair path lands), or `0135` closes without touching them.
- **Step-5 / self-locator sentences do not name the repair path (R3)** (`fkit-task-done/SKILL.md:225` *"(the re-run and owner-upgrade paths must be no-ops here)"*, `:292` *"the file you just moved into `done/`"*, `:316` *"Only the brief just moved is touched here"*) — What: left as written; the second exception's skip-move instruction governs and the clauses hold in effect (locators already naming `done/` are no-ops; `0021`/`0041`/`0014` folders hold only `brief.md`) · Why (structural): the owner-approved plan (2026-08-27, §Step 5 "no edit"; old `:200` "left byte-identical") settles this point, and `0135` re-edits this branch — a widening now would be an out-of-plan edit that `0135` would re-touch anyway; rejected alternative: the one-word widening ("re-run, owner-upgrade and repair paths") · Owner ruling 2026-08-27, live `AskUserQuestion` in the `fkit lead` session driving `/fkit-sprint-ship-loop`: "Record as residual (Recommended)" · Re-raise only if: the repair path misfires on a self-locator (a locator naming `done/` gets rewritten), or `0135` closes without touching these sentences.
