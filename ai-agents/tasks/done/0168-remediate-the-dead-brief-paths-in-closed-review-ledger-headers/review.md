# Review — 0168

Task: 0168 — [brief](./brief.md)
File(s) under review: line 3 of 68 `ai-agents/tasks/done/*/review.md` (67 rewrites + `0080` insert) — working tree vs HEAD `493cecd`; task artifacts `plan.md` (blob `84eba663…`), `worklog.md`
Status: closed-out

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | low | `worklog.md:693` (freeze table, "Line 1 `# Review — <slug>` titles" row) — *"(plan cited 46 across the corpus; 24 sit inside this set)"* | **Carries the plan's wrong "46" forward and implies 22 more sit outside the set.** Re-derived 2026-08-26: `done/*/review.md` whose line 1 is not `# Review — NNNN` = **24 corpus-wide**, all 24 inside the 68 (`plan.md:70` "46 old ledgers" was the mis-measure). Worklog-only; no diff line depends on it. Fix = restate as "24 corpus-wide, all in the set (plan's 46 was wrong)". |
| R2 | 1 | nit | `worklog.md:720` (Residuals bullet) — *"28 `done/` ledgers / 41 distinct dead path tokens below line 3"* | **"41 distinct" is the per-ledger-distinct SUM; corpus-distinct is 37.** The freeze table at `:692` labels it "distinct per file" correctly; the bullet drops the qualifier — the same label slip `0160` §4.6 (R10) corrected in itself ("55 vs 57"). Fix = "41 per-ledger-distinct (37 distinct corpus-wide)". |
| R3 | 2 | nit | `worklog.md:697` (freeze-table row) and `:747` — *"The plan's '46 old ledgers' … counted a broader class — every line 1 that is not exactly `# Review — NNNN`"* | **States the coder's reconstruction of the plan as the plan's own intent.** `plan.md:56` labels the 46 as *"line 1 `# Review — <slug>` titles"*, and the plan's own vocabulary (`plan.md:24-26`) uses `<slug>` for the no-ID form and `<NNNN>-<slug>` for the ID form — so the plan's **label** names the 24-class while its **number** matches only the 46-class (verified: 121 ledgers → 24 no-ID / 46 not-exactly-`# Review — NNNN`; 34 in / 12 out, the 12 named at `:697` all reproduce). Label and number disagree; "counted a broader class" is the best-fitting reading but is inferred, not recorded. Worklog-only; no diff line depends on it. Fix = one clause: "the plan's `<slug>` label names the no-ID class (24) but its 46 matches only the broader not-exactly-`# Review — NNNN` class". Raised by Codex; R1's PARTIALLY CORRECT grading itself stands (46 is a real count of a real class). |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | PARTIALLY CORRECT | Defect (record wording, worklog-only) | Re-measured 2026-08-26: line-1 titles with **no 4-digit ID** = 24 corpus-wide, all 24 in the 68-set, 0 outside — the "24 sit inside this set" parenthetical did wrongly imply 22 more outside; **fixed** at `worklog.md` freeze-table row. But the plan's 46 is not a mis-measure: it counts every line 1 that is not exactly `# Review — NNNN` (23 slug-only + 22 `NNNN-slug` + 1 prose = 46; 34 in set / 12 outside, all `NNNN-slug`). The row now states both classes rather than "46 was wrong". Severity mine: low, no diff line depends on it. | ✅ done |
| R2 | CORRECT | Defect (record wording, worklog-only) | Re-measured with `classify.py`'s `PATH_RE` + positive existence test over lines ≥ 4: 28 files / **41 per-file-distinct** / **37 corpus-distinct** (4 tokens each in 2 ledgers) — reproduces exactly. `worklog.md` Residuals bullet now reads "41 per-ledger-distinct … (37 distinct corpus-wide — 4 tokens each appear in 2 ledgers)". Freeze-table Count cell left as is (its class column already says "distinct per file"). Severity mine: nit. | ✅ done |
| R3 | CORRECT | Defect (record wording, worklog-only) | Verified: `plan.md:56` label "`# Review — <slug>` titles" + `:24-27` vocabulary (`<slug>` = no-ID form, `<NNNN>-<slug>` separate) name the 24-class; the number 46 matches the broader class. Both worklog clauses (freeze-table row, Process-review table row) now say label and number disagree and that "broader class" is my inference from the number. Counts untouched. Severity mine: nit. | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- Owner rulings 2026-08-26 (`plan.md` §7, verbatim labels), recorded so later rounds do not re-open them: **D1 "Dead-only, 67 (Recommended)"** · **D2 "`Task: NNNN — [brief](./brief.md)` for all 67 (Recommended)"** · **D3 "Insert `Task: 0080 — [brief](./brief.md)` (Recommended)"** · **D4 "Defer to 0326 (Recommended)"**. Also binding: `0160` §4.6 (c) + §4.2.1/§8.2 R19 (replace the label, keep the target; bare `Task: NNNN` also correct), `0325` evidence freeze (`File(s) under review:`, body citations, verbatim quotes, trailers stay byte-identical). Re-raise only if: a dead header was missed, a live header or any non-line-3 byte was touched, or `0326` lands a different schema form.
- Out-of-scope residuals named by the plan/worklog, not this task's: 28 ledgers with body-level dead paths; `ai-agents/sprints/reviews/sprint2-*.md:3` Gap-B headers; `0248 plan.md` / `0218 worklog.md` (`0343`); `0168` board cell "needs 0160, hard"; `0175` guard red until `0326`.

*Round 1 (2026-08-26). Reviewers run: fkit-reviewer own pass (measured: `node --test test/*.test.js` → 774/774 pass, 24 suites; all 67 replacements + `0080` insert verified mechanically; independent dead count at HEAD = 67, identical ID set; recorded dry-run diff `cmp`-identical to applied diff) + Codex adversarial pass (`codex exec --sandbox read-only`, codex-cli 0.145.0, exit 0, **reasoning-only second opinion per ADR-042 — Codex could not run the suite (`mkdtemp` EPERM); all execution evidence is the Claude reviewer's**). Codex: "No findings", items 1–8 all passed. Both rows above are the Claude pass's; Codex spot-checked the same freeze figures and found them reproducing.*

*Coder response Round 1 (2026-08-26): both findings verified first-hand (counts above), both fixes applied under the sprint-ship-loop standing approval (mechanical, in the task's own worklog, no `done/*/review.md` byte touched); recorded in `worklog.md` decision log. No residual added. Status → closed-out.*

*Round 2 (2026-08-26). Scope: the two worklog fixes (R1 row `:697`/`:747`, R2 bullet `:724`/`:749`) and the work product's immobility. Reviewers run: fkit-reviewer own pass (measured: `git diff --numstat -- 'ai-agents/tasks/done/*/review.md'` = 69 files — **67 × `1 1`, `0080` `1 0`, `0203` `3 3` — byte-for-byte the Round 1 shape, HEAD still `493cecd`; title classes re-derived over 121 ledgers: 24 no-ID all in set, 46 not-exactly-`# Review — NNNN` = 24 no-ID (23 slug + `0080` prose) + 22 `NNNN-slug`, 34 in / 12 out, the 12 IDs identical to `:697`; R2 with the worklog's exact `PATH_RE` over lines ≥ 4: 28 / 41 / 37, 4 tokens in 2 ledgers each — all reproduce) + Codex adversarial pass (`codex exec --sandbox read-only`, codex-cli 0.145.0, exit 0, **measured pass this round — its python re-counts ran and succeeded inside the sandbox**). Codex raised one item (R3 above); Claude pass raised none. Coder's R1 grading (PARTIALLY CORRECT — 46 is a correctly-measured broader class) is upheld as a count; the residual is that the plan's label contradicts its number (R3). Coder response rows checked against the worklog text: every claimed fix is present and none is broader than claimed. No settled item re-raised.*

*Coder response Round 2 (2026-08-26): R3 verified and fixed under the standing approval (two clauses in `worklog.md`); decision log updated. Status remains closed-out.*
