# Review — 0134

Task: ai-agents/tasks/done/0134-decide-the-sanctioned-repair-path-for-a-half-landed-close/brief.md
File(s) under review: ai-agents/knowledge-base/decisions/adr-048-a-half-landed-close-gets-a-producer-only-reconcile-mode-that-never-upgrades-the-marker.md
Status: closed-out
Coverage: both reviewers measured — scoped to ADR number + link integrity only: Claude ran `node --test test/adr-number-uniqueness.test.js test/reference-integrity.test.js` (exit 0, 36/36); Codex (`codex-cli 0.152.0`, exit 0) ran the same two plus `coordination-citation-policy` (exit 1, fixture tests denied `mkdtemp` by the read-only sandbox; live uniqueness and reference-integrity checks passed). Every finding below is reasoning over prose — no test reads ADR content.

## Reviewer findings
| #  | Round | Sev  | Location | Claim |
|----|-------|------|----------|-------|
| R1 | 1 | medium | ADR-048 § Q5, "Order among step 1's branches" | *"Exceptions 1 and 2 are unchanged"* + *"The mode is a third branch"* contradicts the exceptions' literal agent-stop sentences, which pre-empt BOTH reconcile orientations for a spawned producer: `claude/skills/fkit-task-done/SKILL.md:85` (*"An agent hitting this case still stops"* — brief agent-closed, row open), `:100-103` (*"stop, and report what the row reads"*; *"never fire for a non-owner identity … stops here"* — brief open, row agent-closed), `:108-110` (plain stop for a value beginning `✅ Done`). ADR does not say whether `0135` amends those sentences or places the mode ahead of them → an open decision for `0135` (brief verification item 6). Fails closed (mode never fires; loops fall back to owner). Raised by both. |
| R2 | 1 | medium | ADR-048 § Q5, "Locations" | Rows get a discriminator (*"Brief cell links this task's folder"*); *"every epic slice Status cell for this task"* and *"every in-body `**Status:**` line for this task"* get none. A misattributed open-work location satisfies (c) → the mode fires while brief and board agree (owner constraint 1) and writes the agent-closed value onto a line that is not this task's (must-never 4 in effect). No live instance in this repo today (no in-body Status lines under `ai-agents/sprints/`, no `Parent / Epic` briefs), but the ADR is install-wide doctrine. |
| R3 | 1 | medium | ADR-048 § "The three carve-out sites", bullet *"Either order works"* | False as stated. The loop marks *"the location that is still stale"* and is barred only from writing over a landed `✅ Done` (`claude/skills/fkit-sprint-ship-loop/SKILL.md:305-306`, `:337`; `claude/skills/fkit-task-ship-loop/SKILL.md:197-198`, `:277`). A location reading `⛔ Cancelled …` or an unrecognised string is "stale" to the loop; Blocked-first converts it to open-work, so clause (e)'s refusal is bypassed and the mode overwrites it — defeating must-never 8. Order is load-bearing. |
| R4 | 1 | low | ADR-048 § Q5, edge case *"A brief with no `## Status` → refuse"* | The refusal lives outside clauses (a)–(e), which read as the full predicate: no brief Status + a row agent-closed + an epic/in-body location open satisfies (a)–(e). A multi-line `## Status` (step 5 flags it, `claude/skills/fkit-task-done/SKILL.md:294-295`) is also unclassified. Fold both into the predicate or the value classes. Raised by both (Codex medium). |
| R5 | 1 | low | ADR-048 § Q3, *"The rest of each line follows step 5's existing rules"* and item 2 *"Hrefs still pointing at the `backlog/` path"* | Boundary with the rest of step 5 unstated: epic "next slice" prose on other lines (`claude/skills/fkit-task-done/SKILL.md:180-181`), self-locators (`:216-227`), the moved folder's outbound sibling links (`:206-214`), the reference-integrity guard run (`:343-344`), and the step-7 `Moved: none — <first \| second> exception` line (`:393-394`) with no third value. `0135` must decide include/exclude. *"`backlog/` path"* also assumes the folder left `backlog/` (step 3 allows other boards, `:131`). Raised by both (Codex medium). |
| R6 | 1 | low | ADR-048 § Q5 *"A `➡️ Moved …` row is a **pointer, not a location**"* and § Owner sign-off *"clause (e), the branch-order paragraph, and \"copies, does not resolve\""* | The Moved-row call is unmarked and missing from the sign-off inventory (three listed), though `worklog.md` § Decision log records it as architect's call 2 and says *"Each is marked in the ADR text"*, and the Deciders line promises every non-ruled point is marked. The rule itself is sound (mechanical from step 5 `claude/skills/fkit-task-done/SKILL.md:183-190` + D2 + plan must-never 7); the defect is the disclosure. Raised by both (Codex medium). |
| R7 | 1 | low | ADR-048 § Q3, *"(Architect's call, forced by must-never 1's \"anywhere\".)"* | Basis mis-cited: must-never 1 forbids upgrading an existing marker; writing plain onto an open-work location is not an upgrade. The real forcing is D1(a) (*"Agent-closed value only (Rec)"*), D1(c) not chosen, and must-never 3. Unrecorded consequence the owner should see: owner-present producer + brief agent-closed / row open → exception 1 governs (`claude/skills/fkit-task-done/SKILL.md:81-85`) → plain everywhere; owner-present producer + brief open / row agent-closed → mode governs → agent-closed on the brief. Same disagreement, different outcome by which side landed. |
| R8 | 1 | low | ADR-048 § "`0229`'s exception — kept, owner-only", *"Named residual — the owner-side gap stays open"* | Residual inventory incomplete. A second no-door state is unnamed: brief reads plain `✅ Done` + a row reads agent-closed (with or without other open work). Exception 1 needs an agent-closed brief (`claude/skills/fkit-task-done/SKILL.md:82-83`), exception 2 an open-work brief (`:86-87`), `:108-110` stops on a brief beginning `✅ Done`, and the mode refuses under (d) / must-never 7 — so it escalates to an owner who has no door either. Re-raise trigger 2 does not cover it. |
| R9 | 1 | low | ADR-048 § Q5, clause (d) *"that value is exactly `✅ Done (agent-closed — not owner-verified)`"* | "Exactly" undefined — whole cell or leading marker? Step 5 permits suffixed landed forms (epic `✅ Done (PR #NN)`, `claude/skills/fkit-task-done/SKILL.md:178-180`; rows use *"the same emoji/wording the table already uses"*, `:175-176`). Whole-cell reading refuses mover-written agent-closed cells; `0135` must pick. Fails safe. |
| R10 | 1 | low | ADR-048 § "The three carve-out sites", fallback list item *"no disagreement"* | A "no disagreement" refusal routes the loop to the carve-out — *"the sanctioned disagreement"* — when none exists. Under D2 an href-only staleness is ordinary pointer repair, not a carve-out case. |
| R11 | 1 | low | ADR-048 § Q4, *"`0135` carries it into the skill prose unchanged in meaning"* | Quietly loosens `0135`'s brief § What to build item 1, *"carries the ADR's **must-never** list verbatim in its own prose"*. The owner's stated reason is that the list is the artifact of worth, and `0123`'s defects came from paraphrased doctrine. |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT (sev medium: fails closed, but leaves `0135` an open decision — brief verification item 6) | Defect | ADR-048 § Q5 "Order among step 1's branches" rewritten: explicit order exception 1 → exception 2 → reconcile mode → plain stop, any identity; exceptions keep who they fire for and what they write; the four agent-stop sentences in `fkit-task-done/SKILL.md` (`:85`, `:100-101`, `:102-103`, `:108-110`) quoted, and `0135` re-words each to "this exception does not fire — go on to the mode's check". Order follows from the R7 ruling (owner-present + exception-1 case → exception 1 governs). Tagged "made precise at Process-review, R1". No new behaviour: the ruled mode can only exist if those stops hand on to it | ✅ done |
| R2 | CORRECT (sev medium: latent — no live instance in this repo, but install-wide doctrine; a misfire would breach owner constraint 1) | Defect | ADR-048 § Q5 "Locations": epic slice cells limited to the epic the brief's `## Parent / Epic` names, on a slice row linking this folder; in-body `**Status:**` lines limited to a sprint-plan body section linking this folder and no other. New clause (f): an epic row or body section that links this folder but carries an unattributable status line → refuse and report (step 6's "genuinely unclear" rule), folded into the predicate so it does not repeat R4's outside-the-predicate shape. "What each clause guards" gains (e) and (f). Marked "Architect's call, R2" (link-based attribution, fail-closed — the plan's own row discriminator extended) | ✅ done |
| R3 | CORRECT (sev medium: a Blocked-first loop erases the "other" value clause (e) relies on, defeating must-never 8) | Defect | ADR-048 § "The three carve-out sites", bullet "Either order works" replaced: reconcile spawn first; the loop's `🚧 Blocked — hand-off incomplete` marker only on the fallback path; loop spawns the mode once and not again after its fallback marker is on disk (matches today's "do not re-spawn"). Cites the loops' "still stale" / "never over a landed `✅ Done`" lines. Marked "Architect's call, R3 — forced by D4(a) and must-never 8": spawn-first is what "carve-out stays as the fallback" already implies, not a new branch | ✅ done |
| R6 | CORRECT (sev low: disclosure defect only; the rule is mechanical) | Defect | ADR-048 § Q5: the Moved-row "pointer, not a location" call now marked "Architect's call, disclosed at Process-review, R6". § Owner sign-off: the three-item prose inventory replaced by a numbered table of every architect's call (C1–C4 from Build, C5+ from Process-review), each with section and when made; states the ADR stays `proposed` until the owner approves text and calls (status ruling) | ✅ done |
| R4 | CORRECT (sev low: a (a)–(e)-satisfying no-Status brief is narrow; fixed per owner ruling "Fix all in this pass (Rec)") | Defect | ADR-048 § Q5 "Classes of value": a missing or multi-line brief `## Status` now reads **other** (multi-line mirrors step 5's refuse-to-guess rule), so clause (e) refuses inside the predicate. Edge-case bullet re-pointed to (e). Architect's call C7 | ✅ done |
| R5 | CORRECT (sev low: no wrong behaviour, but five include/exclude calls left to `0135`; fixed per owner ruling "Fix all in this pass (Rec)") | Defect | ADR-048 § Q3: item 2 widened from "hrefs still pointing at the `backlog/` path" to "pointers broken by this folder's move" — hrefs naming this folder under any board other than `done/` (step 3 allows other boards), Moved-row hrefs, the moved folder's outbound sibling links, its self-locators (all step 5 pointer repairs; matches D2(a) "repairs hrefs as a side effect" and the ADR's own D2(c) rejection reason). New "boundary" paragraph: epic "next slice" prose not written; reference-integrity guard still runs unconditionally; step 7 gains `Moved: none — reconcile mode; folder already in done/`. Architect's call C8 | ✅ done |
| R7 | CORRECT (sev low: mis-cited basis + an unrecorded owner-visible asymmetry; behaviour itself matches D1(a)) | Defect | Per owner ruling "Record as consequence (Rec)": ADR-048 § Q3 "copies, does not resolve" parenthetical re-based on D1(a) (D1(c) not chosen) + must-never 3, noting must-never 1 is not the basis. § Consequences → Negative gains the asymmetry: owner-present + brief agent-closed/row open → exception 1 → plain everywhere; owner-present + brief open/row agent-closed → mode → agent-closed on the brief; owner can still upgrade afterwards with an owner-present `/fkit-task-done`. Behaviour unchanged | ✅ done |
| R8 | CORRECT (sev low: pre-existing no-door state, not caused by the ADR; the defect is the incomplete inventory) | Defect | Per owner ruling "Name as residual (Rec)": ADR-048 § "`0229`'s exception — kept, owner-only" gains a second named residual, "brief plain `✅ Done` + row agent-closed", beside the existing "brief plain, row stale", with each branch's reason for not firing (exception 1, exception 2, the plain stop, the mode's (d)/must-never 7). § Consequences → Negative names both gaps; § Re-raise only if trigger 2 now covers either gap | ✅ done |
| R9 | CORRECT (sev low: fails safe — whole-cell reading would only over-refuse) | Defect | Per owner ruling "Fix all in this pass (Rec)": ADR-048 § Q5 clause (d) now tests that every landed value **begins with** the exact `✅ Done (agent-closed — not owner-verified)` marker; trailing text (e.g. step 5's epic ` (PR #NN)`) not compared; a `✅ Done` without that exact qualifier straight after is plain → refuse. "Same value" wording dropped (the leading-marker test makes landed locations agree by construction). Re-raise trigger 5 re-worded to "leading-marker test". Architect's call C9 | ✅ done |
| R10 | CORRECT (sev low: would route a no-disagreement case to a carve-out that sanctions a disagreement) | Defect | Per owner ruling "Fix all in this pass (Rec)": ADR-048 § "The three carve-out sites" fallback list drops "no disagreement"; adds "an unattributable location (clause (f))" and "no landed value at all" (both refusals under D4(a)'s "refuses or fails"). New paragraph: an all-agree refusal is not a fallback case; an agreed landed value with only a stale href is ordinary pointer repair under D2 / ADR-044 §Decision 1. Tagged "Architect's call, R10"; listed as C10 in § Owner sign-off | ✅ done |
| R11 | CORRECT (sev low: wording loosened a brief requirement on the part the owner valued most) | Defect | Per owner ruling "Fix all in this pass (Rec)": ADR-048 § Q4 "unchanged in meaning" → "**verbatim**, as its brief requires", quoting `0135`'s brief § What to build item 1 and the owner's stated reason ("The artifact of worth is the constraint list"). The reviewer's "`0123`'s defects came from paraphrased doctrine" was not carried into the ADR: `0134`'s brief records three review rounds on that logic, not their cause | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- owner-present outcome depends on which side landed (R7) — What: an owner-present producer meeting brief agent-closed / row open gets exception 1 (plain everywhere); meeting brief open / row agent-closed gets the reconcile mode (agent-closed on the brief); the owner can upgrade afterwards with an owner-present `/fkit-task-done` · Why (structural): follows from D1(a) (agent-closed value only; D1(c)'s owner-present plain sub-path not chosen) plus must-never 3, under the branch order exception 1 → exception 2 → mode; making both orientations converge would need D1(c) · Re-raise only if: the owner re-rules D1, or ADR-048's re-raise trigger 2 fires. Owner ruling 2026-09-14 "Record as consequence (Rec)"; recorded in ADR-048 § Consequences.
- second no-door state: brief plain + row agent-closed (R8) — What: no branch fires for anyone (exception 1 needs an agent-closed brief, exception 2 an open-work brief, the plain stop takes a brief beginning `✅ Done`, the mode refuses under (d) / must-never 7); escalates to the owner, who has no door either · Why (structural): pre-existing, not caused by ADR-048; closing it needs an owner-present sub-path (D1(c)), which was not chosen · Re-raise only if: the state actually occurs (ADR-048 re-raise trigger 2). Owner ruling 2026-09-14 "Name as residual (Rec)"; recorded in ADR-048 § "`0229`'s exception".
