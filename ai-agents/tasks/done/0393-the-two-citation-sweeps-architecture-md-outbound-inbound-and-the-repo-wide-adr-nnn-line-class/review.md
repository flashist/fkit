# Review — 0393

Task: ai-agents/tasks/done/0393-the-two-citation-sweeps-architecture-md-outbound-inbound-and-the-repo-wide-adr-nnn-line-class/brief.md
File(s) under review: this row's 11 edited paths, diffed against the before-copies in `.fkit/tmp/0393/before/` (not HEAD) — `ai-agents/knowledge-base/architecture.md` (15/15), `RELEASING.md` (1/1), `ai-agents/sprints/backlog.md` (2/2), briefs `0145` (2/2), `0273` (4/4), `0284` (3/3), and append-only notes in ADR-009 (31/0), ADR-010 (34/0), ADR-011 (28/0), ADR-013 (29/0), ADR-017 (12/0), ADR-022 (28/0), ADR-031 (11/0). Out of scope: all other dirt (closed 0392/0388/0390/0389/0134/0221, ADR-048, briefs 0395/0396, driver status flips).
Status: closed-out
Coverage: reasoning-only second opinion — Round 1: Codex (`codex-cli` 0.152.0, exit 0) returned a usable pass (3 findings) after reading the diff and the live files; its one execution, the two citation guards, failed 24 of 43 only because the read-only sandbox denied every `mkdtemp` (EPERM) — a denied write, not a measurement. The execution evidence is the Claude reviewer's: a fresh `dashboard.sh` render of `backlog.md` diffed against the before-capture (exit 0; only the `0145` and `0284` rows differ, coordinate text only).

> Authoring rule (plan §0): no token + colon + digits outside fences. Sites are written as `<path>` · L<n>; the L<n> values are identity labels measured 2026-09-15, each paired with a quoted fragment or heading.

## Reviewer findings
| #  | Round | Sev | Location | Claim |
|----|-------|-----|----------|-------|
| R1 | 1 | medium | `ai-agents/knowledge-base/architecture.md` §2, the **Codex CLI (`codex`)** row, "Where" cell (worklog § Phase T-DO, row DO-01) | The `claude/fkit-claude.sh` · L274-285 pointer (Codex preflight) is fenced to `0273`, but `0273` does not own it. `0273`'s brief § "Two stale citations that sit ON lines this task already rewrites" names exactly two, both `fkit-review/SKILL.md` pointers (*"`architecture.md` cites `claude/skills/fkit-review/SKILL.md`… for the codex command at site 6"*), and its acceptance step 4 reads *"The two in-passing citation fixes"*. A literal `0273` build fixes those two and leaves this one. `0393`'s brief § Context names this exact pointer as *"an unfixed hit"*. It is repairable under D4 (the claim *"Required, but warned — never walled"* is true), and the same file's §5.1 diagram now correctly cites the same preflight at L532-560 (`codex_preflight`), so the doc contradicts itself. The fences on DO-02, DO-31 and DO-32 are sound: `0273` names them, and `0272` will move those `SKILL.md` anchors. (Raised by both reviewers; verified CORRECT.) |
| R2 | 1 | medium | worklog § Phase T-DI, "Enumeration gaps closed" (*"Totals (inbound coordinates): 94"*) | The inbound enumeration is still incomplete. P5 only picked up bare `:N` tokens on a line that already had an `architecture.md` + colon coordinate, or on the line after it. It misses bare coordinates on lines that name `architecture.md` without a colon form. None of these has a row: (a) `0287` brief § Notes/fences, *"`architecture.md` carries the same claim at"* — 3 coordinates, twins of DI-043/082/083, open brief; (b) `0273` brief § "Two stale citations…", *"at **site 6 (…)**"* / *"at **site 8 (…)**"* — 2 coordinates. Under the worklog's own test these are live site labels, and they now disagree with the site list this row changed to §2 / §7; (c) `backlog.md`, the open `0226` row, *"three further sites inside declared `architecture.md`"* — 3 coordinates, twins of DI-047/084/085; (d) `backlog.md`, the cancelled `0286` row, *"four surfaced-and-not-taken"* — about 11 mentioned coordinates; (e) report `2026-07-11-plan-omnigent-removal.md`, the `architecture.md` table row, *"stale Claude addendum"* — 1 coordinate, frozen. Only (b) would change a treatment. Even so, Verify steps 7 and 8 (*"complete"*, *"one verdict per citation"*) do not hold as recorded. (Raised by both reviewers — Codex found (c) and (d), Claude found (a), (b) and (e); verified CORRECT.) |
| R3 | 1 | low | `ai-agents/knowledge-base/decisions/adr-010-role-locked-sessions-and-skill-lockdown.md` §Context, the new note *"This supersedes one judgement in the 2026-09-03 note"*; and §Related, the old note *"none of them earns a correction note"* | The supersession rests on sound ground. Q2(a) is an owner rule, and the judgement it overrides was an agent-closed architect consult ruling (`0356` worklog § "5.1 What it ruled — `0197`", row P8), not an owner ruling. But the record around it misleads in four ways. (a) The superseded note, which is now the file's last content, carries no marker. A reader who arrives there gets the obsolete conclusion. (b) The header's *"Extended 2026-09-15 by a fifth append"* line does not mention the supersession. (c) The new note does not say the old judgement was an architect ruling, or that the owner was not shown it when ruling Q2 (the worklog admits this in § Phase N). (d) The new note says *"three ADR-008 line pointers in this ADR no longer land"*, but §Related's bare pointer (*"role-access section of ADR-008 (…106-120)"*) has drifted the same +9 and is not mentioned. (Codex raised part (a); verified CORRECT.) |
| R4 | 1 | low | ADR-013 §Consequences, the new note *"every passage it names now carries a `../reports/` path"*; ADR-009 §Related, the new note *"three line pointers in this ADR no longer land"* | Two statements in the new correction notes are not true as written. (a) ADR-013: both ADR-007 passages carry the repo-root path `ai-agents/knowledge-base/reports/2026-07-10-eval-…md` (ADR-007 §Context *"are recorded in"*; §Related *"the full evaluation this ADR"*), not a `../reports/` path. The point being made (the links now reach `reports/`) holds; the literal wording does not. (b) ADR-009: the count is not limited to the classes this row swept. ADR-009 §Context also cites `claude/fkit-claude-init.sh` · L20/L24/L30/L46, and those lines now hold unrelated text, so "three" suggests every other pointer still lands. Accuracy is the whole point of a correction note. (Claude only; verified CORRECT.) |
| R5 | 1 | low | ADR-028 §Consequences, *"Verified 2026-07-19 the same claims are still live"* (worklog T-DI row DI-010 / DI-094; decision log item 5) | Classing this as `mentioned` (no note) does not match how ADR-022's *"Docs to refresh"* list was treated (DI-009/093: a dated worklist, noted by analogy with Q3(b)). ADR-028's list is also a ripple worklist, and a *pending* one: the eighth role is not built (`architecture.md` §4.1, *"An eighth role is decided but not built"*). A future tester build will follow its §4.1 pointer, which has drifted. If a finished worklist earns a note, an unfinished one earns it more. At minimum it should have been `mention?`, not decided silently. Related label slip, no change in outcome: ADR-016's *"Zero hooks"* site (DI-004) is labelled `mentioned`, but it is really *unresolvable, reported* (the claim is false today); the treatment (left, reported) is correct either way. ADR-031 §Context (DI-011, *"still describes the retired `skillOverrides` mechanism"*) is correctly `mentioned`. (Claude only; verified PARTIALLY CORRECT — a reading judgement, but applied inconsistently.) |
| R6 | 1 | low | `0273` brief § Verification, step 4 (*"Confirm `…373-375` was **not** touched"*) | The repair on this line replaced the bare coordinate the next token depended on. The bare `373-375` that follows is now attached to nothing (it used to follow the §7 line coordinate), so a `0273` builder cannot tell which file or range it means. The row was classed *unresolvable, reported* (DI-087), but the repair made it worse, and the report does not say so. (Claude only; verified CORRECT.) |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT — severity **low** (a pointer in a living doc landed on the six-mirrors checklist comment; claim itself true; `0273`'s brief names only the two `SKILL.md` pointers, so its fence never covered this one) | Defect | Owner ruling B, "Repair it in 0393 (Rec)". `architecture.md` §2 Codex CLI row, "Where" cell: launcher coordinate L274-285 → L539-560, plus the quoted comment header *"Codex preflight: required, but a WARNING, not a wall"* and `codex_preflight()` (the DO-15 form). One line rewritten in place, 632 → 632 lines, no prose change. New freeze sha256 `df3aabca…d7be9b1b`; inbound colon-line census 43 rows, (path, match) multiset unchanged. DO-01 row now *corrected*; fenced set is DO-02, DO-31, DO-32. | ✅ done |
| R2 | CORRECT, and **understated** — severity **medium** (Verify steps 7–8 claimed a complete enumeration and one verdict per citation; two live labels contradicted this row's own repair) | Defect | Enumeration widened (block, window and section scans + HEAD-prefixed forms, run on the Step-0 tree, attributed by reading): **26** missed coordinates, not ~20 — the reviewer's (a)–(e) are 22, plus **4 in ADR-042** §"Three further sites, missed by the producer and the lead" that neither reviewer listed. Rows DI-095…DI-120 added (worklog § "Process-review round 1"). Treatments: `0273` brief *"site 6"* / *"site 8"* labels repaired to §2's Codex CLI row / §7's flow *"4 — Review + the adversarial pass"* (fixes the site-label disagreement); `0287` brief ×3 unresolvable, reported (twins of DI-043/082/083); open `0226` row ×3 mentioned; cancelled `0286` row ×13 mentioned, untouched; plan-omnigent-removal report ×1 drifted, frozen, untouched; ADR-042 ×4 drifted, frozen (pending worklist, by owner ruling C's reasoning) → one ⚠️ note at end of file, no header bullet (measured rule: the heading correction note and the historical-ADRs bullet are both cited from outside). **Final inbound total: 120** (was 94). | ✅ done |
| R3 | CORRECT — severity **low** (misleading record, no wrong decision) | Defect | Owner ruling A, "Keep, add back-pointer (Rec)". ADR-010: this row's own header fifth-append line now names the supersession and the new end-of-§Related marker; this row's §Context note now covers **four** ADR-008 pointers (adds §Related's first-bullet range), and says the overridden judgement was an architect consult ruling (`0356` worklog §"5.1 What it ruled — `0197`", row P8) that was not shown to the owner when Q2 was ruled; a back-pointer block appended after the 2026-09-03 note, which stays byte-identical. Append-only vs before-copy: 48/0. | ✅ done |
| R4 | CORRECT — severity **low** | Defect | This row's own note text corrected. ADR-013: *"now carries a `../reports/` path"* → names both forms (`../reports/` links in ADR-009/ADR-010, the repo-root `ai-agents/knowledge-base/reports/` path in ADR-007). ADR-009: title scoped to pointers into ADR-008 and `architecture.md`, plus a scope sentence that the source-file pointers were not assessed and the `claude/fkit-claude-init.sh` ones now land on unrelated lines. Worklog E4's matching false phrase annotated. Append-only vs before-copies: ADR-013 30/0, ADR-009 35/0; ADR-009 L131 still "- Evidence:". | ✅ done |
| R5 | CORRECT — severity **low** (inconsistent class, no false text written) | Defect | Owner ruling C, "Add a drift note (Rec)". DI-010 / DI-094 reclassed drifted, frozen (pending worklist). ADR-028 full form: header `Corrections` bullet + note under Required follow-ups item 2 (opening summary sentence then *"seven roles, no orchestrator"*, today *"**seven built roles**"*; heading *"4.1 The seven roles"* still exists), scoped to the `architecture.md` pointers only. Placement by the measured rule: the only in-scope coordinate into ADR-028 is a specimen quote in the cancelled `0323` row, which a shift does not falsify. DI-004 relabelled unresolvable, reported (treatment unchanged). Append-only: 25/0. | ✅ done |
| R6 | CORRECT — severity **low** | Defect | `0273` brief § Verification step 4: the orphaned bare token → *"§7 flow 4's "**Degradation is loud and mandatory:**" citation cluster"* (the brief's own term for that range). Only the token replaced; 318 → 318 lines. DI-087's premise stays reported (D4). | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
