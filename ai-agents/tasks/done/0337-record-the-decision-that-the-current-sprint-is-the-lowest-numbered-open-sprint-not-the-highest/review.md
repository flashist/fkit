# Review — 0337

Task: `ai-agents/tasks/done/0337-record-the-decision-that-the-current-sprint-is-the-lowest-numbered-open-sprint-not-the-highest/brief.md`
File(s) under review: `ai-agents/knowledge-base/decisions/adr-047-a-sprint-has-an-explicit-status-and-current-means-every-in-progress-sprint.md` (new) · `ai-agents/knowledge-base/decisions/adr-041-the-active-sprint-is-selected-by-resolved-identity-not-by-filename-glob.md` (appended) · this task folder's `worklog.md` (new)
Status: closed-out
<!-- Set closed-out 2026-09-11 by the architect under owner ruling Y4 ("Close 0337, unblock 0338 (Rec)").
     ⛔ The architect holds no task movers (ADR-033) — the FILE MOVE and the sprint-plan row are the
     PRODUCER's act, routed by the driver. This field records the review's state, not the task's. -->
Amend-loop: ⛔ **ENDED at round 3 by owner ruling, 2026-09-11.** R27–R31 were disposed **without a third
amendment to ADR-047 §3.0.2** — see *Coder response* for each finding's route.
Coverage: **round 3 — both reviewers measured.** The Claude pass re-read the amended ADR in full (1220 lines) and **independently re-read ADR-041 §1.5** to test the architect's render-path-scoping claim; verified **39 `dashboard.sh` coordinates**, the two guard coordinates, the four `fkit-status/SKILL.md` coordinates, both task movers' step headings, and every quoted fragment from `task-status-vocabulary.md`, `sprint-7.md` and `fkit-task-brief/SKILL.md`; walked all **nine** §7 rows against R26's trigger-2 test; **re-measured all four baseline numbers rather than inheriting them** — `npm run test:unit` **877 / 877 pass / 0 fail**, the two link/citation guards **41 / 41**, the nine knowledge-base scanners **184 / 184**, and the ADR-041 append at **`78 0` with zero deletions**. The Codex pass ran from a task-keyed path (`.fkit/tmp/0337-r3-adversarial-prompt.md`) — **no collision this round, no other review running** — and **measured**: 47 tool calls, `nl -ba` over ADR-047, ADR-041, `dashboard.sh`, both movers and `fkit-task-brief/SKILL.md`. ⛔ **`bash test/prove-red.sh` was NOT run** — 32 mutations × the full suite (~4h), and this fence holds **no executable file**, so it can prove nothing here. *(Round 2's coverage statement, for the record: both reviewers measured — with the first Codex invocation discarded in full after a concurrent `0271` reviewer overwrote the shared prompt path; now filed as task `0385`. Round 1: both reviewers measured.)* The Claude pass re-executed the **tightened** recognizer against all nine real boards plus four malformed and three well-formed probes, traced P14's discrimination against `identity_gt`'s two candidate call forms, ran live `select-active`, and re-measured `npm test` **877/877** and the two link/citation guards **41/41**; the Codex pass independently executed the trap-vs-swap selection, grepped the mode dispatch for the non-existent `sweep`, and re-measured the ADR-041 append at `78 added / 0 deleted`. ⚠️ **The first Codex invocation reviewed the WRONG SCOPE** — a concurrent `0271` reviewer overwrote the shared `.fkit/tmp/adversarial-prompt.md` between the write and the call; that run was **discarded in full** and re-run from a unique path (`.fkit/tmp/0337-r2-adversarial-prompt.md`). *(Round 1's coverage statement, for the record: both reviewers measured — the recognizer against nine boards, the §6.1 trap against an `identity_gt` transcript, `select-active`, the guards 41/41, and a `65 added / 0 deleted` ADR-041 measurement.)*

## Reviewer findings

| #  | Round | Sev  | Location | Claim |
|----|-------|------|----------|-------|
| R1 | 1 | high | ADR-047 §2.3 — *"The `select-active` interface this ADR fixes for the implementing task"* | The wire format contradicts itself. The bullet reads *"**Every line carries `status=`.**"* but the normative example's board line reads `board  file="sprint-8.md" identity="Sprint 8" reason="lowest-ordered"` — no `status=`; `active none` likewise has none. `0338` must guess the `board` line's field set, and the ship-loop is its consumer. |
| R2 | 1 | high | ADR-047 §3 — *"**A cancelled sprint's rows.**"* / §4 step 2 — *"Dispose of the rows (§3)"* | `/fkit-sprint-done` has **no row policy at all**. §3 defines row disposition only for a *cancelled* sprint; §4 step 2 points at §3 for both movers. `0341` cannot implement the Done mover without inventing the rule — reject open rows, move them to a successor, move them to Backlog, or freeze them are all consistent with the ADR. No P-row covers a mover. |
| R3 | 1 | high | ADR-047 §6.3 row 2 — *"still DRIFT … ADR-041 §1.5 **untouched**"* + §8.2 — *"§1.5 (same-identity tie-break)"* listed in force | Mixed-status same-identity boards have no defined precedence. §1.5 sorts *all* same-identity claimants and takes the first; §5 now filters by `In progress` first. For `plan-sprint-6.md` (`Done`) + `sprint-6.md` (`In progress`), filter-first yields `sprint-6.md`, §1.5-first yields the terminal board. The ADR never orders the two operations, so "§1.5 untouched" cannot hold as written. Nor does it say whether a `Done`+`In progress` identity clash — the ordinary supersession shape — is still drift. No P-row covers a mixed-status clash. |
| R4 | 1 | medium | ADR-047 §2 — *"`^> ## (🔲 Backlog\|🔄 In progress\|✅ Done\|⛔ Cancelled\|🔒 CLOSED)([ .]\|$)`"* | The pinned recognizer does not enforce the grammar it sits under. §2's grammar makes the date mandatory (only trailing prose is bracketed), yet measured: bare `> ## 🔄 In progress`, `> ## 🔄 In progress arbitrary trailing garbage with no date`, and `> ## ✅ Done — not-a-date.` all **match**. §1 has the producer writing `Backlog`/`In progress` **by hand** — the exact path that omits a date. No malformed-banner drift fact in §7, no malformed case in P9, so a builder must guess resolve-vs-drift. |
| R5 | 1 | medium | ADR-047 §6.1 — *"**invert only the comparison at the selection site**"* | At `claude/skills/fkit-status/dashboard.sh:265` the comparison is a `_gt` **function call**, not an operator, so "invert" admits two readings and one is wrong. Measured against a transcript of `identity_gt`: the naive negation `! identity_gt "$_i" "$_best_id"` yields `<=` and picks `sprint-6.md`, where ADR-041 §1.5 requires `plan-sprint-6.md`; the argument swap `identity_gt "$_best_id" "$_i"` is correct. The tie-break silently flips first-wins → last-wins. §6.1 names the target semantic (*"Strictly-LESS preserves that property identically"*) but never the mechanical change, and no P-row pins the tie-break under the new direction — its only backstop is inherited S6, which this ADR puts on the repin list. |
| R6 | 1 | medium | ADR-047 §7.1 carve-out 2 — *"the two **archival** drifts … are therefore **not its to emit**"* | The carve-out's list is incomplete. `drift sprint-status-location-mismatch` fires on *"`Done` under `cancelled/`, or `Cancelled` under `done/`"* — both operands require reading inside `done/`/`cancelled/`, which the same carve-out states depth-1 `select-active` structurally cannot do. It is not exempted and no emitter is assigned, so `0338` either implements a structurally impossible fact or drops it silently. |
| R7 | 1 | medium | ADR-047 §7 table + §Required tests — *"the decision is not satisfied without these"* | Three of the seven declared drift facts have no P-row: `sprint-archived-not-terminal`, `sprint-status-location-mismatch`, `active-marker-on-non-active`. §7's own foot requires *"Every drift kind above must reach the roll-up's drift clause"*, yet only P3 asserts roll-up reach (P7 asserts the flag and the fallback, not the reach). An implementation can satisfy every listed test while omitting three declared hard rules. |
| R8 | 1 | medium | ADR-047 §4 — *"One act, **in the order the task movers already do it**"* | The precedent claim is false. Both cited movers `git mv` **before** repointing references (`claude/skills/fkit-task-done/SKILL.md:123-132` — §3 Move, then §4 "Find every place the task is referenced"; `claude/skills/fkit-task-cancelled/SKILL.md:102-111`, same shape). ADR-047 puts `git mv` last, at step 4. The order may well be the better one; the defect is that `0341` is told to mirror a precedent that does the opposite. |
| R9 | 1 | medium | ADR-047 — the `⟦fkit-dashboard v1⟧` version marker, absent from the whole document | The wire format changes materially (`status=` on every line, plural `active`, a new `board` line, a new `status` mode) while the protocol version marker is never mentioned. That marker exists for exactly this: `claude/skills/fkit-status/SKILL.md:229` — *"If the version marker is not `⟦fkit-dashboard v1⟧`, say so rather than guessing at the shape"* — and `dashboard.sh:53` sets it. §Consequences names the *prose* ripple but not the protocol. `0338` must guess bump-to-v2 (and update five `v1` assertions plus the SKILL's documented shape) versus keep-v1-and-rewrite-the-shape-doc. |
| R10 | 1 | low | ADR-047 §2 — *"No banner at line 3 → status `unresolved`"* / §7.1 carve-out 1 — *"must NOT fire for a `Backlog` or `unresolved` identity"* | `unresolved` names both an identity value and a status value, and unlike `Backlog` the collision is never named — §1.1 sets the standard of naming a one-word-two-meanings collision and this second instance goes unnamed. §2's own sentence runs the two readings together in consecutive clauses. Read in status-space the carve-out swallows `sprint-status-missing` entirely, since every banner-less board has unresolved *status*. P3 is the backstop, so a misreading reds a test rather than shipping. |
| R11 | 1 | low | ADR-047 §1.1 — *"`resolve_identity`'s basename special case (`claude/skills/fkit-status/dashboard.sh:158-163`)"* | The coordinate excludes the thing the sentence names. The branch's comment opens at `:144` and its `basename` test is at `:148`; `:158-163` contains only tail commentary and `_id="Backlog"`. |
| R12 | 1 | low | ADR-047 §8.2 — *"⭐ **Falsified by this ADR:**"* / *"⭐ **In force and untouched:**"* | Neither list mentions ADR-041's `## Decision` block, which is falsified twice over — *"The active sprint is the plan whose resolved identity is the **highest-ordered** `Sprint <N><suffix>`"* is wrong in both direction and singularity. §Related sends readers to §8.2 as *"the exact scope"*. Mitigated: ADR-041's own appended notice covers it in substance (item 2 quotes that sentence, attributing it to §1.4 where it does not live; item 3's blanket replaces the singular reading throughout). |
| R13 | 1 | low | ADR-047 §2 — *"**Exactly one banner per board.** No other line may match the recognizer."* vs test P10 — *"the second line is **not** a status and does not make the board ambiguous"* | A second matching line is simultaneously forbidden and explicitly tolerated, with no drift fact and no rejection. The ADR does not say whether "exactly one" is enforced or is an authoring convention. |
| R14 | 1 | low | ADR-047 §2.3 — the wire-format block, `board  file="sprint-8.md"` | The block shows two spaces after `board` where the live emitter uses one (`claude/skills/fkit-status/dashboard.sh:272`, `:282` — `printf 'active file="%s" identity="%s"\n'`). Presented as the literal interface with no note on whether the whitespace is alignment or significant. |
| R15 | 1 | low | `worklog.md` § Verification — the plan's six steps, step 1 row — *"all eight verbatim option labels (six from 2026-08-25, V1–V4 from 2026-09-10)"* | The count is self-contradictory: six plus V1–V4 is ten, not eight. Measured against ADR-047's Authority tables: five verbatim option labels in Round 1 (OQ-1, OQ-3, SD-1, SD-2, SD-3 — OQ-2 is free text, not an option label) plus four in Round 2 = nine labels and one quoted free-text answer. Bookkeeping only; no spec impact and the underlying check does pass. |
| R16 | 2 | high | ADR-047 §7 foot — *"Every drift kind above must reach the roll-up's drift clause"* + §7 table's *"⛔ Emitted by"* column, rows `select-active` | ⛔ **R7's closure recreated the exact defect R6 closed: a property assigned to a mode that structurally cannot produce it.** §7 assigns five drifts to `select-active` (`sprint-status-missing`, `-malformed`, `active-marker-on-non-active`, `ambiguous-active-marker`, `ambiguous-active-sprint`) and the foot then requires each to reach the roll-up — but **`select-active` has no roll-up.** Measured: the roll-up and its `drift_clause` are built only in the one-argument board render (`claude/skills/fkit-status/dashboard.sh:1136-1205`, `:1185-1196`), and the file's own contract comment says so — `:30`, *"the roll-up exists only in this board render, so the check has to live here."* `mode_select_active` prints `⟦FACTS⟧` then `⟦END⟧` and exits (`:295-297`); the two paths share no state. **Today's code proves the gap:** `select-active` emits `drift ambiguous-active-sprint` at `:292` and it never reaches a roll-up; the render path reaches the roll-up under a **different name**, `drift ambiguous-plan-identity` (via `plan_level_drift`, `:1189`) — a drift kind §7's table does not list at all. So P3, P7, P12, P15 and P16 each demand emission from `select-active` **and** roll-up reach, and `0338` must invent the routing: duplicate-emit from the render path, add a roll-up to `select-active`, merge the two drift names, or redefine "reach". Raised by both reviewers. |
| R17 | 2 | medium | ADR-047 §3.0 — the mover table, *"`➡️ Moved to [Sprint N](sprint-N.md) — priority M`, or `➡️ Moved to [Backlog](../backlog.md)`"* | The successor href is wrong two independent ways, and the same table cell contains its own counter-example. **(a) It assumes the successor's FILENAME is `sprint-N.md`** — the thing ADR-041 exists to deny, and contradicted by this ADR's own P14 fixture, which gives identity `Sprint 6` to `plan-sprint-6.md`. `task-status-vocabulary.md`'s own note is quoted three lines above: *"`N` … is the target sprint's **identity**, not a number"*. **(b) It omits the `../` hop its sibling marker in the same cell carries.** Both markers are written onto the **closing** board, which §4 step 4 then `git mv`s into `done/` — so `../backlog.md` resolves and `sprint-N.md` does not. §3's own rule states it: *"**Href rule for the new folder:** `../backlog.md`, one hop up — identical to `done/`."* ⚠️ The failure has a silent tail: today `done/sprint-N.md` does not exist so the link dangles and §3.0's link-guard clause catches it, but once that successor is itself archived under the same basename the link **silently resolves to the archived copy** instead of the live board. |
| R18 | 2 | medium | ADR-047 §3.0 — *"the **lowest-ordered non-terminal successor** board"* and *"the destination rule is a **default that never has to be guessed**"* | ⛔ §3.0 falsifies its own claim in three places, all of which `0341` must invent. **(1) `priority M` has no defined source.** The only cited authority — *"the de-scope procedure the brief-writing skill already defines"* — defines `M` as *"the priority the task receives in **Sprint N** (step 1)"* (`claude/skills/fkit-task-brief/SKILL.md:355-356`), and step 1 (`:350`) is a **producer choosing a rank**. §3.0 has the mover write the marker automatically for **every** open row and never says whether `M` is appended per ADR-035, carried over from the source board, or chosen — nor what relative order N simultaneously-relocated rows take on the destination. **(2) No tie-break.** Two files can claim one identity (§6.3 row 2; P14's own fixture), so "the lowest-ordered non-terminal successor" can resolve to two files. §6.4's four-step rule is **not** reusable here: its step 2 filters to `In progress` only, while §3.0's successor set is `🔲 Backlog` **or** `🔄 In progress`. **(3) No enumeration set** — §3.0 names no directory and no depth. Raised by both reviewers. ⭐ The row-policy CHOICE is owner-accepted and is not re-litigated here; only its under-determination is. |
| R19 | 2 | medium | ADR-047 §7 table, three rows reading *"**render path / sweep** — ⛔ never `select-active`"*, and §7.1 carve-out 2 — *"All three belong to the **per-board render path** or to an explicit sweep"* | The emitter is an alternation between an existing path and a mode that **does not exist**, with no rule for choosing. Measured: `claude/skills/fkit-status/dashboard.sh:218` — `USAGE="usage: bash dashboard.sh <plan> \| identity <plan> \| select-active <sprints-dir>"` — and the dispatch at `:305-309` recognises only `identity` and `select-active`; `grep -n sweep claude/skills/fkit-status/dashboard.sh` returns **nothing**. The required tests then disagree with each other: **P11** commits (*"emitted from the **render path**"*) while **P13** does not (*"each emitted from the render path / sweep"*), for facts in the same carve-out. `0338` must pick, and R6's closure is what introduced the third row into this alternation. |
| R20 | 2 | medium | ADR-047 §9.2 item 1 — *"⛔ **The marker is emitted by EVERY mode, not just `select-active`.**"* | ⚠️ **Regression risk, stated up front:** a builder implementing this sentence literally breaks a documented contract. The claim is false and is contradicted by its own next clause (*"two emit sites"*). Measured: `grep -n VERSION_MARKER claude/skills/fkit-status/dashboard.sh` returns exactly `:53` (definition), `:269`, `:1199` — the two the bullet names. `mode_identity` (`:226-232`) emits **no** marker, deliberately: `:223-225` reads *"⚠️ NO `⟦…⟧` MARKERS, deliberately. This emits a VALUE, not a rendering, so a caller reads it with a single command substitution."* §2.3a gives the **new** `status <plan>` mode the same contract — *"It mirrors `identity`'s value-not-rendering contract"* — so "EVERY mode" would put a marker line in front of a value every caller reads with `$(…)`. ⭐ The bullet's actual point — that the board render bumps too, so a `select-active`-only bump is not implementable — is **correct and verified**; only the universal quantifier is wrong. |
| R21 | 2 | medium | ADR-047 §Required tests, **P17** — *"the two `⟦SELECT⟧` blocks of §2.3, **byte for byte**"* / *"**exact stdout equality**, not a substring match"* | P17 promotes an **envelope-incomplete** illustration to a normative byte-for-byte fixture. Both §2.3 blocks end at `⟦FACTS⟧`; the real stream ends at `⟦END⟧`. Measured: `bash claude/skills/fkit-status/dashboard.sh select-active ai-agents/sprints` prints `⟦fkit-dashboard v1⟧ / ⟦SELECT⟧ / active … / candidate … / candidate … / ⟦FACTS⟧ / ⟦END⟧`, exit 0 — the `⟦END⟧` printf is at `claude/skills/fkit-status/dashboard.sh:295`. §2.3a does **not** rescue it: its normative table covers the four record kinds and says nothing about envelope markers. A builder either writes a test that reds on its first run or deletes `⟦END⟧` from `select-active`. ⛔ Introduced by the amendment — P17 is new under W1. |
| R22 | 2 | medium | ADR-047 §2.3a — *"The field set per line kind. **This table is normative**"* vs §7's eight-row drift table | The normative table enumerates `active`, `active none`, `board` and `candidate` and **no `drift` line at all**, leaving seven newly-named drift kinds with no field set, no field order and no cardinality — while §2.3a's own rule, *"⛔ **Consumers parse by KEY, never by position**"*, presumes a key set exists. Only `ambiguous-active-sprint` has a shape anywhere, inherited from ADR-041 §1.5's *"Suggested shape, wording is the implementer's"* — which is scoped to that one drift. P7 and P12 require *"naming **every** claimant"*; nothing fixes a single field for the other six. ⭐ **Mitigating and stated:** ADR-041 set the precedent that a drift record's wording is the implementer's, so this is a narrower gap than R1's was. ⛔ Not mitigating: these lines are what the SKILL renders into beat 6, and `0338` is the only place they would get defined. Raised by Codex; verified. |
| R23 | 2 | low | ADR-047 §1.2 — *"The two are independent … and `backlog.md` has **both** unresolved at once."* | The section written to prevent a two-meanings misreading closes on one. `backlog.md`'s identity is **`Backlog`**, not unresolved. Measured: `resolve_identity`'s basename branch sets `_id="Backlog"` (`claude/skills/fkit-status/dashboard.sh:148`, `:163`); §1.1's own table row reads *"`identity="Backlog"` in `select-active`"*; and **P4** asserts `candidate file="backlog.md" identity="Backlog" status="unresolved"`. The claim is instantiable — a depth-1 `.md` with neither an identity nor a banner has both — just not by the board named. ⭐ R10 is otherwise closed: the by-position rule and the §7.1 identity-space warning are correct. Raised by Codex; verified. |
| R24 | 2 | low | ADR-047 §2 — *"⚠️ The `Backlog`/unresolved-identity carve-out applies here too"* vs §7.1 — *"⛔ **Two carve-outs, stated as HARD RULES and not as notes**"*, item 1 | The two sections scope the carve-out differently and nothing pins which wins. §7.1 item 1 names **one** drift — *"`sprint-status-missing` must NOT fire for a `Backlog` or `unresolved` identity"* — while §2 extends the same carve-out to `sprint-status-malformed`. §2's reasoning (*"those boards carry prose at line 3, which is *absent*, not *malformed*"*) makes it near-moot for `backlog.md` itself, but a `Backlog`-identity board that **does** carry a malformed banner falls on both sides. **P16** uses only eligible identities, so it does not decide it. Raised by Codex; verified. |
| R25 | 2 | low | ADR-047 §Authoring constraints item 1 — *"`test/reference-integrity.test.js:180` exempts only `ai-agents/wiki-vault/`"*, repeated in §Related — *"Guards this ADR was written against"* | Off by one, in a file that asserts *"All such coordinates in this file were **measured** against the working tree on 2026-09-10."* Measured: `test/reference-integrity.test.js:180` is `export function exempt(rel) {`; the exemption is `:181` — `if (rel.startsWith('ai-agents/wiki-vault/')) return true;`. Both citation sites carry the same stale number. Raised by Codex; verified. |
| R26 | 2 | low | ADR-047 §Authority, *"Added under W1, and equally the architect's"* — *"⚠️ **Each is listed in *Re-raise only if* where it is genuinely contestable.**"* vs §Re-raise only if | Two W1 design calls that §Consequences records as **named accepted costs** have no re-raise trigger. §4's deliberate inversion of the task movers' step order is a Negative bullet in its own right (*"⚠️ **Negative — the sprint movers do NOT mirror the task movers' step order (§4).**"*, with the interrupted-mover cost named at §4) and §7's three-drift emitter assignment likewise — neither appears in §Re-raise only if. ⭐ The other three W1 calls checked out: §3.0, §6.4 and §2's recognizer each carry a trigger that is concrete and checkable (*"a real case where relocating them loses something"*, *"a real case where a terminal board should win selection"*, *"refuses a banner someone legitimately needs to write — a real form, not a hypothetical one"*). |
| R27 | 3 | medium | ADR-047 §3.0.2 item 2 — *"steps 3 and 4 are unchanged — order by §6.1's comparator lowest-first, then ADR-041 §1.5's byte order under `LC_ALL=C`, **first wins**"* | ⛔ **The mover is told to order by a comparator no mode exposes to it.** `/fkit-sprint-done` is a SKILL — markdown prose an LLM executes, exactly like `/fkit-task-done`. §6.4's steps 3–4 are safe **because they run inside `dashboard.sh`'s `select-active`**; §3.0.2 lifts them out into prose and hands them a set §3.0.2's own step-2 replacement makes **wider** than any mode's — status ∈ {`Backlog`, `In progress`}, where `select-active` filters by identity alone and has no status notion at all (`claude/skills/fkit-status/dashboard.sh:253` — `is_eligible "$_i" || continue`). The two per-file lookups **are** covered — `identity <plan>` exists (`:226-232`) and §2.3a adds `status <plan>` — but **ordering has no surface at all.** `0341` must pick: (a) add a new `dashboard.sh` mode, which §3.0.2's own *"No new traversal, no new depth"* reads as forbidding and §9.1's mode inventory does not authorise; (b) have the skill's prose compare `Sprint 9` against `Sprint 10` itself — ⛔ **the exact hazard `identity_gt`'s own comment names** (`:180-183`: *"a leading zero … and a very long `<N>`, where a 30-digit sprint number overflows the shell's integer and compares as garbage"*), and the *"two grammars for one question"* defect ADR-041 §5 exists to forbid — or (c) reuse `select-active`, which silently drops every `Backlog` successor. ⭐ **Mitigating, and it may make this a pointer rather than a ruling:** ADR-041 §5 — listed in §8.2 as *"in force and untouched"* — already decides the half that matters (*"The exact CLI surface is the implementer's call; re-implementing the grammar is not"*), so the fix may be one sentence citing it, not a new decision. ⛔ **Introduced by the second amendment** — R18 asked for a tie-break and got one that no consumer can execute. |
| R28 | 3 | medium | ADR-047 §3.0.2 — *"Each affected brief's `## Sprint` / `## Status` / `## Priority` follow the de-scope procedure the brief-writing skill already defines."* | ⚠️ **Regression risk, stated up front: a builder obeying this sentence for a successor-sprint move manufactures the exact permanent drift the cited skill's own warning documents.** The brief-writing skill defines **two** procedures, not one, and this bullet names the wrong one for half of §3.0's own table. Measured: *"De-scoping a task out of a sprint and back onto the Backlog board"* (`claude/skills/fkit-task-brief/SKILL.md:369-385`) mandates `## Sprint` → **`Backlog`**, `## Status` → **`🔲 Backlog`**, `## Priority` → **`Unscheduled`**, and states *"There is no `— priority M` suffix here"*. Applied to a row §3.0 sends to a **successor sprint**, the board row then reads `➡️ Moved to [Sprint 9](…)` while the brief reads `Backlog` — and the skill's own ⚠️ warning says what happens: *"drift rule 2 compares a `➡️ Moved` row's target against the brief's `## Sprint` … the row is flagged `drift disagreement` — and because a drifted row **always renders**, it never disappears from the backlog board. Verified empirically, 2026-07-18."* ⭐ **The right procedure exists directly above it in the same file** — *"Pulling a backlog task into a sprint"* (`:348-360`), whose step 3 sets `## Sprint` to `Sprint N`. ⛔ **Two tails.** (1) §3.0.2 item 3 attributes its `M` quote — *"the priority the task receives in **Sprint N**"* — to *"the de-scope procedure"*; it is at `:356`, inside the **pull-into-a-sprint** procedure, which is the opposite move. (2) That procedure's step 2 still writes `➡️ Moved to [Sprint N](sprint-N.md) — priority M` (`:353`) — ⛔ **the filename-as-identity form §3.0.1 just withdrew under R17**; a mover mirroring it verbatim reintroduces R17. ⛔ **Left open by the second amendment**, which fixed the marker in §3.0.1 and the rank in §3.0.2 but never revisited the brief-fields bullet §3.0 wrote under W1. Raised by Codex (as F1, severity high); verified, and re-scoped — the destination and the marker **are** now defined, so only the brief-field transition is unwritable. |
| R29 | 3 | low | ADR-047 §Consequences → *"⭐ Accepted residual — the seven `drift` record FIELD SETS are the implementer's (X2, owner-ruled)"* — *"only `ambiguous-active-sprint` has a shape anywhere, inherited from ADR-041 §1.5"* | The residual's own Why clause is stale against the ninth §7 row added in the same amendment, and the two halves of the residual disagree by one. Its heading and X2's Authority row both say **seven** (correct: nine kinds minus the two that already have shapes); the Why clause says only **one** has a shape, which makes it eight. Measured: `drift ambiguous-plan-identity identity="…" plan="…" also="…"` is a **live, pre-existing** record with a fixed field set at `claude/skills/fkit-status/dashboard.sh:1175`, and §7's table foot **depends on** that field set being fixed — *"The two records carry **different field sets** because they state **different facts**"*. Read literally, *"`0338` chooses each one's fields"* covers `ambiguous-plan-identity` too, licensing a reshape of a record §7.2 relies on not moving. ⛔ **The decision is not re-argued** — X2 is the owner's and stands; only the clause's count is wrong. |
| R30 | 3 | low | ADR-047 §4 — *"One act, in this order: 1. Stamp the line-3 banner."* | Neither sprint mover has an input-validation step, and the one corner that is a **ruling** rather than implementation detail goes unnamed: a board that is already terminal. Step 1 says stamp; V3 and §2 say `🔒 CLOSED` is *"Read FOREVER, written NEVER"*; §8.1 says the seven archived boards get *"⛔ **NO CHANGE**"*. Run against one of them, the four steps as written **overwrite an owner-ruled permanent record**. ⭐ **Mostly mitigated, and stated:** §4 reuses ADR-033's mover reasoning *"verbatim"*, and both task movers carry an explicit step 1 *"Resolve and validate the input"* with enumerated stop conditions (`claude/skills/fkit-task-done/SKILL.md:74-113`; `claude/skills/fkit-task-cancelled/SKILL.md:77-89`) — so `0341` has a precedent shape to mirror, and §3.0 rule 1 already makes a re-run idempotent on rows (*"an already-`➡️ Moved` row"* is never touched). ⭐ Verified against Codex's stronger reading (F2, severity high): the interrupted-mover re-run §4 itself contemplates is **safe by construction** on steps 1–4; the terminal-input case is the residue. **PARTIALLY CORRECT — one stop condition, not a validation spec.** |
| R31 | 3 | low | this task's `worklog.md` § *"Authoring constraints obeyed — stated so a reviewer does not 'fix' them"* — *"`test/reference-integrity.test.js:180` exempts only `ai-agents/wiki-vault/`"* | R25's off-by-one survives at a **third** site the finding never named. ⛔ **R25 itself IS closed** — both ADR sites it cited now read `:181`. This is the same stale coordinate in the build record, in a document that presents it as measured. Measured 2026-09-10: `:180` is `export function exempt(rel) {`; `:181` is `if (rel.startsWith('ai-agents/wiki-vault/')) return true;`. ⭐ **No spec impact, no builder guesses on it** — recorded so the tree does not keep one wrong coordinate after two rounds spent removing it. Raised by Codex (as F3, and as *"R25 NOT CLOSED"*); verified, and **re-classified**: R25 is closed as raised; this is a new site. |

## Coder response

<!-- CODER-OWNED — the reviewer never writes this section. -->

⭐ **Owner rulings, `AskUserQuestion`, 2026-09-11 — option labels VERBATIM. These bind the dispositions below.**

| # | Ruling (verbatim label) | Effect |
|---|---|---|
| **Y1** | **"Record as follow-ups on 0341 (Rec)"** | R27 and R28 become **required follow-ups on `0341`**, recorded in ADR-047 §3.1's existing pattern. ⛔ **§3.0.2 is NOT amended a third time.** ⭐ Owner's basis, recorded: neither is a design ruling that would land in the wrong document, and both fall on `0341`, which has not started |
| **Y2** | **"Correct in passing (Rec)"** | R29 and R31 — two wrong facts — fixed as corrections during this pass. ⛔ **Not as a review round** |
| **Y3** | **"Accepted residual (Rec)"** | R30 → an *Accepted residual* below, in full What / Why / Re-raise-only-if shape. ⛔ **No stop condition is added to ADR-047 §4** — that would be a third amendment. ⭐ Owner's basis: the task movers' step 1 is the precedent `0341` mirrors anyway, and §3.0 rule 1 already makes a re-run idempotent |
| **Y4** | **"Close 0337, unblock 0338 (Rec)"** | After these dispositions `0337` closes. ⛔ **The architect holds no movers (ADR-033)** — the driver routes a producer for the file move and the board row. This ledger's `Status:` is set to `closed-out` by this pass |

⛔ **Rounds 1 and 2 (R1–R26) were disposed in this task's `worklog.md`, under sections *"Round-1 review
amendment"* and *"Round-2 review amendment"*, and are NOT re-stated here.** ⭐ Round 3's rows follow.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R27 | **valid — under-determination, medium (severity derived: it blocks a `0341` build step, it does not falsify a shipped fact)** | Defect | ⛔ **NOT an amendment (Y1).** Recorded as ADR-047 **§3.1 FOLLOW-UP 2 on `0341`**. ⭐ Verified independently before writing: `/fkit-sprint-done` is **prose**, and **no `dashboard.sh` mode orders a `Backlog` ∪ `In progress` set** — the dispatch recognises `identity` and `select-active` only (`claude/skills/fkit-status/dashboard.sh:305-309`), and `select-active` filters on identity alone with no status notion (`:253`, `is_eligible "$_i" \|\| continue`), so it silently drops every `🔲 Backlog` successor. ⭐ **Closed as a POINTER, not a ruling:** ADR-041 §5 — listed in ADR-047 §8.2 as *"in force and untouched"* — already decides the half that matters, *"The exact CLI surface is the implementer's call; re-implementing the grammar is not"*, which **forbids route (b), re-deriving the comparator in the skill's prose**, and leaves the surface to `0341`. ⛔ **This ADR names no route**, and §3.0.2's rule is untouched | ✅ done |
| R28 | **valid — under-determination with a NAMED REGRESSION, medium (severity derived: a builder obeying the sentence literally manufactures a documented permanent drift; it is bounded to one bullet and `0341` has not started)** | Defect | ⛔ **NOT an amendment (Y1).** Recorded as ADR-047 **§3.1 FOLLOW-UP 3 on `0341`**, with the regression stated **before** the detail. ⭐ Verified independently: the brief-writing skill defines **two** relocation procedures — *"De-scoping a task out of a sprint and back onto the Backlog board"* (`claude/skills/fkit-task-brief/SKILL.md:369-385`), which is **Backlog-only** and sets `## Sprint` → `Backlog`, and ⭐ **the right one for a successor move, named: *"Pulling a backlog task into a sprint"* (`:348-360`, the immediately preceding bullet in the same list, warning at `:362-368`)**, whose step 3 sets `## Sprint` → `Sprint N`. Sending a successor relocation through the de-scope procedure manufactures the `drift disagreement` that skill's own ⚠️ documents as *"Verified empirically, 2026-07-18"*. ⛔ **Both tails carried:** §3.0.2 item 3's `M` quote is attributed to *"the de-scope procedure"* but sits at `:356` inside the pull-into-a-sprint one; and that procedure's step 2 (`:353`) still writes the **filename-as-identity** marker §3.0.1 withdrew under R17, so `0341` mirrors its **brief-field steps only, never its marker** | ✅ done |
| R29 | **valid — wrong fact, low (severity derived: no builder acts on the clause, but the ADR asserts its coordinates are measured)** | Defect | ⭐ **Corrected in the ADR in passing (Y2).** The X2 residual's *Why* clause read *"only `ambiguous-active-sprint` has a shape anywhere"* — implying **eight** undefined kinds, against its own heading's and X2's Authority row's **seven**. ⭐ Verified independently: `drift ambiguous-plan-identity identity="…" plan="…" also="…"` is live pre-existing code at `claude/skills/fkit-status/dashboard.sh:1175`. The clause now reads **nine kinds, two with a shape, seven the implementer's**, and states explicitly that `ambiguous-plan-identity` is ⛔ **NOT `0338`'s to reshape**, because §7's table foot rests on its field set staying put. ⛔ **X2's decision is not re-argued — only the count and the claim moved** | ✅ done |
| R30 | **partially valid — one stop condition, not a validation spec; low (severity derived: reachable only by running a mover against an archived board, and the precedent shape is already in the tree)** | Frontier | ⛔ **NOT fixed — Accepted residual (Y3).** ⛔ **No stop condition added to ADR-047 §4.** Recorded in full below | ✅ recorded as residual |
| R31 | **valid — wrong fact, low (severity derived: no spec impact; a stale coordinate in a document that presents it as measured)** | Defect | ⭐ **Corrected in this task's `worklog.md` in passing (Y2)** — *"Authoring constraints obeyed"*: `test/reference-integrity.test.js:180` → **`:181`**, with the correction dated and attributed at the site. ⭐ Verified independently: `:180` is `export function exempt(rel) {`; `:181` is `if (rel.startsWith('ai-agents/wiki-vault/')) return true;`. ⛔ **R25 stays CLOSED** — both ADR sites it named already read `:181`; this is a **third site R25 never named**, not a reopening | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)

### ⭐ Accepted residual — ADR-047 §4's mover has NO input-validation step, and the terminal-board case is the residue (R30, owner-ruled Y3, 2026-09-11)

⚠️ **Owner ruling Y3, 2026-09-11, verbatim label: "Accepted residual (Rec)".** ⛔ Taken **against**
adding a stop condition to ADR-047 §4 — that would have been a third amendment to an accepted ADR, and
the owner ruled the amend-loop ends here. Recorded in full rather than as a note.

- ⛔ **What.** ADR-047 §4 fixes the movers' four steps — *"1. Stamp the line-3 banner. 2. Dispose of the
  rows (§3.0). 3. Repoint links. 4. `git mv` the board into `done/` or `cancelled/`"* — and **defines
  no input validation before step 1.** ⛔ **Run against a board that is ALREADY terminal, step 1 says
  stamp.** Measured 2026-09-11: the seven archived boards carry `> ## 🔒 CLOSED — <date>.` at line 3;
  owner ruling **V3** made `🔒 CLOSED` a permanent compat rung that §2 states is **"Read FOREVER,
  written NEVER"**, and §8.1 gives those seven boards ⛔ **"NO CHANGE"**. So the four steps as written
  would **overwrite an owner-ruled permanent record**. ⛔ **This ADR does not define the movers' input
  validation, and does not enumerate their stop conditions.** `0341` defines them.
- ⭐ **Why — and the reason is STRUCTURAL, not scheduling.** ⛔ **The precedent §4 already reuses is the
  thing that closes this.** §4 says it applies ADR-033's mover reasoning **"verbatim"**, and both task
  movers open with an explicit **step 1, "Resolve and validate the input"**, carrying enumerated stop
  conditions — `claude/skills/fkit-task-done/SKILL.md:74-113` and
  `claude/skills/fkit-task-cancelled/SKILL.md:77-89`. ⭐ **`0341` therefore inherits a worked shape to
  mirror rather than a blank**, which is the same posture §4 takes on every other point it borrows
  from those movers. ⭐ **And the re-run case is already safe by construction:** §3.0 rule 1 makes the
  row pass idempotent — *"A closed row is frozen history and is NEVER touched"*, ⛔ *"an already-`➡️
  Moved` row"* included — so the interrupted-mover re-run §4 itself contemplates does not need this
  validation; only the **terminal-input** case does, and that case is a producer running a close
  against an archived board. ⚠️ **The cost, stated rather than softened:** `0341` is the only place
  these stop conditions get written, so if it mirrors the task movers' step 1 carelessly, a mover that
  stamps a `🔒 CLOSED` board is what ships — and nothing in the tree tests for it today. ⭐ The
  mitigation is that the damage is **loud and recoverable**: the overwrite is a tracked-file edit on a
  board §7's `sprint-archived-not-terminal` and `sprint-status-location-mismatch` drifts also watch.
- ⭐ **Re-raise only if** a sprint mover **actually stamps a terminal board** — ⛔ **the checkable
  form:** a run of `/fkit-sprint-done` or `/fkit-sprint-cancelled` against a board already carrying
  `🔒 CLOSED`, `✅ Done` or `⛔ Cancelled` at line 3 **changed that line**, or `git status` showed an
  edit to any file under `ai-agents/sprints/done/` or `ai-agents/sprints/cancelled/` that no one asked
  for. ⭐ Then the stop condition belongs in ADR-047 §4 as a ruling, because `0341` mirroring the task
  movers' step 1 will have been shown not to be enough. ⛔ *"The ADR should have specified the
  validation for completeness"* is **closeout**: §4 reuses ADR-033's mover shape deliberately, §3.0
  rule 1 already makes the re-run idempotent, and the owner accepted this residual on 2026-09-11 with
  the gap in front of them. ⛔ *"Codex rated it high"* is **closeout too** — its stronger reading (the
  interrupted-mover re-run) was verified **safe by construction**, and the terminal-input case is what
  is left.
