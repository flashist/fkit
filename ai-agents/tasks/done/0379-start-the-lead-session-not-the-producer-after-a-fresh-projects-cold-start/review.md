# Review — 0379

Task: `ai-agents/tasks/done/0379-start-the-lead-session-not-the-producer-after-a-fresh-projects-cold-start/brief.md`
File(s) under review: `claude/fkit-claude.sh` (fresh-project branch only), `test/harness.mjs`, `test/launcher-contract.test.js`, `test/prove-red.sh` (the `30.` header index line + the Mutation 30 block only), and this task folder's `worklog.md`
Status: closed-out
Coverage: reasoning-only second opinion — Codex ran (`codex-cli 0.152.0`, exit 0) and returned a usable, diff-grounded findings list, but every command it executed was `rg` / `sed` / `nl` / `cat` / `git status` / `git diff --check` — source-text reading, never a test, a fixture, or the launcher itself. All execution evidence below is the Claude reviewer's.

## Reviewer findings

| #  | Round | Sev | Location | Claim |
|----|-------|-----|----------|-------|
| R1 | 1 | medium | `test/launcher-contract.test.js:337-356` (assertion 12b) | 12b is the only test covering the **intake-present** cold start — the path a real owner walks — but it asserts only `r.argvs[0]` (the producer) and never that the lead phase happened. **Measured:** a launcher mutant that restores `exec` on the intake path only (deleting the lead hand-off for every owner who completes the terminal intake) passes the whole suite **43/43 green**. The task's own headline behaviour is unpinned on its primary real-world path. 12b already runs with `FKIT_STUB_INITIATE` set, so the lead invocation is *already produced* and merely unasserted — verified on the real launcher: 2 invocations, second is `['--agent','fkit-lead','--settings','.fkit/settings/lead.json']`. |
| R2 | 1 | low | `test/launcher-contract.test.js:361-377` (assertion 12c) | 12c pins the clean-bail *shape* (one invocation, producer) but not its **exit code**. Ruling Y1's "exit exactly as today" makes exit `0` on a clean bail part of the contract; nothing asserts `r.code === 0` there. 12d pins the non-zero side (130) only, so a change touching only the zero case stays green. |
| R3 | 1 | low | `test/launcher-contract.test.js` (Group C) + `claude/fkit-claude.sh:606` (`[ "$fresh" = 1 ] && [ -z "$role" ]`) | No persistent test combines a **fresh tree with an explicit role**. All four fresh-tree assertions call `runFkit([])`; the `-z "$role"` guard that stops `fkit coder` on a fresh project being hijacked into producer→lead is unpinned. The plan's Verification step 2 named this check and it was discharged **by hand** in the worklog, not as a regression test. Pre-existing gap, but the hijack is now two sessions instead of one. |
| R4 | 1 | low | `claude/fkit-claude.sh:608` (`printf '  When the initiation is done, this tab opens the lead.\n'`) vs `:631` (`exit "$cold_rc"`) | The new second line is printed **unconditionally, before** the intake and the producer run — but the bail path (`cold_rc=0`, tree still fresh) exits **silently with 0**. The owner is promised the lead will open, then dropped to the shell with no explanation. Ruling Y1 settles the *exit behaviour*; the *promise* is new text added by this change (worklog decision D3), so the accepted "silent bail" residual is now materially more confusing than it was before this task. Disposition is the owner's — see the questions returned with this review. |

### Disproven — recorded so nobody chases them

Each was a stated suspicion in the review request; each was traced and cleared:

- **`exit "$cold_rc"` exiting 0 after a clean bail is correct**, not a silent-success bug. `exec claude` gave exactly the same code on that path pre-0379, which is what Y1's "exit exactly as today" requires. (The *messaging* half is R4; the exit code is right.)
- **`pm_is_fresh` genuinely re-reads disk.** It is a shell function (`claude/fkit-claude.sh:587`) re-running its three tests on every call, called at `:602` and `:626`; nothing memoizes it, and factoring it (worklog D1) is what makes "the same predicate" true by force.
- **`"$@"` cannot make either phase misbehave — it is always empty here.** `claude/fkit-claude.sh:236` refuses any run with no role but remaining args (`exit 2`), and the fresh branch requires `-z "$role"`. So `$#` is 0 on every path that reaches the cold start; the `"$@"` in the producer call is inert.
- **The `argvs` accessor did not change what older assertions see.** `argv` is the *last* invocation, and every pre-existing path launches exactly one session. Measured: full unit suite **872/872 pass, 0 fail, 24 suites**.
- **`FKIT_STUB_INITIATE` / `FKIT_STUB_EXIT` do not leak between tests.** Both `runFkit` and `runSync` build env per call and `mkdtemp` a fresh argv dir per run; `stubDir` is not exported, so no other consumer of the stub exists.
- **12 / 12c / 12d and the intake seed pin the contract, not the implementation.** Verified with four independent launcher mutants, each redding exactly one assertion and nothing else: dropping `! pm_is_fresh` → 12c red; dropping `[ "$cold_rc" = 0 ]` → 12d red; `exit "$cold_rc"` → `exit 0` → 12d red; swapping the intake seed → 12b red. (The 12b hole in R1 is about the *hand-off*, not the seed.)
- **Mutation 30 reds at assertion 12's own assertion.** Verified independently: `bash test/prove-red.sh` → exit 0, "hard gate PASSED", zero `✗`, 30 mutations, line `30. cold start execs the producer — "12. fresh project ... then the lead" should go RED ... red`.
- **The build's quoted numbers are accurate.** 872/872 and 30 mutations both re-measured here, not inherited.

## Coder response

<!-- CODER-OWNED — the reviewer never writes this section. -->

Round 1. Every severity below is **derived here from the blast radius traced against the code**, not inherited from the reviewer's label. Each verdict was re-measured firsthand; none is accepted on the reviewer's say-so.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | **CORRECT** — reproduced independently. I rebuilt the escaping mutant (restore `exec` on the intake-present branch only) against a full `claude/` tree copy and measured the launcher suite **43/43 green** — the hand-off could be deleted for every owner who completes the intake and nothing failed. Severity **medium**, derived: no live defect (the shipped launcher is correct), but the task's headline behaviour is unpinned on the *only* path a human walks — test 12 covers the headless/no-intake path, which is CI, not people. | Defect (test-coverage) | Added the lead-phase assertions to 12b — invocation count plus the lead's full argv — beside the seed assertions it already had. Then **proved the hole shut**: the same mutant now reds at **exactly 12b**, with 12 / 12c / 12d still green. Promoted it to a permanent gate as **prove-red mutation 31**, so the regression cannot come back silently. ⚠️ Mutation 30's narrowness check needed a consequential edit — an *unconditional* `exec` now correctly reds 12b as well, so 12b was removed from its "must stay green" list (12c/12d, which pin the gate rather than the hand-off, remain). | ✅ done |
| R2 | **CORRECT** — reproduced independently. Rewrote the clean bail to `exit 7` and measured the launcher suite **43/43 green**: 12c pinned the bail's shape but never its code, and 12d pins only the non-zero side. Ruling Y1 makes "exit exactly as today" contractual, and today's clean bail is `0`. Severity **low**, derived: narrow blast radius — one exit code on a decline path, with the no-second-session shape already pinned. | Defect (test-coverage) | Added `assert.equal(r.code, 0, …)` to 12c. Proved it: the `exit 7` mutant now reds at **exactly 12c**, nothing else. | ✅ done |
| R3 | **CORRECT as stated, but out of scope for this task.** Verified firsthand: exactly four `fresh: true` projects exist in the suite and **all four call `runFkit([])`**, so the `[ -z "$role" ]` guard — the thing that stops `fkit coder` on a fresh tree being hijacked into producer→lead — has no regression test. Severity **low**, derived: the guard is correct in the shipped code and the gap is **pre-existing**, not introduced here; what this task changed is the *cost* of a future regression (two sessions hijacked instead of one), not the guard itself. | **Frontier** — new test coverage for a pre-existing, unrelated gap; adding it would widen this task beyond its approved plan. | ⛔ **Not fixed here — owner-ruled to a follow-up brief** (2026-09-07, verbatim option label: *"Split to a follow-up brief (Rec)"*). Recorded as an accepted residual below. ⛔ I do not write the brief — briefs are the producer's; the driver routes it. | won't fix (frontier) |
| R4 | **CORRECT** — verified against the code. The promise *"When the initiation is done, this tab opens the lead."* prints **unconditionally**, before the intake and before the producer runs; the bail arm then exited with no output at all. Severity **low**, derived: messaging only — no functional or exit-code consequence — but it is a confusion this change itself introduced, so it is not a pre-existing residual. | Defect (UX, introduced by this change) | ⭐ **Owner-ruled** (2026-09-07, verbatim option label: *"Add one line on the bail path (Rec)"*). Added one line to the bail arm: *"Setup did not complete — run `fkit` again when you are ready."* ⛔ **Exit code and session behaviour byte-unchanged** — `exit "$cold_rc"` untouched, no session opened — which is what keeps this clear of ruling Y1's *"exit exactly as today"*. Measured on all three gate outcomes: success → no line, 2 invocations, code 0; Ctrl-C → line, 1 invocation, code **130**; clean bail → line, 1 invocation, code **0**. | ✅ done |

**One deliberate non-test, declared rather than left silent (R4).** The new bail line is **not** pinned by an assertion. The launcher-contract suite contains **zero** stdout assertions — verified by grep — so it pins argv, settings, and exit codes, never launcher prose; test 12 does not pin the *"this tab opens the lead"* promise either. Adding a prose assertion here would set a new and brittle precedent inside a task whose approved plan did not call for one. The line's behaviour on all three gate outcomes was instead measured directly (above). ⚠️ The consequence is real and stated: **reworded or deleted, that line reds nothing.**

## Accepted residuals (shared, do-not-re-litigate)

- **No fresh-tree × explicit-role regression test (R1 round 1 → owner-ruled to a follow-up brief, 2026-09-07)** — What: all four fresh-tree assertions call `runFkit([])`, so the fresh branch's `[ -z "$role" ]` guard — which stops `fkit coder` on an uninitiated project being hijacked into the producer→lead cold start — has no test. Verified correct in the shipped launcher; discharged by hand at build time, in the worklog, not as a regression test. Why (structural): the gap is **pre-existing and independent of this task** — this change altered the *cost* of a regression (two sessions hijacked rather than one), not the guard. Closing it means new coverage for behaviour outside the approved plan, which is a scope widening, not a fix to what shipped here. Owner ruled it to its own brief (verbatim option label: *"Split to a follow-up brief (Rec)"*); ⛔ the brief is the producer's to write. Re-raise only if: the follow-up brief is dropped rather than written, or the `-z "$role"` guard itself changes.
- **Producer-first, then a gated lead hand-off (ruling Y1)** — What: the producer runs the initiation with its live owner channel intact; the launcher re-checks the same freshness predicate and opens lead only on `exit 0` AND a no-longer-fresh tree. Why (structural): options A (lead-first with a spawned producer) and B (manual hand-off) were both put to the owner and rejected — A's downstream behaviour is outside what this repo can prove, and B lands the owner in the producer anyway. Re-raise only if: the initiation stops being producer-only, or the freshness predicate stops being a reliable proxy for "initiated".
- **The bail exits exactly as today (ruling Y1)** — What: Ctrl-C, "skip setup", or an incomplete initiation exits with the cold start's own code and opens nothing. Why (structural): being force-dropped into a session you did not ask for is worse than returning to the shell. Re-raise only if: the owner revisits the force-drop tradeoff itself. ⚠️ Note R4 is **not** a re-raise of this — it concerns the new promise text, not the exit behaviour.
- **The producer session's conversation is lost at the transition (ruling Y1, named and accepted)** — What: only the initiation's *output* (`PROJECT.md`, `architecture.md`) survives into the lead session. Why (structural): unavoidable in any design where two sessions run in one tab. Re-raise only if: session hand-off gains a context-carry mechanism.
- **The harness needed a change (ruling Y1, named and accepted)** — What: the stub records one argv file per invocation so both phases are observable. Why (structural): the old single-file stub truncated on every call, making the producer phase unobservable. Re-raise only if: a cheaper seam appears.
- **The terminal intake stays where it is (ruling Y2)** — What: `.fkit/interview` is unmoved and unedited. Re-raise only if: the owner reopens where the intake runs.
- **Rank stays `P15` (ruling Y3)** — What: no re-ranking; `Depends on` carries the ordering. Why (structural): ADR-035 forbids a mid-board insertion. Re-raise only if: ADR-035 changes.
- **`claude/agents/fkit-lead.md`'s routing line needs no edit (ruling Y4)** — What: its parenthetical stays TRUE under B′ because `fkit` does still go to the producer first. Re-raise only if: the cold start stops starting at the producer.
- **Launcher not manifest-tracked / not dual-homed; prove-red needed no new infrastructure** — What: settled by the plan's own measurements, not gaps. Re-raise only if: the launcher is added to `claude/structure-manifest.tsv`.
