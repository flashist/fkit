# Review — 0388

Task: ai-agents/tasks/done/0388-give-the-sprint-mover-pins-and-the-successor-mode-durable-prove-red-mutations/brief.md
File(s) under review: test/prove-red.sh, test/dashboard-contract.test.js (working tree vs HEAD)
Status: closed-out
Coverage: reasoning-only second opinion — round 1: Codex ran (codex-cli 0.152.0, exit 0) and reasoned over the diff; it executed only `node --check` and `sh -n` (syntax, not behaviour) and its E1–E4 run was blocked by sandbox `mkdtemp` EPERM. Execution evidence is the Claude reviewer's: a scratch-copy prose mutation run through `node --test --test-name-pattern='emitter map'` (R1).

## Reviewer findings
| #  | Round | Sev    | Location | Claim |
|----|-------|--------|----------|-------|
| R1 | 1     | medium | test/dashboard-contract.test.js:3930 | E2's prose needle (`E2_PROSE`, "read its `⟦FACTS⟧` for `drift ambiguous-plan-identity`, which names every other claimant.") pins the RECORD but not the MODE — "render the board — `bash … dashboard.sh ai-agents/sprints/<basename>`" sits outside it. Measured on a scratch copy: rewriting both sprint movers to "run `select-active` — `bash … dashboard.sh select-active ai-agents/sprints`" leaves E1–E4 all green. That is the exact ADR-047 class Item A exists for ("sends a drift to a mode that cannot produce it"). Direction: widen the needle to include the render instruction (whitespace-normalized match already spans the line break); no SKILL.md change needed. Raised by both. |
| R2 | 1     | low    | test/dashboard-contract.test.js:3974 | `E4_SELECT_PROSE` names `ambiguous-active-sprint` and its gloss but nothing tying it to `select-active`; `expectProseOnce` is position-blind, so the bullet moved under another mode's record list stays green. The test title claims "under select-active"; only the render needle (via "The render path's counterpart") carries its mode. Raised by both (Codex X3, rated medium; lowered — the bullet's list format differs from the render block, so a verbatim move is unlikely). |
| R3 | 1     | low    | test/dashboard-contract.test.js:3949 | E3's needles omit the claim's lead clause "This skill runs no mode that reports it —" in both movers, and the `fkit-sprint-cancelled` needle ("step 1 calls `identity` and nothing else reads siblings.") carries no "no drift" wording at all — so an inverted lead ("runs a mode that reports it") stays green. Codex X2's further claim that `identity` is never behaviour-checked is disproven: E3's loop runs `identity` and asserts one line, exit 0, no `drift`. |
| R4 | 1     | low    | test/prove-red.sh:1625 | Mutation 35 has no explicit "no un-mutated `../../cancelled/X` survives" check — one of the brief's four checks ("no un-mutated copy survives"), which mutation 39 does carry. Only bites if a future edit puts two anchors on one line (`sed` without `g`, line-counting guards). Mirrors mutation 34, the precedent. Raised by Codex (X4). |
| R5 | 1     | low    | test/prove-red.sh:392 | The 0i message, edited by this diff, now reads "mutations 14 and 36-39" but still omits mutation 32, which rides the same `make_repo_copy` + `run_dashboard_suite` seam (its own comment says so). Pre-existing omission, flagged by the coder in worklog decision-log item 4; the line was touched, so it is now knowingly incomplete. |
| R6 | 1     | low    | .github/workflows/test.yml:33 | Not in this diff's files — observation. CI `timeout-minutes: 20`, with a comment still quoting "15 mutants" and "~3x headroom" over ~6 min. Local numbers now: unit ~94 s + gate 667 s ≈ 12.7 min, ~1.6x headroom before runner slowness; this task adds ~88 s (5 more mutants). Not measured on the ubuntu runner. |

Suppressed as settled (not rows): count/set of mutations, one-task split, E-tests living in dashboard-contract.test.js, not asserting "no other test reds", `dashboard.sh` comments unpinned — plan.md § "10. Owner rulings on the open decisions"; hand-rolled gate — ADR-026.

Verified sound, no row: mutations 36–38 four checks + named greps (36's awk scoped to `mode_successor`, in-function survivor 0 and whole-file 1, marker once; anchors re-counted: tie-break 2, strict-order line 1, filter case-arm 1); trailing-colon named greps (no S30/S40/S60/E20 titles exist); mutation 35's sed hits the INVERSION sentence S2 substitutes; mutation 39's single-line anchor; E1/E2/E4 behaviour halves are real (not tautologies); E3 per-mover needles (decision-log item 1) consistent with the on-disk sentences; awk/sed constructs are POSIX and already used by mutations 14/32–34 (no ubuntu run by this reviewer).

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect — sev medium (agreed): the guard Item A added passes the exact ADR-047 wrong-mode class it exists for; test-only gap, no runtime impact | `E2_PROSE` widened to start at "render the board — `bash claude/skills/fkit-status/dashboard.sh ai-agents/sprints/<basename>` — and read its …", plus a 2-line comment saying why. Reviewer's rewrite ("run `select-active` — `bash … dashboard.sh select-active ai-agents/sprints`", both movers) re-run on a scratch copy: before the fix E1–E4 green (reproduced), after it E2 red at its PROSE half. Mutation 39's anchor ("for `drift ambiguous-plan-identity`, which names") lies inside the widened needle, so 39 still reds at E2 (full gate re-run). No SKILL.md edit. | ✅ done |
| R2 | CORRECT | Defect — sev low (agreed): title claims "under select-active", needle carried no mode | `E4_SELECT_PROSE` widened to start at that list's lead-in "**it has no roll-up**: `⟦FACTS⟧` **is** its complete output, …" — true of `select-active`, false of the render (its `⟦BOARD⟧` carries a roll-up). "five kinds" deliberately left out so adding a record kind does not red E4. Scratch copy with the bullet moved under the render's record list: before the fix green (reproduced), after it E4 red at its PROSE half. | ✅ done |
| R3 | CORRECT | Defect — sev low (agreed) | Both `E3_PROSE` needles widened to "This skill runs no mode that reports it — step 1 calls `identity` … So the collision is silent on this path". Scratch copies with the lead inverted to "runs a mode that reports it": before the fix green (reproduced, both movers together), after it E3 red at its PROSE half — measured for both movers together and for `fkit-sprint-cancelled` alone. The reviewer's disproof of Codex X2's side claim stands (E3's loop does run `identity`). | ✅ done |
| R4 | CORRECT | Defect — sev low (agreed): latent, bites only if an edit puts two anchors on one line | Per owner ruling "Fix in 35 (Rec)": added the survivor check `grep -c '\.\./\.\./cancelled/X'` must be 0 (`✗ WRONG TARGET — an un-mutated board word survives.`), between the one-line diff check and the landed check, mirroring mutation 39. Synthetic file with two anchors on one line: diff count 1, landed 1 (old checks pass), survivors 1 (new check fires). | ✅ done |
| R5 | CORRECT | Defect — sev low (agreed): message text only; misleads whoever debugs a red 0i | 0i message now reads "mutations 14, 32 and 36-39". Mutation 32 verified to use `make_repo_copy` + `run_dashboard_suite` (29 uses `run_throughput_suite`, so it stays out). | ✅ done |
| R6 | CORRECT | Out of this diff's scope — sev low (derived): a timeout kills the job loudly (CI red, never a false green), and the ~1.6x figure is local, not measured on the runner | No change to `.github/workflows/test.yml`, per owner ruling "File follow-up brief (Rec)"; routed to the follow-up brief `0395` (its folder appeared in backlog/ during this step; not written by this worker). Verified: `timeout-minutes: 20`, comment still says "15 mutants" and "~3x headroom". Status is `won't fix (frontier)` as the closest of the five: not `✅ done` (nothing changed here), not `disproven` (claim holds), not `closeout (re-litigation)` (no residual or ADR settles it), not `blocked` (nothing prevents a fix — the owner placed it out of scope). ADR-045's ledger-terminus lane does not apply: the file is not in this diff. | won't fix (frontier) |

## Accepted residuals (shared, do-not-re-litigate)
- CI timeout headroom out of 0388 (R6) — What: `.github/workflows/test.yml`'s `timeout-minutes: 20` and its stale "15 mutants" / "~3x headroom" comment are not changed by 0388 · Why (structural): owner ruling 2026-09-14, option label "File follow-up brief (Rec)" — the fix needs a runtime measured on the ubuntu runner, which this task cannot take; rejected: fixing it here on local numbers · Re-raise only if: the follow-up brief `0395` is cancelled without a fix, or a CI run of this tree hits the 20-minute timeout.
