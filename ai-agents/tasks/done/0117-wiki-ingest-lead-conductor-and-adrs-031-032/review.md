# Review — 0117-wiki-ingest-lead-conductor-and-adrs-031-032

Task: ai-agents/tasks/done/0117-wiki-ingest-lead-conductor-and-adrs-031-032/brief.md
File(s) under review: `ai-agents/wiki-vault/index.md` · `ai-agents/wiki-vault/log.md` ·
`ai-agents/wiki-vault/wiki/systems/testing-and-verification.md` ·
`ai-agents/wiki-vault/wiki/tasks/build-fkit-sprint-ship-loop-skill.md` ·
`ai-agents/wiki-vault/wiki/tasks/design-fkit-lead-as-orchestrating-front-door-and-sprint-ship-loop.md`
(working tree, uncommitted. `ai-agents/sprints/sprint-2.md` and this brief are the driver's own
bookkeeping — out of scope, not reviewed.)
Status: in-review

Round 1 reviewers: Claude (accuracy/traceability pass) + Codex (adversarial, model-diverse, `codex-cli
0.145.0`, succeeded first try). **FULL model-diverse coverage — no degradation.**

Review bar applied: this is a **documentation/knowledge ingest**, so the bar is **factual accuracy and
traceability**, not runtime correctness. Primary defect class: **overclaiming and dropped caveats**.

## Reviewer findings
| #  | Round | Sev    | file:line | Claim |
|----|-------|--------|-----------|-------|
| R1 | 1     | high   | wiki/tasks/build-fkit-sprint-ship-loop-skill.md:45 | **"One accepted residual, R6" is factually wrong — 0111 recorded TWO, and the omitted one is the more consequential.** `tasks/done/0111-…/review.md:5` states *"R6 + **Process-review-autonomy** accepted residuals (owner-ruled)"*, and its *Accepted residuals* section lists both separately: **"Sprint-loop Process-review autonomy is prose-trust (R4/option b, owner-ruled 2026-07-22)"** (with its own `Re-raise only if:` guard) and **"Crash/idle stranding … (R6)"**. Worse, the **substantive consent-model fact is absent from the entire diff**: nowhere does the vault now say the sprint-loop Process-review worker applies verified-`CORRECT`, in-approved-plan fixes **without per-fix owner approval** — `claude/agents/fkit-coder.md:52-56` calls this the **second** of "**Two** loops [that] override that per-fix gate with a **standing approval**". The diff records the carve-out's *authority* half (who may write) and drops its *consent* half (what gate was removed). Blast radius: the vault is the loop-prevention memory the project consults before non-trivial work — a residual carrying a do-not-re-litigate guard that the vault does not carry is a residual a future reviewer will re-raise. Raised by **both** reviewers (Claude + Codex). |
| R2 | 1     | medium | wiki/decisions/adr-032-fkit-sprint-ship-loop-autonomy-and-consent-model.md:4 (consequence of the diff's scope choice) | **The ingest's own headline finding is recorded only on task pages; the ADR-032 page a reader actually lands on still reads as fully amended.** The diff correctly asserts on two task pages that *"ADR-032 does not yet record the carve-out its own drive sequence depends on"* (verified: `grep` for `carve-out`/`declared-approval`/`Process-review` in `knowledge-base/decisions/adr-032-*.md` returns **nothing**, and `tasks/backlog/0118-…/brief.md` confirms ADR-032 **"predates these two carve-outs"**, still `🔲 Backlog`). But the **vault's** `decisions/adr-032` page carries `**Status**: accepted *(… **amended 2026-07-23** by [[decisions/adr-033-…]] …)*` and **no signal whatsoever** about the missing 2026-07-22 autonomy amendment — so a reader reasonably concludes ADR-033 is the only amendment. Meanwhile `claude/agents/fkit-coder.md:53` cites *"[ADR-032] Decision 3 + **its 2026-07-22 autonomy amendment**"* — a citation to text that does not exist. **This consequence is NOT in the worker's five self-reported flags** (it reported "ADR-032 left byte-unchanged" as a scope decision, not this effect). Fix is outside the approved change surface → owner disposition. Claude only. |
| R3 | 1     | medium | wiki/tasks/build-fkit-sprint-ship-loop-skill.md:39 | **Round 2 is framed as a mechanical extension, erasing a live owner consent-model choice between two options with different guarantee surfaces.** The page says only *"the carve-out authorized the Build worker only … **Extended** to cover the Process-review worker too."* The source records a decision, not a mechanism: `0111/worklog.md:85-87` shows the coder **recommended option (a)** — *"driver relays every accepted fix for owner approval → re-spawns a Build-type worker … (**reuses the carve-out, no new guarantee-surface change**)"* — and `0111/review.md:45` records **"FIXED — owner ruled option (b)"** (widen the carve-out). The owner **overrode the coder's recommended, narrower option** in favor of removing the per-fix gate. Same root as R1: the outcome is recorded as if it were the only available move. Note also the page's round-1/round-2 option letters come from **two different option sets** (round 1's `(b)` = the declined "split"; round 2's `(b)` = widen the carve-out) — the page uses `(a)`/`(b)` at line 38 without flagging the reuse. Claude only. |
| R4 | 1     | low    | wiki/tasks/design-fkit-lead-as-orchestrating-front-door-and-sprint-ship-loop.md:68 · index.md:142 · log.md:398 | **Absence-to-fact promotion: three compressed sites flatten the correctly-scoped "no record it was ever run" into the bare assertion "never run".** The page bodies get it right — design page:58 *"**No record it was ever run.** Searched the vault and the `0109`/`0110`/`0111` task artifacts"*, and `systems/testing-and-verification.md:106` is likewise scoped. But the design page's `## Related` line, the `index.md` one-liner, and `log.md` flag 1 (*"**Probe 2 was never run**"*) state it as established fact. The evidence supports only "no run found in the searched records". I independently searched and also found no evidence probe 2 was run — so the claim is **probably true but not established**, and the search frame (three task folders + the vault) excludes the loop's substantial production history (0123/0124/0127/0129/0139/0140 all reference the driver). Raised by **Codex** (3 instances, deduped); Claude verified and narrowed. |
| R5 | 1     | low    | log.md:395 | **"last full pass was at 132" is wrong — it skips the two more recent vault-wide passes, including the immediately preceding entry.** `log.md:371` (the entry directly above, the 2026-07-26 sync) records *"Lint (targeted, then vault-wide) … Final integrity: 0 broken · 0 one-way · 0 index gaps · 0 dangling · 0 secrets. Vault now **161 pages**"*, and `log.md:342` records another at **136 pages**. Only `log.md:323` (2026-07-22) was at 132. The parenthetical implies the vault has grown far past its last real integrity check, understating how recently it was fully verified. Raised by **Codex**; Claude verified. |
| R6 | 1     | low    | wiki/tasks/design-fkit-lead-as-orchestrating-front-door-and-sprint-ship-loop.md:55 | **Probe 1 framed as a completed binary probe ("ANSWERED, and the answer was NO") is in tension with the same bullet's admission that it was never measured.** Report §13 prescribed *"confirm once against the binary (the 'measure, don't reason' discipline)"*; what actually happened was a reviewer reading a prose contract. **Downgraded from Codex's `medium`**: the caveat is **not dropped** — it appears prominently in the same bullet (line 57: *"a reviewer **reading a contract file**, not a live probe … still **unproven against the running binary**"*), on `systems/testing-and-verification.md`, and in `log.md` flag 1. For a contract-defined refusal, reading the governing contract is substantial evidence, so "answered NO" is defensible; the residue is rhetorical tension, not a false claim. Raised by **Codex**; Claude verified and reduced severity. |

## Coder response
<!-- CODER/WIKI-OWNED — the reviewer never writes this section. -->
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | **CORRECT** — verified independently against the sources, not taken on trust. `0111/review.md:5` reads *"R6 + **Process-review-autonomy** accepted residuals (owner-ruled)"*, and its *Accepted residuals* section lists **both separately**, the autonomy one carrying its own `Re-raise only if:` guard. `claude/agents/fkit-coder.md` confirms the substantive half: *"**Two** loops override that per-fix gate with a **standing approval**"* — (1) `/fkit-task-ship-loop`, (2) the sprint loop's Process-review worker — *"**Nowhere else**"*. My page said *"One accepted residual, R6"* and recorded only the carve-out's **authority** half (who may write), dropping its **consent** half (which gate was removed). | Defect (dropped caveat — factual) | **FIXED.** Replaced with a *Two accepted residuals* section recording **both**, the autonomy one first and marked ⚠️, with its what/why/re-raise guard and the "prose-trust is the named cost, not a defect" line carried across. | resolved |
| R2 | **CORRECT** — verified: `grep` for `carve-out`/`declared-approval`/`Process-review` in `knowledge-base/decisions/adr-032-*.md` returns nothing, `fkit-coder.md` nonetheless cites *"[ADR-032] Decision 3 + its 2026-07-22 autonomy amendment"*, and `0118` is still 🔲 Backlog. The vault ADR-032 page signalled only the ADR-033 amendment, so a reader lands there and concludes it is the only one. | Defect (consequence of a scope choice) | **FIXED under the owner's explicit ruling (option a): a ONE-LINE authorized widening of the approved surface.** Added a `⚠️ STALE — a SECOND amendment is missing` note to the vault ADR-032 page naming the missing 2026-07-22 autonomy amendment, the dangling `fkit-coder.md` citation, and `0118` as the task that lands it. **Exactly one block on exactly that page — nothing else touched there.** | resolved |
| R3 | **CORRECT** — verified: `0111/worklog.md:85-87` records the coder **recommending** the narrower option (*"driver relays every accepted fix for owner approval → re-spawns a Build-type worker … **reuses the carve-out, no new guarantee-surface change**"*), and `0111/review.md` R4 records **"FIXED — owner ruled option (b)"** — the owner **overrode** it and widened the carve-out. My *"Extended to cover the Process-review worker too"* framed a live consent-model choice as a mechanical repair. The option-letter-reuse sub-point is also correct: round 1's and round 2's `(a)`/`(b)` are different sets. | Defect (framing erases an owner decision) | **FIXED.** Round 2 now names the coder's recommendation, states plainly that **the owner ruled the other way**, and carries the architect's safety point that the wider option is nonetheless a ***smaller* surface than the Build carve-out already shipped**. Added an explicit note that the two rounds' option letters do not carry across. Also recorded round 1's **untaken option (c) "probe the binary first"** — germane to this page's own probe thesis (reviewer had noted it as minor/unrecorded). | resolved |
| R4 | **PARTIALLY CORRECT, and the split is the owner's ruling.** The finding is right that three compressed sites flattened *"no record it was ever run"* into *"never run"*, and right that the search frame excluded the loop's later production history. **The `log.md` flag was a genuine overclaim** — a flag is where scope matters most. **The two one-liners are not**: a catalog line and a `## Related` line are indexes pointing at a body that states the scope correctly. | Defect (absence-to-fact) in `log.md`; **frontier-move** on the one-liners | **`log.md` FIXED** — now *"No record probe 2 was ever run"*, naming the searched frame, naming what it **excludes** (0123/0124/0127/0129/0139/0140), and stating explicitly it is *not* established fact that none happened. **`index.md:142` and the design page's `## Related` line left as-is** per the owner's ruling — accepted as reasonable catalog compression. | resolved (partial, owner-scoped) |
| R5 | **CORRECT** — verified: `log.md` records vault-wide passes at **132** (2026-07-22 lint), **136** (2026-07-22 sync) and **161** (the immediately preceding 2026-07-26 sync). My *"last full pass was at 132"* skipped two, and implied the vault had grown far past its last integrity check when in fact it has not grown at all since. | Defect (factual) | **FIXED** — now states the previous vault-wide pass was the immediately preceding entry, also at 161 pages, and that the vault has not grown since. | resolved |
| R6 | **CORRECT as analyzed — and the reviewer's own conclusion is that it is not a false claim.** Verified the caveat is present and prominent in three places: the same bullet (*"a reviewer **reading a contract file**, not a live probe … still **unproven against the running binary**"*), `systems/testing-and-verification.md`, and `log.md` flag 1. For a **contract-defined** refusal, reading the governing contract is substantial evidence, so *"answered NO"* is defensible; what remains is rhetorical tension, not overclaiming. | Frontier-move (rhetorical) | **NO CHANGE**, per the owner's ruling and the reviewer's own severity reduction. Recorded as an accepted residual rather than silently dropped. **I deliberately did not "improve" the wording** — an unrequested edit here would be a speculative fix. | accepted residual |

**Wiki note.** Six findings, **six verified against the primary documents before any edit** — `0111/review.md`, `0111/worklog.md`, `claude/agents/fkit-coder.md`, and the vault pages themselves. **None was taken on the reviewer's or the driver's summary.** All six were confirmed accurate; **zero disputed**. R1 and R3 share one root worth naming: *I recorded outcomes as though they were the only available move*, dropping the alternatives and the gates that were traded away — precisely the "overclaiming and dropped caveats" defect class this review's bar targeted. Four fixes applied autonomously under the sprint-loop declared-approval carve-out (verified-`CORRECT`, in-plan, prose-local); **R2 was applied only because the owner explicitly widened the surface by one line**, and is flagged as such.

**One thing I could not do inside the surface, stated rather than silently skipped:** R1's fix cites ADR-019, but `decisions/adr-019-…` has **no back-link** to `tasks/build-fkit-sprint-ship-loop-skill`, and that page is outside the approved surface. Rather than create a one-way link (schema §Cross-Reference Rules) or edit out of bounds, I referenced **ADR-019 as plain text, not a `[[wiki-link]]`**. A tidy follow-up would add the reciprocal pair.

## Accepted residuals (shared, do-not-re-litigate)
<!-- Added only on the owner's disposition. -->
- **The compressed one-liners state "never run" where the bodies state "no record it was ever run" (R4, owner-ruled 2026-07-26: accept)** — *What:* `index.md:142` and the design page's `## Related` line assert probe 2 was never run; the page bodies, `systems/testing-and-verification.md` and `log.md` all carry the correctly-scoped *"no record … in the records searched"* version. *Why:* a catalog entry and a Related line are pointers to a body that gets it right; compressing a scoped claim into an index line is normal cataloguing, not overclaiming. *Re-raise only if:* evidence emerges that probe 2 **was** run — which falsifies the claim itself, not merely its compression, and means fixing the bodies first.
- **Probe 1 described as "ANSWERED, and the answer was NO" though it was settled by contract-reading, not a live probe (R6, owner-ruled 2026-07-26: accept)** — *What:* the design's §13 asked to *"confirm once against the binary"*; what happened was a reviewer reading `fkit-coder.md`. *Why:* for a contract-defined refusal the governing contract **is** substantial evidence, and the "not measured against the running binary" caveat is carried prominently in three separate places, so nothing is hidden. *Re-raise only if:* a live run contradicts the contract reading — i.e. a spawned coder is observed writing source **without** the declared-approval marker, or refusing **with** it. **"It was never actually probed" is the named, recorded limit, not a new finding.**

## Verified clean — checked and NOT findings (recorded so they are not re-checked)
- **§12 "split" alternative, including "knowingly".** Not an inference — report §12:519-520 reads
  *"the owner chose maximum single-point-of-interaction over the structural plan gate, **knowingly**"*.
  Verbatim-supported. "Zero prior hits vault-wide" also verified (all other vault uses of "split" are
  unrelated senses).
- **All four rows of the *Alternatives weighed* table** match report §12:511-522 faithfully.
- **Probe 1 blocked task 0111.** `0111/worklog.md:60` — *"⛔ Blocked — awaiting owner decision (R1)"*.
- **"Codex's adversarial pass found (finding R1…)"** — attribution correct per `0111/worklog.md:62`.
- **"Round 1 — owner approved option (a)"** — correct per `0111/worklog.md:78`.
- **R1's rejected alternative (b) = the declined "split" option** — correct per `0111/worklog.md:97-98`.
  (Minor, not recorded: that source line also offered **option (c) "probe the binary first"**, which
  the owner did not take — directly germane to this page's own probe thesis, and omitted.)
- **"three prose signals"** — exactly three, exactly those, per `fkit-coder.md:61-64` (a) caller id,
  (b) concrete approved plan, (c) owner approved it via live `AskUserQuestion` relay.
- **All quoted strings from `fkit-coder.md`** — verbatim within their ellipses.
- **ADR-032 genuinely does not record the carve-out**, and **0118/0119 are genuinely still Backlog**.
  Verified directly. (The *consequence* is R2.)
- **Vault counts exact:** 161 pages = 0 features + 8 systems + 33 decisions + 120 tasks. **33 vault ↔
  33 knowledge-base** ADRs.
- **Link hygiene clean:** 0 broken links vault-wide; all 161 pages indexed; 0 dangling index entries;
  the three diff-touched pages have **full reciprocal back-links in both directions**.
- **The 2 self-reported one-way links are exactly right** — a vault-wide scan finds exactly 2, exactly
  those, exactly from `tasks/add-no-secrets-rule-to-fkit-lead`. Honest and precise self-report.
- **No secrets.** Scanned the diff; the only `token` hits are the prose *"not a verifiable token"*.

## Disproven this round (reviewer wrong — do NOT chase)
- **Codex: `build-…-skill.md:35` misquotes `fkit-coder.md` by lowercasing "Do not".** **INCORRECT.**
  The quote is embedded mid-sentence behind an em-dash with a leading ellipsis; down-casing a
  sentence-initial capital is standard quotation practice. Not a defect.
- **Codex: `log.md:396` "all edits staged in the working tree" is false because nothing is
  `git add`-ed.** **INCORRECT (disproven as a new defect).** The state claim is true (nothing
  committed, all five files unstaged-modified), and **"staged in the working tree" is this log's
  established house idiom** — `log.md:356` (*"changes staged in the working tree only"*) and
  `log.md:373` (*"every change is staged in the working tree only"*) use it identically in prior
  entries. Pre-existing convention, not introduced here.

## Suppressed as settled (this round — not re-raised)
- **"The orchestrated plan gate is only prose, not a structural write-wall."** Settled — ADR-031
  honesty clause; per `0111/review.md:82-84` a finding must show the gate **failing in practice**.
  Neither reviewer re-raised it. Noted only to record that the priming held.
- **The declared-approval marker is trust, not proof.** Settled and *correctly carried* by the diff on
  both task pages. R1 is **not** this finding — R1 is about the removed **per-fix approval gate**, a
  different fact that the diff omits entirely.
