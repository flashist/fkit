# Review — 0352

Task: `ai-agents/tasks/done/0352-adr-the-narrow-in-flight-review-fix-lane/brief.md`
File(s) under review: `ai-agents/knowledge-base/decisions/adr-045-an-in-flight-review-finding-terminates-in-the-ledger-not-a-new-task.md` (new, 365 lines) · `ai-agents/tasks/backlog/0352-adr-the-narrow-in-flight-review-fix-lane/worklog.md` (new)
Scope: working tree vs `HEAD`, restricted to this task's change surface. ⚠️ The tree is dirty with other workers' concurrent changes; the board-status flip on `sprint-7.md`, `backlog.md` and `0352/brief.md` is the driver's, not this task's, and is out of scope.
Status: closed-out
Coverage: **both reviewers measured** (ADR-042 D1 — Round 3's state, refreshed to this round's fact; the state is **per round** and Rounds 1–2 are **not** amended — Round 2 was `reasoning-only second opinion` and stays so). Codex ran (`codex-cli 0.152.0`, `codex exec --sandbox read-only`, **exit 0**) and returned a usable findings list. ⭐ **Both passes executed something bearing on what they reported, and this round that bar is met because the claim under review is a tool's runtime behaviour, not a file's text.** **Codex executed** `/usr/bin/grep`, `sed`, `awk`, `python3` and `node` against the NUL-bearing `0246/review.md` and reported their actual output and exit codes (`Binary file … matches`, exit **0**), plus `type -a grep` → `BSD grep 2.6.0-FreeBSD` and `command -v ugrep` → **127** — the evidence for its own reader finding. **The reviewer executed** the same five-reader probe plus a third invocation path (bare `ugrep 7.8.4` vs `ugrep 7.8.4 -I`, exit 0 vs exit 1), an independent per-file loop over all **131** ledgers computing all four rule/reader combinations and the disagreement set, `node --test` over the six decisions-reading suites — **232 pass, 0 fail, exit 0, 16.5s** — and a full `npm test`, which **exited 0 with the prove-red hard gate PASSED** (*"real + unmutated copy green; each mutation reds its NAMED assertion"*). ⚠️ **The main suite's pass/fail totals were truncated by the reviewer's own `tail` and are therefore NOT restated as verified this round**; what is verified is exit 0 and the gate line. ⚠️ **All of it remains partial evidence about this deliverable** — the change surface is Markdown prose; a green suite shows ADR-045's number stays unique and its file-level contracts hold, and proves nothing about whether the prose is right.

⚠️ **Note on the Round 2 header, corrected here rather than quietly:** Round 2 wrote this field as `Coverage (Round 2 — …):`, which is not the schema's `Coverage:` field name. It is restored to `Coverage:` above. The Round 2 **state** is unchanged and unamended. (ADR-042 D1 — the normal state, not a degradation, and it fires no banner and forces no verdict). Codex ran (`codex-cli 0.152.0`, `codex exec --sandbox read-only`, exit 0) and reasoned over the diff at length, independently reproducing every corpus figure in the ADR (130/131 ledgers, 127 `File(s)` fields, 12 empty, 43/63/22/2, and all four combinations 24/23/40/39) — but everything it ran was `grep`/`rg`/`awk`/`sed`/`git` over file **text**, which §Coverage states rules is reading, not measuring. **The execution evidence in this round is the reviewer's:** `node --test` over the six decisions-reading suites (`adr-number-uniqueness`, `converge-contract`, `dashboard-contract`, `dual-home-parity`, `skill-frontmatter`, `wiki-flag-convention`) — **232 pass, 0 fail, exit 0, 15.4s**; plus an independent per-file loop over all 131 `ai-agents/tasks/*/*/review.md` ledgers computing all four rule/reader combinations and enumerating the prefix-vs-exact disagreement set, and a five-reader probe (`grep`, `sed`, `awk`, Python strict-UTF-8, Node `utf8`) of the NUL-bearing `0246/review.md`.

## Reviewer findings

| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | high | `adr-045-…-not-a-new-task.md` §1 conditions C and E (the rows beginning "The review **has not closed**" and "⛔ Condition E") | The entry condition never says how `Status:` is to be read — prefix or exact, and by what reader — and the two readings disagree on 16 of 130 ledgers. Prefix gives the ADR's headline **24/130 unreadable (18%)**; exact gives **40/130 (31%)**. §Context states the prefix rule ("`Status:` value **beginning** `in-review`") but §1, the binding text, says only "reads `in-review`" and "reads anything other than `in-review` / `closed-out`", which reads as exact. Reader choice matters too: `0246/review.md` carries a NUL byte, so plain `grep` sees no header at all while `sed`/`grep -a` reads `Status: closed-out` — that single file is the entire cause of the two reviewers' differing corpus counts. §7's skill edit cannot implement condition C without guessing. |
| R2 | 1 | high | `adr-045-…-not-a-new-task.md` §1 condition B ("Its `file:line` cell names a file inside … `File(s) under review:`") and §4 point 1 ("A lane finding is a work-product defect by construction") | Condition B is a **per-file** test, but ADR-034 rules the opposite granularity: *"**The boundary is per-site, not per-file.** `0159/brief.md` was *both* the task's own brief *and* one of…"*. So "by construction" does not follow. Ledgers routinely list a task's own `worklog.md` inside `File(s) under review:` (`0353`'s does), and a finding on that file passes condition B, enters the lane, and inherits ADR-034's **blocking** work-product bar — while ADR-034 classes it as an own-record residual that does not block. Over-admits to the lane and misapplies ADR-034 in the same step. |
| R3 | 1 | medium | `adr-045-…-not-a-new-task.md` §6 check 1 ("Structural, and already in force — the owner's consent gate is untouched") | *"Nothing enters the lane that the owner did not see and approve"* overstates the gate. The quoted phrase is verbatim and the gate is real, but it covers **only code changes**: `fkit-process-stateful-review` Step 5 reads *"Then **wait for my explicit approval** before changing any code"*. Three of the four terminal states §3 limit 3 admits are coder-set with no owner approval — `disproven` and provisional `won't fix (frontier)` at Step 4 (*"INCORRECT → Status `disproven`, Action `none`"*), and `closeout (re-litigation)` at Step 2. A real in-scope defect misclassified `disproven` terminates in the lane, files no brief, and reaches no board. This is §6's load-bearing answer to limit 4. |
| R4 | 1 | medium | `adr-045-…-not-a-new-task.md` §3 limit 3 ("it is exactly the ledger's own close condition") | Not "exactly". The skill's close condition names **three** states — *"If all novel findings are closeout / disproven / accepted and nothing blocking remains"* — while the ADR's terminal set is four, adding `✅ done`. Worse, `won't fix (frontier)` is set at Step 4 explicitly *"(pending my confirmation to record it as a residual)"*, becoming settled only at Step 6. Treating the token alone as terminal lets a finding still awaiting the owner count as lane-terminated — which contradicts the ADR's own sentence in the same row that a finding the owner *"agrees is real but **defers**"* must file a brief. |
| R5 | 1 | medium | `adr-045-…-not-a-new-task.md` §1 condition A ("It is a row in the ledger's `## Reviewer findings` table carrying this pass's `Round`") | Condition A does not prove the finding came from this review. `fkit-process-stateful-review` Step 1 directs the coder: *"If findings arrived as pasted text rather than already in the file, first append them as rows to *Reviewer findings* (that's seeding the reviewer's section on their behalf — note it)"*. A seeded row carries the current Round and satisfies A. So a finding from an ephemeral review, or any external source, can be pasted into the table and enter the lane — defeating limit 5, which is an owner-kept ruling. |
| R6 | 1 | medium | `adr-045-…-not-a-new-task.md` §1 anti-widening rule ("stated as a rule, not a hope") and §6 check 2 ("the scope field cannot be edited to let something in") | "Cannot" is not true and the ADR knows it. Nothing enforces or detects a widening: no test and no hook reads `File(s) under review:` — the string occurs only as prose in `fkit-task-done`, `fkit-process-stateful-review`, `fkit-stateful-review` and `claude/scaffold/ai-agents/tasks/README.md`; neither review skill records or compares an original scope value; and the reviewer **owns** the header. 128 ledgers are git-tracked, so an edit to an already-committed ledger is diff-visible, but a widening inside one review round lands in the same commit as the ledger and leaves no trace. The ADR's own re-raise list concedes this — *"A `File(s) under review:` field is found **edited mid-review** to admit a finding"* is phrased as something observed, not prevented. |
| R7 | 1 | medium | `adr-045-…-not-a-new-task.md` §4 point 3 (the "⚠️ **An accuracy note**" callout) and `worklog.md` §"The 'frozen' accuracy check the plan flagged" | The accuracy note's own measurement is false. It claims *"I grepped ADR-034 and the whole conventions folder on 2026-08-30 and the word is absent from both."* The ADR-034 half reproduces (0 hits). The conventions half does not: `grep -rnic "frozen" ai-agents/knowledge-base/conventions/` returns 1 hit in each of `one-skill-one-output.md`, `durable-citation-anchors.md` and `priority-is-rank-not-identity.md`. The worklog records the same command as *"→ 0 in every file"*. The conclusion the note supports (do not attribute "frozen" to ADR-034) is correct and unaffected — only the stated measurement is wrong, in a callout whose entire authority is that it measured. |
| R8 | 1 | low | `adr-045-…-not-a-new-task.md` §6 ("Three checks, cheapest first") | §6 offers three checks against the lane becoming a hole, but only check 1 is in force. Check 3 (*"the `Action` cell names the files touched"*) rests on the §2 content rule that §2 itself says *"a follow-up skill edit implements … Nothing changes behaviour until that edit ships"*; check 2 is an instruction, not a mechanism (R6). Only check 1 carries a status marker (*"already in force"*), so a reader counting today's safeguards gets three and has one. Elsewhere the ADR is scrupulous about this, stating "nothing changes behaviour until the follow-ups ship" three separate times. |
| R9 | 1 | low | `worklog.md` §"What was written" ("One file, and nothing else") vs §"This task's own contribution to the diff" ("Written by me — two files") | The worklog contradicts itself about its own output. The first is about the deliverable and the second counts the worklog itself, so both are defensible in isolation, but as written they state different totals for the same run. |
| R10 | 2 | medium | `adr-045-…-not-a-new-task.md` §1, the bullet beginning "**Prefix over exact** is what holds the cost at 24 rather than 40" | **Raised by both reviewers, same numbers.** The count is right; the sentence describing it is false. The prefix-vs-exact disagreement set is **16** ledgers — reproduced — but only **2** carry an open `in-review …` value (`0023-converge-ai-agents-additively-on-launch/review.md`, `0171-write-the-durable-citation-anchors-convention-page/review.md`). The other **14** carry decorated `closed-out …` values, which fail condition C under prefix and fail condition E under exact — a brief either way, **no gate-outcome change**. So "Exact would shut the lane on a further 16 ledgers whose reviews are genuinely open" overstates the benefit of the prefix choice by 8×; the true figure is 2. ⭐ The **choice is unaffected** — prefix still strictly dominates (2 > 0) — but this is the R7 defect class exactly: a false measurement inside the callout whose sole authority is that it measured, and it is the ADR's only stated justification for the prefix ruling that §7 must implement. |
| R11 | 2 | high | `adr-045-…-not-a-new-task.md` §1 condition A's *Proving artifact* cell ("written by the reviewer in this review's own pass") and the rule beneath it ("⛔ **Condition A does not admit a seeded row.**") | **Raised by both reviewers.** ⭐ **The R5 fix breaks §1's own defining property and cannot be evaluated at gate time.** §1 states two lines above that *"Each condition is proved by a **field** that already exists in the ledger schema"*, and the ADR re-asserts the property twice more (§4: *"keeps the gate a field read with no judgement at gate time, which is the property the entry condition exists to have"*; Consequences: *"the entry condition is three field reads"*). But "written by the reviewer in this review's own pass" is **not a field**: the *Reviewer findings* schema is `# · Round · Sev · file:line · Claim` in **both** skills, with no author, origin, or seeded column, and `fkit-process-stateful-review` Step 1 asks only for an unstructured *"note it"*. Provenance is therefore knowable only from outside the ledger — a judgement at the gate, which is what §1 exists to exclude. ⚠️ It is also **over-broad**: it bars findings that genuinely did originate in this review's reviewer pass but reached the file by paste — including Step 0's own bootstrap path, where the coder creates the ledger and *"seed[s] Reviewer findings from whatever findings you were handed"*. Those file briefs, inflating the record-repair rows this ADR exists to cap. ⚠️ And it is **unenforceable**: nothing marks a seeded row, so the same paste that defeats limit 5 defeats this rule — yet it is stated flatly as *"⛔ It does not satisfy A"*, with none of the candour §6 check 2 now carries for the identically-unenforceable anti-widening rule, and it is absent from §6's "exactly one in force" table. |
| R12 | 2 | medium | `adr-045-…-not-a-new-task.md` §1, the bullet "**Reader: an ordinary text reader, with no binary-tolerant decoding**" and the four-row costs table above it | **Raised by Codex; confirmed and extended by the reviewer.** ⭐ **R1's fix landed on the match rule but not on the reader — and as worded it points the §7 implementer at the row the ADR did *not* choose.** Probed five ordinary readers against the NUL-bearing `0246/review.md`: `grep` (no `-a`) sees nothing — **but `sed`, `awk`, Python (`errors='strict'` UTF-8) and Node (`readFileSync … 'utf8'`) all read `Status: closed-out`.** None of those four is binary-tolerant; a NUL is a valid codepoint and nothing is being tolerated. **`grep`'s binary-file heuristic is the outlier**, and it is the sole thing the chosen count of 24 rests on. So the table's labels are inverted against the prose: the row labelled *plain (chosen) = 24* is grep-specific behaviour, while the row labelled *binary-tolerant = 23* is what the plainest possible reader actually does. An implementer following line-for-line the words *"an ordinary text reader, with no binary-tolerant decoding"* in Node or Python produces **23**, not 24. The fix's stated purpose — *"Both are fixed here so the §7 follow-up implements one thing rather than guessing"* — is therefore not met for the reader half. ⭐ **Graded medium, below Codex's `high`, on traced blast radius:** the ambiguity touches **one** ledger, changes **no** gate outcome today (`0246`'s real value fails condition C under every reader — verified), and nothing behavioural ships until §7. |
| R13 | 2 | low | `adr-045-…-not-a-new-task.md` §6 check 1's precision paragraph ("three of the four terminal states in §3 limit 3 are **coder-set with no owner approval**") against §3's per-state table row for `won't fix (frontier)` | **Raised by Codex; confirmed by the reviewer.** ⚠️ **The R3 fix and the R4 fix contradict each other, in the same round.** §3's table rules the Step-4 provisional `won't fix (frontier)` token explicitly **not** terminal — *"It is terminal for the lane only once the matching Accepted residuals entry is recorded. A row still resting on the Step 4 provisional token is unresolved, and **files a brief**"*. §6 then counts that very token among "three of the four **terminal** states" that are coder-set. On the ADR's own table the coder-set-and-terminal set is **two**: `disproven` and `closeout (re-litigation)`. The error is conservative — §6 states the residual as larger than it is — and the sentence points the reader at the authoritative table, which caps the harm. ⚠️ Second, smaller: §3's *"`✅ done` … is carried by that step's 'nothing blocking remains'"* over-reads Step 6, whose first conjunct is a **closed enumeration** (*"all novel findings are closeout / disproven / accepted"*) that `✅ done` does not satisfy; "nothing blocking remains" is a second conjunct, not an alternative membership. Both are over-readings of skill text inside the paragraph written to correct an over-reading of skill text. |
| R14 | 2 | low | `adr-045-…-not-a-new-task.md` §6, the sentence "⛔ **Exactly one of them is in force today**" and its table column "In force today?" | **Raised by Codex; confirmed by the reviewer.** A wording refinement of R8's accepted fix, recorded so the coder can dispose of it rather than meet it again in a later round. §1 calls the anti-widening rule *"a rule the reviewer is bound by"* — a rule that binds **is** in force, even with no mechanism behind it — while §6 counts it as not in force. The defensible claim is *"exactly one is **mechanically enforced**"*. ⚠️ **Mitigating, and stated so this is not inflated:** §6's own table row answers *"⚠️ As role discipline only. No mechanism enforces or detects it"*, and §1 cross-refers to §6 for enforcement, so a careful reader is not misled. ⭐ **This is a legitimate `won't fix` — flagged as such up front.** |
| R15 | 3 | medium | `adr-045-…-not-a-new-task.md` §1, the bullet *"**Reader: one in which a stray NUL byte anywhere in the file makes the whole file unreadable — as `grep` without `-a` does.**"*, its sub-bullet *"`grep` without `-a` nonetheless returns **nothing** from that file — a match on its very first line included"*, and the closing clause *"the `grep` probed here was `ugrep 7.8.4`, and no claim is made about any other implementation"* | **Raised by both reviewers** (Codex graded `low`; ⚠️ **the reviewer grades it `medium`** — see the blast-radius note below). ⭐ **THE FIFTH INSTANCE — the R12 fix's own new claim does not survive re-measurement.** ⭐ **Codex reproduced it from a shell that does NOT carry this environment's `grep` shim** — it saw `grep is /usr/bin/grep`, `BSD grep 2.6.0-FreeBSD`, and got `Binary file … matches` with **exit 0** for the first-line pattern and for `^Status:` alike; its `command -v ugrep` returned **127**, so the ADR's named probe tool **is not on this machine's `PATH` at all** and no independent reader can re-run it as written. Probed 2026-09-02 on this machine, three ways: **`/usr/bin/grep`** (BSD grep 2.6.0-FreeBSD — the `grep` on `PATH`) returns `Binary file …/0246-…/review.md matches` and **exit 0**, for the `^Status:` pattern and for a first-line pattern alike; **bare `ugrep 7.8.4`** (invoked with no `-I`) does **exactly the same** — `Binary file … matches`, **exit 0**; **only `ugrep 7.8.4 -I`** returns nothing, exit 1. ⛔ `-I` (*ignore binary files*) is **injected silently by this environment's `grep` shell function**, never typed — so what was probed was `ugrep -I`, not `grep` and not bare `ugrep 7.8.4`. Two of the bullet's assertions are therefore false as written: *"returns **nothing** … a match on its very first line included"* (both real greps return a message and **exit 0**), and the tool identification *"the `grep` probed here was `ugrep 7.8.4`"* (bare `ugrep 7.8.4` does not exhibit the behaviour; `-I` is its sole cause). ⭐ **What survives, stated so this is not inflated:** the **behaviour** the bullet defines — *whole file*, not per line — **does** hold for BSD grep too (no line content is retrievable from that file, first line included), and the **counts are unaffected** — recomputing all four combinations from the bullet's prose definition directly gives **24 / 23 / 40 / 39** over 131 and over 130, and **no gate outcome moves**. ⚠️ The defect is that the bullet whose sole stated purpose is *"so the §7 follow-up implements one thing rather than guessing"* names an example and a tool+version that an implementer re-running will not reproduce — and an implementer taking the exit-code route from *"returns nothing"* gets **exit 0 = match**, the opposite conclusion. ⚠️ **Why it survived two rounds, named so the class is not mistaken for carelessness:** both reviewers' `grep` is that same shim, so both measured `-I`'s behaviour and both wrote it down as `grep`'s. ⛔ **The same clause is in `worklog.md`** — *"which makes the file invisible to a plain `grep`"*, in the Round 1 out-of-scope note — so a fix that touches only the ADR is a partial sweep, this document's signature defect. ⚠️ **This touches owner-confirmed wording and the disposition is the owner's:** Ruling F's confirmed sentence carries the clause *"as `grep` without `-a` does"*. Its **substance survives** — whole-file, not per-line, reproduces on BSD grep too — so the indicated repair is to **delete the illustrative clause and the `ugrep 7.8.4` attribution**, not to reopen the ruling. |
| R16 | 3 | medium | `adr-045-…-not-a-new-task.md` §6, the check-1 table row — whose answer cell now reads *"✅ **Yes.** `fkit-process-stateful-review` Step 5 halts the coder until the owner answers. Unchanged by this ADR"* — under the R14-reworded column *"Mechanically enforced today?"*, and the headline *"⛔ **Exactly one of them is *mechanically enforced* today**"* | **Raised by both reviewers, at the same severity, from independent evidence.** ⚠️ **The R14 fix changed the column's predicate and made row 1 true by assertion.** Ruling G replaced *"in force"* with *"mechanically enforced"*, and the same pass tightened row 1 to name a mechanism — but the thing named is **a sentence in a skill file**, the same artifact class this table marks *"⚠️ **No — role discipline only**"* for check 2. Verified: **nothing enforces or detects a breach of Step 5.** The complete hook wiring is `build_settings()` in `claude/fkit-claude.sh` — `PreToolUse` on matchers **`Skill`**, **`AskUserQuestion`** and the **Agent / Task** spawn matcher, plus `Stop` and `UserPromptExpansion`; **no hook matches `Edit`, `Write` or `Bash`**. `claude/skill-ownership-hook.sh` denies Skill calls by role; `claude/askuserquestion-marker-hook.sh` and `claude/shiploop-marker-hook.sh` document themselves *"RECORDS ONLY — NEVER DENIES"* / *"NEVER BLOCKS"*; `claude/turn-completion-hook.sh` and `carry-check-hook` are Stop-time / spawn-time presence checks on a reply's shape. ⭐ **The repo already records why no mechanism is possible here:** `claude/carry-check-hook.mjs` states that a hook *"can **NEVER** establish that P is what the owner approved — approval lives in a session channel that leaves no artifact (ADR-021)"*, and `claude/fkit-claude.sh` repeats it — *"approval leaves no artifact — ADR-021"*. An approval that leaves no artifact cannot be mechanically checked, by construction. On §6's own standard the honest answer for row 1 equals row 2's, and the headline count is **zero**, not one — ⛔ **the third recurrence of "a reader counting safeguards gets N and has M" (R8 → R14 → here)**, in the ADR's own load-bearing answer to limit 4. ⚠️ **The counter-argument, stated up front so it can be disposed of rather than met again next round:** Step 5 is a **halt whose resume condition is external** (the owner's answer), which is arguably more mechanical than a bare constraint — if the owner draws the line there, row 1 stands and this is a **frontier-move, not a defect**. ⚠️ **Corroborating, and observed rather than hypothetical:** both rounds of fixes on this very document were applied *"under that loop's declared-approval marker"* — one up-front plan approval covering the whole loop — so the gate §6 glosses as operating *"finding by finding"* did not operate finding by finding in the runs that produced the ADR. ⚠️ **Secondary, same section, partial-sweep residue:** §6's numbered items still head check 1 *"already in force"* and check 3 *"not yet in force"* — the predicate R14 replaced in the headline and column. Neither sentence is false; the terminology is half-swept. |
| R17 | 3 | low | `adr-045-…-not-a-new-task.md` §1, the seeded-row bullet *"✅ **A row seeded from findings that did come from this review's reviewer pass still satisfies A.** In particular `fkit-process-stateful-review` **Step 0's own bootstrap path** … is **not** barred."* | ⚠️ **The R11 fix's illustrating example over-reaches the rule it illustrates — and the quotation it cites is the evidence against it.** Step 0 seeds *"from **whatever** findings you were handed"* (verified verbatim, whitespace-normalised, `claude/skills/fkit-process-stateful-review/SKILL.md` Step 0). The path carries **no origin guarantee**: it is taken precisely when findings arrived **without** a ledger, which is the same arrival shape as an ephemeral review's findings. So *"Step 0's own bootstrap path … is **not** barred"*, stated flatly, exempts a **transport** — which the bullet immediately above rules the rule is not about (*"What the rule bars is an **origin**, not a transport"*) — and re-opens the limit-5 walk-around condition A exists to close. ⭐ **Mitigating, and stated so this is not inflated:** the controlling bullet directly above is correct and unambiguous, a careful reader is not misled, and nothing behavioural ships until §7. ⭐ **The repair is a qualification, not a re-cut** — *"not barred **by that fact alone**; the origin test still applies to whatever was handed"* — which preserves owner **Ruling E**'s shape exactly. ⛔ **Flagged as touching an owner-ruled shape:** Ruling E asked for Step 0 to be named as not barred, so the disposition is the owner's, not a silent edit. |
| R18 | 3 | medium | `adr-045-…-not-a-new-task.md` §4 point 1, the sentence *"Condition B is kept per-file because that keeps **the gate** a field read with no judgement at gate time, which is the property the entry condition exists to have"*, and §Residual risks, *"The over-admission is accepted deliberately, so that **the gate stays** a field read with no judgement at gate time"* | **Raised by Codex; confirmed and extended by the reviewer.** ⛔ **R11's cross-section sweep was incomplete, and this is the section it missed.** R11's fix established that condition **A is a judgement at the gate**, and §1 and Consequences now say so plainly — *"origin is not a field, so this read is a judgement"* and *"⚠️ **Condition A is not yet a field read** … it is a judgement at the gate — the very property §1 exists to avoid"*. **§4 and §Residual risks still assert the pre-R11 claim in the present tense**: that *the gate* is a field read with no judgement at gate time, and that this is *"the property the entry condition exists to have"* — which §1 now says it does **not** have. Two sections of one document give opposite answers to the same question. ⚠️ **This is not cosmetic where it lands:** the §Residual-risks sentence is the **stated reason** for the per-file frontier-move the owner ruled on (Ruling A), so the recorded justification overstates what the choice buys. ⭐ **What survives:** the argument itself is sound in its corrected form — keeping B per-file avoids adding a **second** judgement to the gate — so the repair is a qualification, not a re-cut of the frontier-move. ⛔ **The same stale phrase is in the shared *Accepted residuals* entry** for R2 (*"the entry condition exists to be **three field reads with no judgement at gate time**"*). That section is shared and owner-approved, so the reviewer has not touched it: ⚠️ **the disposition there is the owner's.** ⭐ **Why this row matters beyond its own text:** R11's *Coder response* row records the sweep as covering *"§1's heading, its lead sentence, the *Proving artifact* column header, and both Consequences bullets"* — an accurate list that was reported as a completed sweep, and §4 and §Residual risks were not in it. |

## Coder response

**Round 1 processed 2026-09-02 by `fkit-coder`, the Process-review worker of `/fkit-sprint-ship-loop`,
under that loop's declared-approval marker** (owner approved the plan at `plan.md` via `AskUserQuestion`
on 2026-08-30, label *"Approve as written (Rec)"*, plus four rulings). ⚠️ **Severity in the table below is
derived here from traced blast radius, not inherited from the *Reviewer findings* labels** — where it
coincides with the reviewer's, it coincides because the trace agreed.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT (my severity: **high**) | Defect | §1 now fixes **both** unstated reads: condition C matches on **prefix**, and the reader is a **plain text reader with no binary-tolerant decoding**. A costs table gives the failing-ledger count under all four rule/reader combinations (prefix+plain **24 of 130 — 18%**, the ADR's existing headline; exact+plain 40 — 31%; the two binary-tolerant variants 23 and 39), names the **16**-ledger prefix-vs-exact disagreement, and names `0246/review.md`'s NUL byte as the sole cause of the reader's difference of one — noting that choice changes **no gate outcome** in today's corpus, only the count | ✅ done |
| R2 | CORRECT (my severity: **high**) | **Frontier** — owner Ruling A | ⛔ Condition B **stays per-file** per the ruling. The *"work-product defect **by construction**"* claim is **deleted**; §4 point 1 now states ADR-034's *"The boundary is per-site, not per-file"* against it and separates the two questions — condition B decides **where a finding is recorded**, ADR-034 still decides **whether it blocks**, per site, unrelaxed. The over-admission is recorded as a named frontier-move with a re-raise condition in §Residual risks, and as an *Accepted residuals* entry below | won't fix (frontier) |
| R3 | CORRECT (my severity: **medium**) | Defect | §6 check 1's *"Nothing enters the lane that the owner did not see and approve"* is **removed**. Replaced, per owner Ruling B, with what the consent gate actually covers: **every code change** (`fkit-process-stateful-review` Step 5), but **not** the three coder-set terminal states — `disproven` and provisional `won't fix (frontier)` at Step 4, `closeout (re-litigation)` at Step 2. The residual is named as **not closed by this check**, with the reviewer's next round as what bounds it | ✅ done |
| R4 | PARTIALLY CORRECT (my severity: **medium**) | Defect | ⭐ **Partially:** the literal *"exactly the ledger's own close condition"* is false — the skill's Step 6 names **three** states (*"closeout / disproven / accepted"*) and `✅ done` is not among them — but limit 3's underlying point survives, since `pending approval`, `blocked` and a deferred finding are all excluded either way. Per owner Ruling B the **terminal set is unchanged** (four states); the false claim is **withdrawn in the text** and replaced by a per-state table giving, for each state, **who sets it and whether it is owner-confirmed**: `✅ done` yes; `won't fix (frontier)` **only at Step 6, once the residual is recorded** (Step 4's token is explicitly provisional); `disproven` and `closeout (re-litigation)` **no — coder-set** | ✅ done |
| R5 | CORRECT (my severity: **medium**) | Defect | §1 adds a rule that condition A is **not** satisfied by a row seeded on the reviewer's behalf from pasted text (`fkit-process-stateful-review` Step 1), because A asks where the finding **came from**. Closes the path by which an ephemeral review's findings could be pasted into a stateful ledger and defeat limit 5. ⚠️ **No owner ruling covered R5 — this was an obvious-winner call made autonomously and is flagged to the owner**; it narrows a binding entry condition, though only to what condition A already says it means, and limit 5 is owner-kept (plan Ruling 4) | ✅ done |
| R6 | CORRECT (my severity: **medium**) | Defect | Per owner Ruling C, §6 check 2's *"the scope field **cannot** be edited"* is **softened to the truth**: the anti-widening rule **binds the reviewer, it does not prevent the reviewer** — no test and no hook reads `File(s) under review:` (verified: the string occurs only in `fkit-process-stateful-review`, `fkit-stateful-review`, `fkit-task-done`, `claude/scaffold/ai-agents/tasks/README.md` and the ADR itself; **zero** hits under `test/` or `claude/hooks/`), the reviewer owns the header, and a widening is **detectable only in review**. §1's *"stated as a rule, not a hope"* framing softened and cross-referenced to §6. ⛔ **No detection follow-up filed** — the owner chose the wording fix alone | ✅ done |
| R7 | CORRECT (my severity: **medium**) | Defect | Reproduced: `grep -rnic "frozen" ai-agents/knowledge-base/conventions/` returns **1 hit in each of** `one-skill-one-output.md`, `durable-citation-anchors.md`, `priority-is-rank-not-identity.md` (7 other files 0), while `adr-034-*.md` returns **0**. The *"absent from both"* half is **false and is corrected in place** in §4 point 3, with the three named files and a re-run date; the same false line is corrected in `worklog.md`. ⭐ The note's **conclusion is unaffected** — it rests only on the ADR-034 half, which reproduces | ✅ done |
| R8 | CORRECT (my severity: **low**) | Defect | §6 now heads its three checks with *"⛔ Exactly one of them is in force today"* and a table: check 1 ✅ in force; check 2 ⚠️ role discipline only; check 3 ⛔ not yet — it ships with the §7 follow-up edit. Check 3's own paragraph carries the same marker | ✅ done |
| R9 | CORRECT (my severity: **low**) | Defect | `worklog.md` §"What was written" now states the **same total** as §"This task's own contribution" — one deliverable plus this task's own record, **two files** — and names the earlier discrepancy rather than quietly swapping it. The two-file line is cross-referenced back | ✅ done |

**Nothing was suppressed as settled this round.** *Accepted residuals* was empty at Round 1, and the ADRs
loaded at Step 0 (**ADR-034**, **ADR-029**, **ADR-042**, **ADR-044**, and the deliverable **ADR-045**'s own
*"re-raise only if"* list) carry no condition that makes any of R1–R9 a `closeout (re-litigation)`.
⭐ **In particular ADR-045's own re-raise list does not suppress R1–R9**: it bars re-litigating the
**decision**, whereas every finding this round is a defect in how the decision is **written**.

**⚠️ Out of scope, reported not fixed.** The reviewer's coverage note records a literal **NUL byte at offset
12107** of `ai-agents/tasks/done/0246-build-the-consent-gated-repair-path-inside-the-check-skill/review.md`,
which makes that file invisible to a plain `grep`. Independently reproduced here (plain `grep` exits 1 on its
`Status:` line; `grep -a` reads `Status: closed-out`). Owner **Ruling D** — *"Fold into an existing sweep
(Rec)"* — folds it into sweep task `0357`. ⛔ **Not fixed here**; `0357` is another task's brief and outside
this task's bounds. Routed to the driver for a producer.

**Verification re-run after the edits** (2026-09-02, against the ADR):
- Banned size words / numeric thresholds — `grep -niE 'trivial|one-liner|small enough|under [0-9]+ (lines|bytes|words)|[0-9]+[- ]line'` → **zero hits** (exit 1). ✅
- `path:NNN` citations — `grep -nE '\.(md|sh|js|ts):[0-9]+'` → **zero hits** (exit 1). ✅
- `grep -ni 'frozen'` → **4 hits**, none attributing the word to ADR-034: one stating ADR-034 does **not** use it, one about the conventions folder, two attributing it to `fkit-task-done`. ✅
- Standing rule still quoted verbatim — `grep -c "smallest possible shippable tasks"` → **1**. ✅
- ADR-034 named by number — `grep -c 'ADR-034'` → **19**. ✅
- **`npm test`** — re-run 2026-09-02 after the edits: **792 tests, 792 pass, 0 fail, exit 0**, prove-red **hard gate PASSED** (*"real + unmutated copy green; each mutation reds its NAMED assertion"*). Main-suite duration 68.6s; the mutation gate ran several minutes longer, so the run was detached rather than timed out in the foreground. ⚠️ **This is only PARTIAL evidence about this deliverable and is reported as such.** The change surface is Markdown prose; no source and no test was touched. Six suites do read the decisions folder (`adr-number-uniqueness`, `converge-contract`, `dual-home-parity`, `dashboard-contract`, `skill-frontmatter`, `wiki-flag-convention`), so a green suite **does** show ADR-045's number stays unique and its file-level contracts still hold — and it proves **nothing** about whether the prose corrections above are correct. That judgement rests entirely on the greps and the per-row source reads above.
- **Markdown table integrity** — all **7** tables in the edited ADR parse with a consistent column count. ✅
- **Change surface** — `git status --porcelain` limited to `claude/`, `test/`, `ai-agents/wiki-vault/` and `ai-agents/sprints/` returns **empty**; the only modified files are ADR-045, this ledger and `worklog.md`. `plan.md` re-hashes to `57331b2e5cb4fa11c05a10bb51f266b7e9569628` — **not re-authored**. ✅
- **Reviewer section untouched** — the diff of this ledger is **53 lines added, 1 removed**, and the single removed line is the *Accepted residuals* placeholder. The header (including `Coverage:` and `Status:`) and every *Reviewer findings* row are byte-unchanged. ✅


---

**Round 2 processed 2026-09-02 by `fkit-coder`, the Process-review worker of `/fkit-sprint-ship-loop`,
under the same declared-approval marker** (plan blob re-verified against disk before acting:
`57331b2e5cb4fa11c05a10bb51f266b7e9569628`, 22300 bytes — it matched). ⚠️ **Severity below is derived
here from traced blast radius, not inherited.**

### ⭐ Step 3.5 — regression / oscillation check, stated before any fix

⛔ **Four of this round's five findings are defects my own Round 1 fixes introduced** — R10 and R12 from the
R1 fix, R11 from the R5 fix, R13 from the R3 and R4 fixes contradicting each other. The reviewer named the
class: **a stated measurement or skill-quotation that does not survive re-measurement** — the same class as
R7, recurring in the very round that fixed R7.

**The cause, named:** each Round 1 fix closed its finding by *adding a new asserted claim* — a figure, a
characterisation of a reader, a derivation from skill text — and I verified only the claim I was **fixing**,
never the claim I was **writing**. Fix and new claim were written in one motion; only the fix was checked.

**The discipline adopted for this round, decided before editing:**
1. ⛔ **No figure or quotation enters the ADR unless a command run this round produced it**, and that
   command is re-run after the edit.
2. **Prefer deleting a claim to restating it.** Where a correction needs no number, write the qualitative
   fact and no number.
3. **Every claim about skill text is checked against the skill file byte-for-byte**, whitespace-normalised,
   after the edit — not from memory of Round 1.
4. ⭐ **A consistency sweep across sections after editing.** R13 exists precisely because two fixes in one
   round were written without checking each other.

⭐ **The discipline caught a fifth instance before it shipped.** Re-measuring under owner Ruling F's own
phrasing (*"a NUL suppresses the line"*) reproduced **23**, not 24 — because the NUL in `0246/review.md`
sits at **byte offset 12107**, deep in the body, while the `Status:` header near the top is clean, and
`grep` suppresses **the whole file** (a match on its first line also returns nothing). See R12.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R10 | CORRECT (my severity: **low**) | Defect | Reproduced independently: the prefix-vs-exact disagreement set is **16**, of which **2** carry an open decorated `in-review — …` value and **14** carry decorated `closed-out …` values that fail either way (prefix → condition C; exact → condition E). §1's bullet is **corrected in place** and names the earlier overstatement rather than swapping it: the count stays 16, the **gate-outcome benefit is stated as 2**, and prefix is kept because it strictly dominates — opens the lane on 2, shuts it on none. ⚠️ **My severity grades this one of my own defects *down* from the reviewer's `medium`:** traced blast radius is a reader misled about the size of a benefit — no gate outcome moves, the prefix ruling §7 implements is stated flatly and unambiguously either way, and nothing behavioural ships until §7. ⛔ The low grade is not a claim the class is minor; the class is why this round exists | ✅ done |
| R11 | CORRECT (my severity: **high**) | Defect | Per **owner Ruling E** the seeded-row rule **stays**. Three things done: **(a) over-breadth fixed** — the rule now bars an **origin**, not a transport, and `fkit-process-stateful-review` **Step 0's own bootstrap path** (*"seed[s] *Reviewer findings* from whatever findings you were handed"*) is stated as **not barred**; **(b) the gap stated, not papered** — §1 now says condition A is **not** a field read, that the *Reviewer findings* schema is `# · Round · Sev · file:line · Claim` in **both** skills with no author/origin/seeded column (verified against both files), that nothing marks a seeded row, and that the rule *"binds … it does not prevent"* — the same candour §6 check 2 carries; **(c)** §7 names the follow-up: a **durable provenance field** carried by both review skills together, after which A becomes a field read. ⭐ Consistency swept: §1's heading, its lead sentence, the *Proving artifact* column header, and both Consequences bullets asserting *"three field reads"* / *"every artifact already exists"* are corrected to match. ⚠️ **Recorded in the ADR for the record:** the owner confirmed this rule after Round 1 **before** this evidence existed and **re-confirmed it 2026-09-02 with the evidence in view** — so no later reader mistakes the confirmation for an uninformed one. ⛔ **No skill file edited.** ⛔ **No fourth row added to §6's table** — that would restructure §6's "three checks" beyond the ruling; the candour is placed inline in §1 with a cross-reference instead | ✅ done |
| R12 | CORRECT (my severity: **medium**) | Defect | Reproduced the five-reader probe exactly: `grep` (no `-a`) returns nothing; `sed`, `awk`, Python UTF-8 `errors='strict'` and Node `readFileSync(…,'utf8')` **all** read `Status: closed-out`. Per **owner Ruling F** the reader is now named as a **behaviour** with `grep` as *"the example, not the definition"*. ⭐ **⚠️ DEVIATION FROM THE RULING'S PRESENTED WORDING, flagged for the owner — see the note below this table.** The ruling's phrase was *"a NUL suppresses **the line**"*; measured, that is false and reproduces **23**, not 24. The ADR states the behaviour that actually holds: **a NUL anywhere in the file makes the *whole file* unreadable**, which reproduces **24** — the ruling's stated aim. All four table row labels re-cut accordingly (the old `plain` / `binary-tolerant` labels were inverted against the prose, as the finding said). The ADR also now records that a per-line rule would yield 23, that four ordinary readers disagree with the chosen one, and that the `grep` probed was `ugrep 7.8.4` with **no claim made about any other implementation** | ✅ done |
| R13 | CORRECT (my severity: **low**) | Defect | Both halves fixed, and the contradiction named rather than quietly swapped. **(a)** §6 check 1 now counts **two** coder-set-with-no-owner-confirmation terminal states — `disproven` (Step 4) and `closeout (re-litigation)` (Step 2) — with an explicit note that an earlier draft counted three by including the Step-4 provisional `won't fix (frontier)` token that §3's own per-state table rules **not** terminal, and that the error **overstated** the residual. **(b)** §3's clause that `✅ done` *"is carried by that step's 'nothing blocking remains'"* is **withdrawn**: that phrase is a **second conjunct**, not an alternative way into the first, and the first is a closed enumeration that does not name `✅ done`. ⛔ The ADR now states it **reads nothing into the skill's enumeration and settles nothing about it** — the reconciliation is not this ADR's to make | ✅ done |
| R14 | CORRECT (my severity: **low**) | Defect | ⚠️ **The reviewer flagged this as a legitimate `won't fix`; owner Ruling G overrode that** — *"Reword to 'mechanically enforced' (Rec)"*, and ⛔ explicitly **not** to record it as `won't fix`. §6's headline and its table column now read **mechanically enforced**, and the table says plainly that check 2 **binds** the reviewer — a rule that binds is in force *as a rule* — and that what it lacks is anything enforcing or detecting a breach. ⭐ Row 1 was tightened in the same pass so the new column heading stays true of it: it now names the mechanism (`fkit-process-stateful-review` Step 5 halts the coder until the owner answers) rather than asserting a bare *"Yes, already"* | ✅ done |

⭐ **The Ruling F deviation, stated plainly because the owner has not seen it.** Ruling F was presented as
*"a reader in which a NUL suppresses the line, as `grep` does"*, with the stated aim that **24 be
reproducible from the ADR's own words**. Measured this round, those two halves conflict: a per-line rule
reproduces **23**. The ADR therefore implements the ruling's **aim** and its **shape** (behaviour named,
`grep` as example not definition) with the **behaviour corrected to whole-file**, which reproduces 24.
⛔ Writing the ruling's literal phrase would have shipped a fifth instance of the exact defect class this
round exists to stop. **This is a judgement call about an owner ruling and is returned for the owner's
confirmation rather than settled here.**

**Nothing was suppressed as settled this round.** R10–R14 are each checked against *Accepted residuals*
(the R2 per-file/per-site entry) and against the ADRs loaded at Step 0 (**ADR-034**, **ADR-029**,
**ADR-042**, **ADR-044**, and ADR-045's own *"re-raise only if"* list). ⭐ None matches: the residual and
ADR-045's list bar re-litigating the **decision**, while every finding this round is a defect in how the
decision is **written**. ⛔ **R1–R9 were not re-opened.**

**⚠️ The reviewer's note on R2's residual entry — judged, not silently kept.** The reviewer observed, as a
note rather than a finding, that the entry's second half (*"the reverse — a real work-product defect waved
through as own-record…"*) is broader than its own cause, since it is not caused by keeping condition B
per-file. **Verified: the observation is right.** ⛔ **Left as written — judged not a defect.** An
over-broad re-raise condition costs an extra look and can never cause a missed one, so it errs toward
re-opening a settled tradeoff, which is the safe direction; tightening it would make re-raising an
owner-ruled frontier-move *harder*. R2 is **not** re-opened.

**Verification re-run after the Round 2 edits** (2026-09-02) — ⭐ **every figure the ADR states was
re-measured, not carried:**
- Four rule/reader combinations, per-file loop over all **131** ledgers: **24 / 23 / 40 / 39**, matching
  the ADR's table under the corrected whole-file reader. Under a per-**line** reader they are 23 / 23 / 39
  / 39 — which is how the R12 defect was caught.
- Disagreement set **16**; decorated-open **2**; decorated-`closed-out` **14**; ledgers containing a NUL:
  **exactly 1**.
- §Context's dated table re-measured: today's corpus reads `in-review` **44**, `closed-out` **64**, neither
  **22**, no `Status:` line **1**; `File(s) under review:` present **129** (2 none), **12** empty. ⛔ The
  headline is **unchanged at 24 — 18%**. The ADR now carries this re-measure beside the dated snapshot so a
  reader re-running the loop is not surprised by the row-level drift.
- `frozen`: **0** in `adr-034-*.md`; **1 hit in each** of `one-skill-one-output.md`,
  `durable-citation-anchors.md`, `priority-is-rank-not-identity.md`. Unchanged from Round 1.
- Every skill fragment quoted in the ADR re-checked **whitespace-normalised against the skill file**:
  Step 1 seeding, Step 0 bootstrap, Step 6 close condition, the Step 4 provisional clause, the `disproven`
  clause, the Step 5 approval clause, and the `# · Round · Sev · file:line · Claim` schema in **both**
  skills — **all match**. ⭐ **The standing rule was diffed against `fkit-task-brief/SKILL.md` for the
  first time** (Round 1 had only counted it): **byte-identical modulo blockquote markers**. Its step-3
  companion sentence is **word-identical**, with emphasis re-rendered (skill: `*independent
  shippability*` inside a bold line; ADR: `**independent shippability**` inside an italic quote) — checked
  and judged a quoting artifact, not a misquote.
- Drafting constraints: banned size words / numeric thresholds → **zero hits** (exit 1); `path:NNN`
  citations → **zero hits** (exit 1); `grep -ni frozen` → 4 hits, **none** attributing the word to
  ADR-034; `ADR-034` named **19** times; standing rule present **1**. ⭐ The new text cites the NUL by
  **byte offset**, never by a naked line number, per `conventions/durable-citation-anchors.md`.
- Markdown table integrity: all **7** tables parse with a consistent column count.
- **`npm test`** — **792 tests, 792 pass, 0 fail**, prove-red **hard gate PASSED** (*"real + unmutated copy
  green; each mutation reds its NAMED assertion"*). ⚠️ **Partial evidence about this deliverable, and
  reported as such:** the change surface is Markdown prose. A green suite shows ADR-045's number stays
  unique and its file-level contracts hold; it proves **nothing** about whether the prose corrections are
  right. That rests entirely on the measurements and quote-diffs above.
- **Change surface** — `git status --porcelain` over `claude/`, `test/`, `ai-agents/wiki-vault/` and
  `ai-agents/sprints/` returns **empty**. This round's modified files are ADR-045, this ledger and
  `worklog.md`. `plan.md` re-hashes to `57331b2e5cb4fa11c05a10bb51f266b7e9569628` — **not re-authored**.
- **Reviewer section untouched** — no row in *Reviewer findings* was edited, and the ledger header's
  `Coverage:` field was not overwritten (it is the reviewer's).

**⛔ `Status:` left at `in-review` — the ledger is NOT closed, deliberately, and for the same standard
applied in Round 1.** `fkit-process-stateful-review` Step 6 closes when *"all novel findings are closeout /
disproven / accepted and nothing blocking remains"*. R10–R14 are all `✅ done`, which that **closed
enumeration does not name** — the very over-reading R13(b) corrects in the ADR this round. Closing over it
here while correcting it there would be the same defect in a new place. ⭐ **Second, independent reason:**
this round deviates from the presented wording of owner **Ruling F**, and substantial new prose has not yet
been read by any reviewer. Both want an eye before close.

**⚠️ Out of scope, restated so it is not lost.** The NUL byte at offset 12107 of
`ai-agents/tasks/done/0246-build-the-consent-gated-repair-path-inside-the-check-skill/review.md` folds into
sweep task `0357` by owner **Ruling D** (*"Fold into an existing sweep (Rec)"*). ⛔ **Not fixed here** —
outside this task's bounds. Routed to the driver for a producer.

---

**Round 3 processed 2026-09-02 by `fkit-coder`, the Process-review worker of `/fkit-sprint-ship-loop`,
under the same declared-approval marker** (plan blob re-verified against disk before acting:
`57331b2e5cb4fa11c05a10bb51f266b7e9569628`, 22300 bytes — it matched). ⚠️ **Severity below is derived here
from traced blast radius, not inherited.**

### ⭐ Step 3.5 — regression / oscillation check, stated before any fix

⛔ **Three of this round's four findings are defects my own Round 2 fixes introduced** — R15 from the R12
fix, R16 from the R14 fix, R17 from the R11 fix. R18 is the R11 fix's **sweep stopping one section short**.

⭐ **The reviewer's answer to my Round 2 question — recorded because it is the finding behind the finding.**
Asked whether Round 2's discipline broke the pattern, the reviewer answered: **"No — the discipline did not
break the pattern. It narrowed it sharply, and it broke exactly one half of it."** ✅ **Numbers: broken.**
Every figure the ADR states reproduced under two independent measurements, over 131 ledgers and over 130.
⛔ **Characterisations: recurred.** Rounds 1–2's rules 1–3 governed **figures** and **quotations**; R15 and
R16 are **neither** — a characterisation of a **tool**, and a claim that something **is a mechanism** — each
written alongside a fix and never re-measured. Its exact words: *"Round 2's own claim that the discipline
caught a fifth instance before it shipped is true — and a sixth, seventh and eighth shipped anyway."*

**⭐ The discipline extended before editing, and what it was extended to.** Three rules added to the four:
**(5)** no characterisation of a **tool, reader or artifact** enters the document unless a command run this
round produced it, and the command must be re-runnable by an independent reader **as written** — ⚠️ where
the environment shadows a tool, probe the **absolute binary** and every invocation path claimed; **(6)** no
claim that something is **mechanically enforced** unless the mechanism is located by name in a file and
shown to fire on the relevant event — a sentence in a skill file is **discipline, not mechanism**; **(7)** a
correction's sweep is **enumerated by `grep`** over the whole document and this task's records, never from
memory, with every hit given a disposition.

⭐ **Why this round is structurally safer, and the condition owner Ruling L rests on: all four repairs are
deletions or qualifications.** ⛔ **No repair added a new asserted claim**, so the failure class — a *new*
claim written alongside a fix and never measured — has no purchase on them. The R16 fix in particular was
written to borrow §6 row 2's **own wording** rather than import the hook-wiring evidence into the ADR; that
evidence is recorded in `worklog.md` instead.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R15 | CORRECT (my severity: **medium**) | Defect | Reproduced independently, three ways: **`/usr/bin/grep`** (BSD grep 2.6.0-FreeBSD) returns `Binary file … matches` and **exit 0** for `^Status:` and for a first-line pattern alike; **bare `ugrep 7.8.4`** (the shim's own binary, no `-I`) does **exactly the same**; **only `ugrep -I`** returns nothing, exit 1 — and `-I` is injected by this shell's `grep` function (`snapshot-zsh-…v239z7.sh`, lines 4467–4471), never typed. `command -v ugrep` → **not found**. Per owner **Ruling H** the clause *"as `grep` without `-a` does"* and the `ugrep 7.8.4` attribution are **deleted**, ⛔ **with no replacement tool characterisation** — the bullet now states that no tool is named as definition or example, and records the removal rather than swapping it. ⭐ Two further false `grep` clauses in the same passage (*"returns **nothing** … first line included"*, *"finds no `Status:` line"*) are removed **the same way — by deletion**, since leaving verified-false text standing was not an option and Ruling H's shape is *delete, do not replace*. ⭐ **Kept:** the behaviour definition (a NUL anywhere ⇒ the whole file unreadable — it reproduces on BSD grep too) and every count. ⛔ **`worklog.md`'s copy fixed the same way** — the partial-sweep failure this document is named for | ✅ done |
| R16 | CORRECT (my severity: **medium**) | Defect | Verified: **nothing mechanically enforces Step 5.** `build_settings()` in `claude/fkit-claude.sh` wires `PreToolUse` on **`Skill`**, **`AskUserQuestion`** and **`Agent\|Task`**, plus `Stop` and `UserPromptExpansion` — ⛔ **no matcher for `Edit`, `Write` or `Bash`**; the two marker hooks self-document *"NEVER DENIES"* / *"NEVER BLOCKS"*; and `carry-check-hook.mjs` records that a hook can *"**NEVER** establish that P is what the owner approved — approval lives in a session channel that leaves no artifact (ADR-021)"*. Per owner **Ruling I**, row 1 becomes **"⚠️ No — role discipline only"**, the headline becomes **"*None* of them is mechanically enforced today"**, and §6 now says plainly that **the anti-hole answer rests on discipline**. ⭐ The counter-argument the reviewer stated up front (a halt whose resume condition is external is arguably more mechanical) is **disposed of by the ruling, not left open**. ⚠️ **Secondary half-sweep also fixed:** item 1's *"already in force"* → *"binding"*, its *"No lane fix reaches the codebase unapproved — that much is exact"* → *"is **permitted** to … exact **as a rule**"*, and item 3's *"not yet in force"* → *"it does not exist yet"*. ⭐ **No new asserted claim entered the ADR** — row 1 mirrors row 2's existing wording; the hook evidence lives in `worklog.md` | ✅ done |
| R17 | CORRECT (my severity: **low**) | Defect | Verified against `claude/skills/fkit-process-stateful-review/SKILL.md` Step 0, whitespace-normalised: it seeds *"from **whatever** findings you were handed"* — **no origin guarantee**. Per owner **Ruling J** the flat *"is **not** barred"* becomes *"is **not barred by that fact alone; the origin test still applies to whatever was handed**"*, with a clause naming why: being seeded at Step 0 is a **transport**, and the bullet immediately above rules that A tests an **origin**, not a transport. ⭐ **Owner Ruling E is preserved exactly** — the seeded-row rule is neither widened nor narrowed; only its illustrating example is qualified | ✅ done |
| R18 | CORRECT (my severity: **medium**) | Defect | Verified: §1 and Consequences say condition A *"is not yet a field read"* and is *"a judgement at the gate"*, while §4 and §Residual risks still asserted the **pre-R11** claim that *the gate* is a field read with no judgement at gate time. Per owner **Ruling K** all **three** sites are corrected together: **§4 point 1** now says a per-site test would add a **second** judgement and states plainly that the gate is **not** judgement-free; **§Residual risks** now reads *"B itself stays a field read and the gate carries no **second** judgement"*, naming A's; and the shared ***Accepted residuals*** entry's *Why* clause is corrected in place, **labelled as a stale-phrase correction**. ⛔ **R2 is NOT re-opened and remains `won't fix (frontier)`** — the decision is untouched; only its recorded reason is corrected. ⭐ Swept by `grep` per new rule 7: `judgement at gate` / `field read` / `three field reads` across the ADR and both records — **no stale hit remains**; §7's forward-looking *"becomes a **field read** … instead of a judgement at the gate"* is correct and left alone | ✅ done |

**Nothing was suppressed as settled this round.** R15–R18 are each checked against *Accepted residuals*
(the R2 per-file/per-site entry) and the ADRs loaded at Step 0 (**ADR-034**, **ADR-029**, **ADR-042**,
**ADR-044**, and ADR-045's own *"re-raise only if"* list). ⭐ None matches: the residual and ADR-045's list
bar re-litigating the **decision**, while every finding this round is a defect in how the decision is
**written**. ⛔ **R1–R14 were not re-opened.** ⛔ The *Accepted residuals* edit under Ruling K is a
**stale-phrase correction inside R2's recorded reason**, not a re-opening of R2.

**Verification re-run after the Round 3 edits** (2026-09-02) — ⭐ **including the claims this round did
NOT touch, because R18 exists for want of exactly that:**
- ⭐ **Three-way tool probe** (the R15 evidence): `/usr/bin/grep` → `Binary file … matches`, **exit 0**;
  bare `ugrep 7.8.4` → identical; `ugrep -I` → nothing, exit 1; `command -v ugrep` → not found.
- ⭐ **Four-reader probe re-run, untouched claim:** `sed`, `awk`, Python strict-UTF-8 and Node `utf8`
  **all** read `Status: closed-out` from `0246/review.md`. NUL count **1**, first offset **12107**.
- ⭐ **Four rule/reader combinations re-measured over all 131 ledgers: 24 / 23 / 40 / 39** — unchanged, and
  matching the ADR's table. Disagreement set **16**; decorated-open **2**; decorated-`closed-out` **14**;
  ledgers containing a NUL **1**. ⛔ The headline **24 — 18%** is unaffected by every Round 3 edit.
  ⭐ **Re-run a second time *after* this ledger's `Status:` flip below** — all of it holds.
- ⚠️ **One figure my own close moved, caught by new rule 7 and corrected — reported because it is a defect
  this round created, not one it was handed.** §Context's re-measure sentence read `in-review` **44**,
  `closed-out` **64**. Closing this ledger moved **`0352`'s own row**, so today's reading is **43 / 65**.
  The ADR now states 43 / 65 and names the cause, so one row moving is not read as corpus drift. ⛔ This is
  a correction to an **existing measured figure**, re-measured after the flip — **not a new asserted
  claim**, so Ruling L's condition still holds. It is outside the four rulings and is logged as such in
  `worklog.md`.
- **Hook wiring** (the R16 evidence): `PreToolUse` matchers are `Skill`, `AskUserQuestion`, `Agent|Task`;
  plus `Stop` and `UserPromptExpansion`. ⛔ **No `Edit`, `Write` or `Bash` matcher.**
- **Skill quotation re-checked byte-for-byte, whitespace-normalised:** Step 0's *"whatever findings you
  were handed"* — matches.
- **Drafting constraints re-run against the edited ADR:** banned size words / numeric thresholds
  (`trivial|one-liner|small enough|under N (lines|bytes|words)|N-line`) → **zero hits**; `path:NNN`
  citations → **zero hits**; `grep -ni frozen` → **4 hits, none attributing the word to ADR-034**;
  standing rule present **1**; `ADR-034` named **19** times.
- ⭐ **Stale-predicate sweep (new rule 7):** `in force` / `mechanically` / `exactly one` / `consent gate`
  across the ADR — every hit inspected; **no leftover claiming a mechanism**. `grep`/`ugrep`/`` `-a` ``
  across the ADR → **zero hits**. `judgement at gate` / `no judgement` → **zero stale hits**.
- **Markdown table integrity:** all **7** tables in the ADR parse with a consistent column count.
- **`npm test`** — **exit 0**, prove-red **hard gate PASSED** (*"real + unmutated copy green; each mutation
  reds its NAMED assertion"*), all **28** mutations red their named assertion. ⚠️ **The gate run's own
  main-suite totals were truncated by my `tail`, so they are not restated from it**; the totals were
  obtained by re-running `node --test test/*.test.js` directly: **792 tests, 792 pass, 0 fail**, 46.2s.
  Separately, the six decisions-reading suites: **232 pass, 0 fail**, 25.4s. ⚠️ **Partial evidence about
  this deliverable, and reported as such:** the change surface is Markdown prose, and no source or test was
  touched. A green suite shows ADR-045's number stays unique and its file-level contracts hold; it proves
  **nothing** about whether the prose corrections are right. That rests entirely on the measurements,
  source reads and greps above.
- **Change surface** — `git status --porcelain` over `claude/`, `test/`, `ai-agents/wiki-vault/` and
  `ai-agents/sprints/` returns **empty**. This round's modified files are ADR-045, this ledger and
  `worklog.md`. `plan.md` re-hashes to `57331b2e5cb4fa11c05a10bb51f266b7e9569628` — **not re-authored**.
- **Reviewer section untouched** — no row in *Reviewer findings* was edited, and the header's `Coverage:`
  field was not overwritten (it is the reviewer's).

**✅ `Status:` set to `closed-out` — under owner Ruling L, which is the authority for closing here.**
⚠️ **Stated plainly because it reverses my own position twice taken:** in Rounds 1 and 2 I declined to close
on `fkit-process-stateful-review` Step 6's **closed enumeration** (*"closeout / disproven / accepted"*),
which does not name `✅ done`. ⭐ **Ruling L is the owner's answer to exactly that** — verbatim label
*"Act, then close — under the no-new-claim condition (Rec)"* — so closing here is **authorized, not a
departure**, and the ruling is recorded as the authority. ⭐ **The ruling's condition is met:** all four
repairs are deletions or qualifications, and ⛔ **no repair required a new asserted claim I could not
verify** — the two closest calls (R15's extra deletions, R16's row-1 wording) were both resolved *by
deleting or by borrowing existing wording*, precisely to keep it met. Had it not been met, the ledger would
have stayed open with the claim named.

⚠️ **Out of scope, restated so it is not lost.** The NUL byte at offset 12107 of
`ai-agents/tasks/done/0246-build-the-consent-gated-repair-path-inside-the-check-skill/review.md` folds into
sweep task `0357` by owner **Ruling D** (*"Fold into an existing sweep (Rec)"*). ⛔ **Not fixed here** —
outside this task's bounds. Restated to the driver for a producer.

## Accepted residuals (shared, do-not-re-litigate)

- **Condition B is per-file while ADR-034's boundary is per-site** (from R2; **owner Ruling A**, 2026-08-30,
  label verbatim *"Record as a deliberate frontier-move (Rec)"*) — **What:** ADR-045's condition B admits a
  finding to the lane when its `file:line` cell names a file listed in `File(s) under review:`. That is a
  **per-file** test. ADR-034 rules the opposite granularity — *"**The boundary is per-site, not per-file.**"*
  — so a finding on a task's own `worklog.md` listed in that field passes condition B even though ADR-034
  may class the site as an own-record residual. The per-file test is **kept**. · **Why (structural):** the
  entry condition **aims at field reads with no judgement at gate time**; a per-site test would put a
  **second** judgement at the gate, which is what the owner's narrow-lane ruling was chosen to avoid.
  ⚠️ **Stale-phrase correction, Round 3, owner Ruling K — R2 itself is NOT re-opened and stays
  `won't fix (frontier)`:** this clause previously read *"exists to be three field reads with no judgement
  at gate time"*. R11 (Round 2) established that **condition A is not a field read** and is a judgement at
  the gate, so the old phrasing overstated what keeping B per-file buys. The **decision is unchanged**;
  only its stated reason is corrected, in step with §4 point 1 and §Residual risks in ADR-045. The
  cost is bounded by splitting the two questions in §4 point 1: condition B decides only **where the finding
  is recorded**, and **ADR-034 still decides whether it blocks**, per site, unrelaxed. The rejected
  alternative — re-cutting condition B to per-site — was weighed and declined by the owner. The false
  *"work-product defect **by construction**"* claim that rode on the per-file test **was** removed; only the
  over-admission is accepted. · **Re-raise only if:** a lane-admitted finding on a task's **own record** is
  found to have been treated as a **blocking** work-product defect and to have driven a further review round
  ADR-034 would not have required; **or** the reverse — a real work-product defect waved through as
  own-record because its site sat inside a file the ledger listed for both reasons.
