# Review — 0369

Task: `ai-agents/tasks/done/0369-amend-the-review-ledger-location-column-to-heading-plus-fragment-for-coordination-documents/brief.md`
File(s) under review: `claude/skills/fkit-stateful-review/SKILL.md` · `claude/skills/fkit-process-stateful-review/SKILL.md` · this task folder's `worklog.md`
Status: closed-out
Coverage: both reviewers measured — Codex `codex-cli 0.152.0`, exit 0, ran `node --test test/coordination-citation-policy.test.js test/reference-integrity.test.js` (19 pass; 22 fixture tests blocked by the read-only sandbox's `EPERM` on `mkdtemp`) plus parity and link checks; the Claude reviewer ran the same two suites unsandboxed → **41 pass, 0 fail**.

⚠️ **Note on this ledger's own header.** It uses the amended `Location` column, not `file:line`. The amendment is not live in the gitignored `.claude/` mirror this round's reviewer executed from, so the column name here is taken from the canonical owner-approved source under review, not from the running skill.

## Reviewer findings

| #  | Round | Sev | Location | Claim |
|----|-------|-----|----------|-------|
| R1 | 1 | low | `claude/skills/fkit-stateful-review/SKILL.md:71-76` · `claude/skills/fkit-process-stateful-review/SKILL.md:77-82` | The insertion shifts **40 of 61** distinct inbound `path:line` coordinates that cite these two files, across **42** artifacts (6 ADRs, 3 `wiki-vault/` pages, `claude/skills/fkit-sprint-ship-loop/SKILL.md:126`, an archived sprint board). `plan.md § D4` enumerates every **outbound** `file:line` site and claims *"Every site gets a verdict. No site is left implicit"*; the **inbound** citation surface is never enumerated, and `worklog.md § Flags` does not name it. Most of those coordinates were already stale at `HEAD` (spot-checked: the coder skill's `:195` and `:200-201`, the reviewer skill's `:95` and `:156` all pointed at wrong content before this change), so this change is **not** the origin of the rot — but it adds a deterministic **+6** (coder skill, from line 77) / **+6 then +7** (reviewer skill, from lines 71 and 143) to all 40. A task whose entire subject is durable citation anchors should say so in its own record. |
| R2 | 1 | low | `claude/skills/fkit-stateful-review/SKILL.md:208` | ⚠️ **Touches an owner-ruled OUT verdict — new evidence, not re-argument.** `plan.md § D4` ruled both Hard-rules bullets OUT with one shared reason: *"scoped to verifying claims against the code"*. That reason is true of the coder half — `claude/skills/fkit-process-stateful-review/SKILL.md:247` opens *"Read the code; cite `file:line`"* — and **false of the reviewer half**, which reads *"Both reviewers are **inputs to evaluate, not authorities** — verify every claim; cite `file:line`."* with no code-scoping words at all. Compare `claude/agents/fkit-reviewer.md:83`, which keeps them (*"Verify every claim **against the code** before reporting it… Cite `file:line`"*). So the reviewer's stateful skill now carries an unqualified *"cite `file:line`"* in its Hard rules — the section a reviewer treats as binding, and the **last** citation instruction in the file — contradicting the new rule block ~140 lines above it. Not a request to widen the ban; the minimal correct form is a two-word scoping, not a prohibition. |
| R3 | 1 | low | `claude/skills/fkit-stateful-review/SKILL.md:74` (and the byte-identical `claude/skills/fkit-process-stateful-review/SKILL.md:80`) | J4's new sentence — *"put the heading in the `Location` cell … and the quoted fragment in `Claim`"* — instructs pasting a verbatim fragment from a coordination document into a **markdown table cell**, with no instruction to escape `|`. The documents most often quoted are themselves tables: `ai-agents/sprints/sprint-7.md` has 70 pipe-leading lines and `ai-agents/sprints/backlog.md` has 208. An unescaped pipe splits the findings row. `0322` (*"escape the stray pipes in the board rows and guard against new ones"*, open, unscheduled) is this exact failure class already filed against other artifacts. Frontier tension noted: J4 capped this at **one** sentence, so a caveat is a second one. |
| R4 | 1 | low | this task folder's `worklog.md` § *Decision log — autonomous calls made without asking*, row **A1** | Raised by both reviewers. A1's justification is wrong on two counts. It says keeping the `> ` prefixes *"preserves the four-line structure in every renderer (consecutive plain lines would collapse into one paragraph)"* — but (a) the block is **five** source lines, not four, and (b) CommonMark collapses consecutive lines **inside a blockquote** into one paragraph exactly as it does outside one, so the blockquote buys no line-structure preservation at all. The **decision** is still right (it reproduces `plan.md § 3.2` as written, and matches the callout idiom already in both files); only the stated reason is false. Record accuracy in an ADR-019 audit log. |
| R5 | 1 | low | `claude/skills/fkit-stateful-review/SKILL.md:73` (and `claude/skills/fkit-process-stateful-review/SKILL.md:79`) | Raised by Codex. The clause *"`test/coordination-citation-policy.test.js` **reds on it** in any open task folder"* is true for a plain findings-table row but overstates the guard elsewhere: `maskFencesAndQuotes` (that file, `:268-280`) blanks fenced blocks **and blockquote lines**, so the same coordinate written inside a fence or a `> ` quote stays green — and resolved shorthand is out of `TARGET` by name. Mitigating: the ban above it is categorical (*"never `path:NNN`"*), the guard clause is only the enforcement note, and the text arguably does well not to teach the evasion. **PARTIALLY CORRECT.** |

## Coder response

<!-- CODER-OWNED (fkit-process-stateful-review). The reviewer does not write this section. -->

Round 1 processed 2026-09-04. Every finding verified against source by me before acting; severities
derived from the blast radius I traced, not inherited. Owner dispositions **K1–K5** were relayed from a
live `AskUserQuestion`. No `pending approval` row: this round runs under `/fkit-sprint-ship-loop`'s
standing approval (ADR-019 / ADR-032), which replaces the per-round gate.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | PARTIALLY CORRECT | Defect (in the record, not the work product) | Recorded the shift in `worklog.md` § *Inbound citation shift this task caused* with my own measured figures — **31 of 47** distinct inbound coordinates moved, across **30** durable artifacts (6 ADRs, 2 vault pages). ⛔ Coordinates **not** repaired (K2); producer follow-up returned asking `0368` to absorb the measurement. Substance confirmed; my counts differ from the claimed 40/61/42 on **corpus, not substance** — including `.fkit/tmp/` and this folder my count is 45/89 across 42 artifacts, matching the claimed artifact count. | ✅ done |
| R2 | CORRECT | Defect | Verified: the reviewer skill's Hard-rules bullet carried **no** code-scoping words, so `plan.md § D4`'s shared OUT reason was false for that half (true for the process half, which opens *"Read the code"*). Scoped it minimally — *"verify every claim **against the code**; cite `file:line`"* — matching `claude/agents/fkit-reviewer.md` § *Behavioral rules*. ⛔ Process half untouched. Owner ruling **K1**. | ✅ done |
| R3 | CORRECT | Frontier | Verified both figures independently: `ai-agents/sprints/sprint-7.md` has **70** pipe-leading lines and `ai-agents/sprints/backlog.md` has **208**, so an unescaped `\|` in a pasted fragment would split a findings row. ⛔ No escaping clause added — J4 capped this at one sentence and exceeding it would overrun a live ruling (**K4**). Recorded as a residual below; producer follow-up returned. | won't fix (frontier) |
| R4 | CORRECT | Defect (own record) | Verified both errors: the block is **five** source lines, not four, and CommonMark collapses consecutive lines inside a blockquote exactly as outside it — so the blockquote bought no line-structure preservation. Corrected entry A1's stated reason in `worklog.md`, with a dated note saying what was wrong. ⭐ The **decision** stands; only its justification was false. Owner ruling **K3**. | ✅ done |
| R5 | PARTIALLY CORRECT | Frontier | Verified the mechanism: `maskFencesAndQuotes` in `test/coordination-citation-policy.test.js` blanks fenced blocks **and** every line matching `/^\s*>/`, so the same coordinate inside a fence or a `> ` quote stays green, and resolved shorthand is outside `TARGET` by name. The *"reds on it"* clause therefore overstates the guard. ⛔ Wording deliberately **not** tightened (**K5**) — residual below. | won't fix (frontier) |

**Convergence call — round 1 converged; recommend closing out.** No re-litigation: nothing in R1–R5
matches an accepted residual whose *Re-raise only if* is unmet. R2 does touch an owner-ruled **OUT**
verdict (`plan.md § D4`, approved at **J0**), and I checked it specifically for oscillation — it is
**new evidence, not re-argument**: it does not ask to reverse the OUT verdict or widen the ban, it shows
the verdict's *stated reason* was false for one of the two bullets and proposes scoping words. That is a
first-round finding on a premise, not a second bite. All five are **low**; none touches the amendment's
correctness; two were fixed, two accepted as frontier points with residuals, one recorded and routed to
`0368`. ⚠️ Two findings (R1, R4) are defects in the task's **own record**, which `ADR-034` would allow
closing as residuals without another round; the owner ruled the stronger action (fix) on both, which
is strictly more than the bar requires.

## Accepted residuals (shared, do-not-re-litigate)

- **Mirror-the-guard, don't link the convention page** — What: the rule block names the guard's target class inline (`ai-agents/sprints/*.md`, a task folder's `brief.md`/`plan.md`/`worklog.md`/`review.md`, `ai-agents/wiki-vault/log.md`) instead of deferring to `conventions/durable-citation-anchors.md`. · Why (structural): the convention page's row 3 is **narrower** — it omits `plan.md`, `worklog.md` and `review.md`, the three the guard widened to on 2026-08-30 — so a reviewer following only the page would go red. `plan.md § D2` weighed and rejected link-only, broader, and narrower; the owner approved it as part of `plan.md § ⭐ OWNER RULINGS` **J0**. Accepted cost: the prose can drift if the guard widens again, mitigated by naming the guard file in the text. · Re-raise only if: `test/coordination-citation-policy.test.js`'s `TARGET` or `exempt` actually changes, leaving the prose factually wrong.
- **Two files, not four** — What: `ai-agents/tasks/README.md` and its byte-identical `claude/scaffold/` twin keep the old `file:line` header, so the README desyncs from the skills. · Why (structural): both are manifest-hashed; amending them forces `npm run generate:manifest`, putting a regenerated shipped artifact in the diff — beyond "amend a column". Owner ruling **J1**, option label verbatim *"Out of this run; file a follow-up (Rec)"*. Binding side-condition verified this round: `claude/structure-manifest.tsv` is **untouched** in `git status`. · Re-raise only if: the follow-up is not filed, or the manifest moves.
- **No new test for the amendment** — What: no prose-pin guard and no mechanical sync check between the two schema halves. · Why (structural): the citation guard reds on the **actual artifact** a reviewer produces, which is strictly stronger than asserting a sentence exists in a `SKILL.md`; a string-pin would red on a wording improvement. `plan.md § D5`, owner ruling **J3**, verbatim *"No new check (Rec)"*. · Re-raise only if: a **behavioural** gap is found that a test could catch — prose drift alone does not qualify.
- **`durable-citation-anchors.md` left unedited** — What: its § *Review-ledger practice* still reads *"A review finding's `file:line` cell **stands**"*, which read alone contradicts the amendment. · Why (structural): the page's own scope note makes that section row 2 (a **source** file under review), while row 3 rules coordination documents categorically; the amendment names the target condition explicitly, which is what resolves it. Same manifest + dual-home cost as J1. Owner ruling **J2**, verbatim *"Leave it (Rec)"*. · Re-raise only if: the amendment is shown to **contradict** the page rather than differ from one section of it.

- **One sentence on cell layout, no pipe-escaping caveat** (finding R3, round 1) — What: J4's sentence tells the reviewer to paste a verbatim fragment into the `Claim` cell of a markdown table, and the rule block says nothing about escaping `|`. · Why (structural): **J4 capped this at one sentence deliberately**, and the owner re-affirmed the cap at **K4** (verbatim *"Follow-up task (Rec)"*) after seeing this exact cost — a caveat would be a second sentence and would overrun a live ruling. The cost is real and measured: `ai-agents/sprints/sprint-7.md` carries 70 pipe-leading lines and `ai-agents/sprints/backlog.md` 208, so an unescaped pipe splits a findings row. It is bounded — a corrupted table row is visible and recoverable, it changes no behavior, and `0322` already owns this failure class for other artifacts. · Re-raise only if: the producer follow-up is not filed, **or** a findings row is actually observed corrupted by an unescaped pipe from a pasted fragment.
- **"Reds on it" is deliberately imprecise about the guard's escape hatches** (finding R5, round 1) — What: the rule block says `test/coordination-citation-policy.test.js` *"reds on it in any open task folder"*, which overstates — `maskFencesAndQuotes` blanks fenced blocks and every line matching `/^\s*>/`, and resolved shorthand is outside `TARGET`, so those forms stay green. · Why (structural): ⭐ **not teaching the evasion has value.** A precise clause would tell every reviewer exactly which wrappers dodge the guard, which is the guard-dodge `0356`'s plan named and rejected as its approach 3. The ban immediately above the clause is **categorical** (*"never `path:NNN`"*) and does not depend on the enforcement note being exhaustive; the clause is there to say the rule has teeth, not to delimit them. Owner ruling **K5**, verbatim *"Accept deliberately, record a residual (Rec)"*. Accepted cost: a reader who tests the boundary will find the clause is not literally exhaustive. · Re-raise only if: the imprecision is shown to have actually **licensed** a bad citation — someone citing a coordination document `path:NNN` and defending it with this clause — rather than merely being imprecise.

## Suppressed as settled — raised, not dropped

- **The enumerated class omits `ai-agents/sprints/reviews/*.md` and `ai-agents/sprints/done/*.md`.** The reviewer skill's own Hard rules name `ai-agents/sprints/reviews/<branch-slug>.md` as a legitimate ledger home, and `collectFiles` in `test/coordination-citation-policy.test.js:187-204` never descends into it — so under the amendment's own enumeration a sprint-keyed ledger may still be cited `path:NNN`. This is the **exact, deliberate** cost of mirroring the guard rather than linking the broader convention. Settled by the *Mirror-the-guard* residual above and owner ruling **J0**; suppressed rather than raised as a finding.
