# Review — 0359

Task: ai-agents/tasks/done/0359-the-throughput-counter-created-vs-closed-per-iso-week-and-record-repair-share/brief.md
File(s) under review: `claude/skills/fkit-status/throughput.mjs` (new) · `claude/skills/fkit-status/SKILL.md` (beat-1 headline cell) · `test/throughput-counter.test.js` (new, 25 tests) · `test/prove-red.sh` (runner + step `0n` + mutation 29) · this task folder's `worklog.md`
Status: closed-out
Coverage: both reviewers measured — Codex ran (`codex-cli 0.152.0`, exit 0) and **executed** probes bearing on what it reported (a `foldTransitions` identity-collision run returning `{"created":2,"closed":1}`, and a `diff.renames=false` re-run of the pinned baseline returning `created 452 closed 312`); the Claude reviewer executed the pinned baseline three times, a 14-case `isoWeek` harness, a replication of mutation 29, the D4 guard-deletion disproof, and the subdirectory reproduction.

## Reviewer findings

| #  | Round | Sev | Location | Claim |
|----|-------|------|----------|-------|
| R1 | 1 | high | `claude/skills/fkit-status/throughput.mjs:282` | The git pathspec `'--', 'ai-agents/tasks/'` is resolved **relative to the caller's cwd**, not the repo root. Run from any directory other than the repo root — or with `--repo` pointing at a subdirectory — the counter emits a complete, well-formed, machine-readable report of **all zeros** (`totals created 0 closed 0`, `open 0`, `repair-pct 0.0`) and **exits 0**. Measured: `git -C <repo> log cf289c2 -- ai-agents/tasks/` → 146 commits; the same command with `-C <repo>/claude` → **0**; with the repo-root-relative pathspec `:/ai-agents/tasks/` → 146. The amended `/fkit-status` cell instructs an agent to "run `node .claude/skills/fkit-status/throughput.mjs`, quote its per-week created/closed figures, and name it as the source" with **no cwd precondition**, so a status run from anywhere else quotes fabricated zeros and sources them to this script. `dashboard.sh` does not share the hazard (it takes an explicit path argument). |
| R2 | 1 | high | `claude/skills/fkit-status/throughput.mjs:281` | Rename detection is **not forced** on the `git log` call, so the headline figures depend on the reader's git configuration. Measured at the **pinned** baseline: default → `totals created 351 closed 222`; with `diff.renames=false` → `totals created 452 closed 312`, plus `note no task-folder migration commit is present in this history` and `note 0 same-segment renames excluded`. Silent, exit 0. `plan.md § 3` noticed the dependency ("rename detection on (`diff.renames` defaults true …)") and relied on the default; `plan.md § 11` offered `-M20%` only as a "Mitigation if needed". The script's own `created − closed == open` reconciliation would detect it (140 ≠ 129) but the script never evaluates that invariant. |
| R3 | 1 | high | `claude/skills/fkit-status/throughput.mjs:359` | The counter **prints a false claim in its own output**: `note that commit is a shape change inside one board segment, so it contributes zero closes and zero creations`. Measured — the migration commit `331f298` carries 101 renames under `ai-agents/tasks/`, of which exactly one crosses a board segment: `R099 ai-agents/tasks/backlog/migrate-tasks-to-folder-structure-and-update-tooling.md → ai-agents/tasks/done/0062-migrate-tasks-to-folder-structure-and-update-tooling/brief.md`. The counter **correctly counts it as one close** in 2026-W30; the note denying it is what is wrong. Inherited from `plan.md § 3`, which contradicts itself in the same section — its own transition table lists "flat `backlog/*.md` → `done/*/brief.md` (**is** a close) │ 1". ⚠️ This is the exact failure class the task exists to prevent: a sourced, machine-readable, false measured claim in the output `0360`'s archival banner will quote. |
| R4 | 1 | medium | `claude/skills/fkit-status/throughput.mjs:311` | `readOpenRows` uses `git ls-tree -d` (directories only), so `--at <any pre-migration revision>` fabricates a share instead of declaring it unavailable. Measured at `331f298~1`: output is `open 0` / `repair 0` / `repair-pct 0.0` / `repair-excluding-source-defects-pct 0.0` at exit 0, while that tree actually holds **11** flat `.md` briefs under `backlog/`. Ruling W2 put the pre-migration era **in scope** ("Count it by filename identity"), and the per-week counts there are genuinely correct — only the `⟦REPAIR⟧` block is fabricated. Same silent-zero class as R1; the trigger and the fix differ. |
| R5 | 1 | low | `claude/skills/fkit-status/throughput.mjs:115` | Shipped header comment states a measured number that is wrong: "100 spurious `re-closed` records and a shape-change count reads 0". Measured by deleting the guard and re-running against this repo: **90** `re-closed` records, not 100. 100 is the *shape-change* count, which splits 79 `done` + 11 `cancelled` + 10 `backlog`; only the 90 landing in a closed segment emit `re-closed`. Same class as R3 — a claim asserted as measured, off by 10 — in the file whose purpose is not printing unmeasured numbers. |
| R6 | 1 | low | `claude/skills/fkit-status/throughput.mjs:228` | First-close-wins (`if (st.closed === null)`) means a task closed, reopened and re-closed contributes one close in the **first** close's week, and a task currently reopened still counts as closed — so the `created − closed == open` reconciliation would silently stop holding. **Codex rated this high; I rate it low.** `plan.md § 11` chose it deliberately ("first-close-wins by definition … counts the *first* close and notes reopens separately"), owner-approved under W0, and the reopen is emitted as a `reopen` line rather than dropped. Measured reopens in this corpus at HEAD: **0**. Latent, not live. |
| R7 | 1 | low | `claude/skills/fkit-status/throughput.mjs:217` | `tasks.set(to.identity, prior)` overwrites unconditionally, so a rename **into an identity that already has state** destroys that task's record. Codex reproduced it: two created identities, then `0001 → 0002`, then closing the original `0002` yields `created=2, closed=1`, one surviving map entry, and the second close misfiled as `re-closed`. Requires an ID collision; ADR-029 makes task IDs permanent and globally unique and `test/task-id-uniqueness.test.js` guards the shape, so it cannot fire on this corpus. Latent. |
| R8 | 1 | low | `test/throughput-counter.test.js:379` | `assert.ok(Number(f.open) > 0, 'the only structural claim: the corpus is non-empty')` is an **assertion against the live, moving corpus**, which `plan.md § 5.4` forbids in absolute terms ("All live-corpus reads are `t.diagnostic`, **never** `assert`"), and which the test file's own header repeats. The claim asserted is minimal and only fails if the backlog empties entirely, so the practical risk is small — but the file states a rule it then breaks once, and that is the owner's call to record rather than mine to wave through. |
| R9 | 1 | low | `claude/skills/fkit-status/throughput.mjs:418` | `parseArgs` accepts a following flag as a value: `parseArgs(['--at','--list'])` returns `{rev:'--list', list:false}`. The `opts.rev === undefined` guard catches only a **missing final** argument, so the user gets a downstream git rev-parse failure rather than the usage error the code intends. |
| R10 | 1 | low | `worklog.md § Verification, measured this turn` — and `plan.md § 11`, "**Merge commits** — none in this history (linear)" | That premise is false: this repo has **3** merge commits. Verified harmless — all three touch **zero** files under `ai-agents/tasks/`, and folding the history under the shipped default order, `--topo-order` and `--date-order` produces **byte-identical** totals and per-week counts (`created 375 closed 242` in every case). Recorded so the "history is linear" premise is not carried forward into a later task as established fact; the `--reverse` ordering assumption Codex flagged rests on it. |

## Coder response

<!-- CODER-OWNED (ADR-038). The reviewer never writes this section. -->

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect | Resolved every git call through `repoRoot()` (`rev-parse --show-toplevel`) and made both pathspecs root-relative (`:/ai-agents/tasks/`, `ls-tree --full-tree`). Verified myself: `git -C <root>` → 146 commits, `git -C <root>/claude` → 0. New test `R1 cwd-independence` runs the script from a subdirectory and with `--repo` on a subdirectory, asserting both equal the root run. | ✅ done |
| R2 | CORRECT | Defect | Forced `--find-renames` on the log call, and **wired the reconciliation invariant** rather than only documenting it: the report now emits `reconciliation created-minus-closed N open M` and **throws, exit 1, printing no report** when they disagree. Reproduced the failure myself via `GIT_CONFIG_*` (`created 452 closed 312` at the pinned rev); after the fix that same env yields byte-identical output. Two new tests. ⭐ **The figures did not move** — 351/222 and 46/129 before and after. | ✅ done |
| R3 | CORRECT | Defect | The note is now **computed from a per-commit tally** (`byCommit`), not asserted. Verified the migration commit carries 101 renames under the tasks tree, exactly one crossing a segment (`backlog/migrate-tasks-…md` → `done/0062-…/brief.md`), and that its 57 `A` records are all `plan`/`review`/`worklog`/`README`, never a `brief.md` — so its true contribution is **1 close, 0 creations**. Output now reads that. Pinned as an assertion against the real corpus. ⛔ `plan.md` not re-authored. | ✅ done |
| R4 | CORRECT | Defect | Severity **medium**, agreeing with the reviewer: it needs a deliberate `--at <pre-migration rev>`, so it cannot fire on a default run, but ruling W2 put that era in scope and a fabricated `0.0` is the same silent-wrongness class as R1/R3. `readOpenRows` now lists both eras (folders **and** flat `<slug>.md` stems). Measured after the fix: `--at 331f298~1` → `open 11`, `repair 3`, `27.3%`, reconciliation `11 = 11`. New test builds a flat-era repo whose `ls-tree -d` is empty. | ✅ done |
| R5 | CORRECT | Defect | Severity **low** — a comment, not shipped output, so it misleads a maintainer rather than a banner. Reproduced exactly: deleting the guard yields **90** `re-closed` records, not 100. Corrected to 90 and added the arithmetic that explains it (100 same-segment renames = 79 `done` + 11 `cancelled` + 10 `backlog`; only the 90 in a closed segment reach the re-close branch). | ✅ done |
| R6 | CORRECT | Frontier | No code change. Confirmed the behaviour is exactly as described and deliberate: `plan.md § 11` chose first-close-wins, it is W0-approved, the reopen is emitted rather than dropped, and I measured **0** reopens in the corpus at HEAD. ⭐ The invariant it could break is now **evaluated** (R2's fix), so a future reopen fails loudly instead of silently. Recorded as an accepted residual. | won't fix (frontier) |
| R7 | CORRECT | Defect (latent, unreachable) | No code change, per ruling X3. Reproduced Codex's case myself — `created=2, closed=1`, one surviving map key `0002`. It requires an ID collision, which ADR-029 forbids and `test/task-id-uniqueness.test.js` guards, so it cannot fire on this corpus. Guarding the impossible is over-building. Recorded here so a later reader finds the reproduction rather than rediscovering it. | won't fix (frontier) |
| R8 | CORRECT | Frontier | No code change, per ruling X2 — the owner ruled the assertion **stays**. The claim is accurate: `plan.md § 5.4` and this test file's own header both state the rule absolutely, and the file breaks it once. ⛔ The tension is recorded rather than smoothed: the absolute wording is *not* amended to match the code, because the guard is the only thing standing between a silent R1-class failure and a green suite. Recorded as an accepted residual. | won't fix (frontier) |
| R9 | CORRECT | Defect | Confirmed by execution: `parseArgs(['--at','--list'])` returned `{rev:'--list', list:false}`. A value that starts with `--` is now refused with the usage error, at the point of the mistake instead of a downstream `rev-parse` failure. New test covers `--at --list`, `--repo --at x`, and a missing final value. | ✅ done |
| R10 | CORRECT | Defect (in a plan's premise, not in shipped output) | No code change, per ruling X3. Verified independently: this repo has **3** merge commits, and **0** of them touch anything under `ai-agents/tasks/`. Harmless in effect — the walk passes `--no-merges` explicitly — but the premise is false and is recorded so it is not carried forward as established fact. ⛔ `plan.md` not re-authored. | won't fix (frontier) |

## Accepted residuals (shared, do-not-re-litigate)

- **The script's home is `claude/skills/fkit-status/`** — What: `throughput.mjs` ships beside `dashboard.sh`, not under `bin/`. Why (structural): `install.sh` copies only `claude/`, so this is the only location where the amended `/fkit-status` line names a file that exists in a consuming project; precedented by `dashboard.sh` and `fkit-heal`'s two scripts; engages no manifest. Owner ruling W0/W1. Re-raise only if: a test is shown to break because of the location.
- **The pre-migration era is counted by flat-filename identity** — What: 72+ real pre-`331f298` closes are counted, with per-ID drill-down declared unavailable. Why (structural): discarding them throws away roughly a third of the history the sprint wants a trend from. Owner ruling W2. Re-raise only if: per-week counts are shown wrong in that era (R4 concerns the `⟦REPAIR⟧` block only, not the per-week counts).
- **The baseline is verified at pinned `--at a9c2709`, never the live corpus** — What: verification step 2 names a commit. Why (structural): the live tree holds 133 at HEAD and 112 on disk against the 129 the baseline needs, so a live-corpus check can never pass again. Owner ruling W3. Re-raise only if: the pinned run is shown non-reproducible (measured: three consecutive runs byte-identical).
- **The sprint misses its 10% target, and the miss is reported** — What: `0360`'s banner carries the measured miss with the counter named as source; no number is adjusted to fit. Owner ruling W4. Re-raise only if: an arithmetic error in the figures themselves is demonstrated.
- **No `--worktree` mode** — What: uncommitted closes are counted only once committed; the honest sequence is commit → run → `0360` reads it. Why (structural): a worktree headline would mix two corpora in one report and stop the number being a git-history figure. Owner ruling W5. Re-raise only if: the ruling is revisited by the owner.
- **The classification key is the folder slug, not the brief's H1 title** — What: `classify()` reads the slug. Why (structural): a title can be reworded freely and would silently move the metric; the folder name is the task's identity. Owner-ruled. ⚠️ Verified residual: slug and title disagree on **8** rows at the baseline (`0145`, `0303`, `0304`, `0305`, `0331`, `0338`, `0344`, `0348`) and **3** flip class (`0331`, `0344`, `0348`, all toward record-repair); reading the H1 instead would give **49**, not 46. Re-raise only if: a flipping row outside `{0331, 0344, 0348}` is found.
- **The nine-verb set and the figure 46** — What: both `gloss` (row `0279`) and `reconcile` (row `0317`) are pinned, giving 46/129 = 35.7% at the baseline, against the brief's recorded 45/129 = 34.9%. Why (structural): the seven-verb core scores 44 and each of those two verbs adds exactly one row, so two different eight-verb sets each reproduce 45 — admitting exactly one to hit the brief's number is the curve-fitting the task forbids by name. Verified by reading the rows, not the totals. Owner-ruled ("The counter's 9-verb figures (Rec)"). Re-raise only if: a pinned verb is shown to classify a row whose subject is not the project's own written record.
- **First-close-wins on a reopened task** — What: a task closed, reopened and re-closed contributes ONE close, in the first close's week; the reopen is emitted as a `reopen` line rather than dropped. Why (structural): `plan.md § 11` chose it deliberately and it is W0-approved; the alternative (last-close-wins, or counting both) makes "closed" non-monotonic and would let one task contribute two closes to a trend the sprint reads as throughput. Measured reopens in this corpus at HEAD: **0** — latent, not live. ⭐ The `created − closed == open` invariant this could break is now **evaluated on every run** and refuses to print a report when it fails, so a future reopen surfaces loudly instead of silently. Finding R6; Codex rated it high, I rate it low on the traced blast radius. Re-raise only if: a reopen actually appears in the corpus, or the reconciliation guard is removed.
- **One live-corpus assertion survives in the test file, against its own absolute rule** — What: `assert.ok(Number(f.open) > 0, 'the only structural claim: the corpus is non-empty')` stays. Why (structural): owner-ruled (X2). It is the only guard in the suite against the exact failure R1 described — a well-formed report of all zeros — and every *numeric* live-corpus read really is `t.diagnostic`. ⚠️ **The tension is recorded, not smoothed:** `plan.md § 5.4` and the test file's own header state the rule in absolute terms ("All live-corpus reads are `t.diagnostic`, **never** `assert`"), and this one assertion breaks that wording. The wording was NOT amended to match the code, because softening a rule to fit its exception is how the rule stops binding. It fails only if the backlog empties entirely. Finding R8. Re-raise only if: the assertion is broadened beyond non-emptiness, or a second live-corpus assertion is added.
- **The identity-collision path is left unguarded** — What: `tasks.set(to.identity, prior)` overwrites unconditionally, so a rename into an already-populated identity would destroy that task's record (reproduced: `created=2, closed=1`). Why (structural): owner-ruled (X3). It requires two live tasks to share one ID, which ADR-029 makes impossible by construction and `test/task-id-uniqueness.test.js` guards; adding a branch for it would be guarding the impossible, and the branch itself would need a fixture that violates ADR-029 to exercise. Finding R7. Re-raise only if: ADR-029's uniqueness guarantee is relaxed, or `task-id-uniqueness` is removed.
- **"This history is linear" is false, and harmless** — What: `plan.md § 11`'s premise "Merge commits — none in this history (linear)" is wrong; the repo has **3** merge commits. Why (structural): owner-ruled (X3) as record-only. Measured harmless — all three touch **zero** files under `ai-agents/tasks/`, the walk passes `--no-merges` explicitly, and folding under the shipped default order, `--topo-order` and `--date-order` gives byte-identical totals. Recorded so a later task does not inherit the premise as established fact; `plan.md` is deliberately NOT re-authored. Finding R10. Re-raise only if: a merge commit is found that touches `ai-agents/tasks/`.
