# Plan — 0173: tighten the wiki completion-flag block in all three wiki SKILLs

> **Written by the driver (`fkit-sprint-ship-loop`) at plan approval, before the Build spawn**, per
> `claude/skills/fkit-sprint-ship-loop/SKILL.md` §Durable artifacts (landed by task `0202`, 2026-08-03).
> Copied from the approved text, not re-rendered.
>
> ⚠️ **Carry honesty:** the driver copied this from **its own context**, not file→file — the approved plan
> existed only in this session's `AskUserQuestion` exchange. That is fewer hops than a spawned re-render,
> not zero hops, and it is the accepted `carried-not-approved` residual recorded in `0162`'s ledger.
> Approved by the owner via `AskUserQuestion`, live lead session, 2026-08-03.

---

## Evidence — verified first-hand (no `:NNN` citations; file + heading + quoted phrase)

**Anchor heading, all three files:** step **"Flag any completed tracked task — close nothing"** — step 7 in `claude/skills/fkit-wiki-ingest/SKILL.md`, step 8 in `claude/skills/fkit-wiki-lint/SKILL.md`, `## Step 9` in `claude/skills/fkit-wiki-sync/SKILL.md`.

### 1. The defect, measured

Under **"End the report with one line per task that came out complete or partial, in exactly this form:"**, all three carry, byte-for-byte:

```
- complete → `Task <NNNN>'s vault work is complete — ready to close (producer runs /fkit-task-done on ai-agents/tasks/backlog/<NNNN>-<slug>/brief.md)`
- partial or uncertain → `Task <NNNN>: partial — not ready to close (ai-agents/tasks/backlog/<NNNN>-<slug>/brief.md)`
```

Why the path is dead: it hardcodes the board folder `backlog/`, but a task's folder **moves** to `done/` or `cancelled/` on close (movers only, `/fkit-task-done` / `/fkit-task-cancelled`). The emitted string is a literal path that no tool can open once the move happens.

**Emissions on disk — 4 lines, 2 files:**

| Emission | File | State today |
|---|---|---|
| `Task 0148's vault work is complete — ready to close (producer runs /fkit-task-done on ai-agents/tasks/backlog/0148-…/brief.md)` | `ai-agents/tasks/done/0148-wiki-reingest-the-amended-adr-032-and-clear-its-stale-banner/review.md` | **DEAD** — `0148` is in `done/`. Frozen ledger. |
| `Task 0206's vault work is complete — ready to close (… backlog/0206-…/brief.md)` | `ai-agents/wiki-vault/log.md` | **live** — `0206` still in `backlog/` |
| `Task 0199: partial — not ready to close (… backlog/0199-…/brief.md)` ×2 | `ai-agents/wiki-vault/log.md` | **live** — `0199` still in `backlog/` |

### ⚠️ The producer's characterization is right in substance, overstated in one phrase

- **"The generator is running" — CONFIRMED.** Template present, unmodified, in all three skills. `:NNN` prohibition genuinely absent (`grep` for `line number` and `:NNN` across the three: **zero hits**).
- **"A ledger quoting one freezes it" — CONFIRMED.** One specimen: `0148`'s closed `review.md`.
- **"Manufactures a dead path on every emission" — OVERSTATED as of today: 1 of 4 emissions is dead, 3 are still live.** All 4 will rot; only one has.
- **A sharper correction: the brief's claim that the `complete` form "dies almost immediately … before anyone reads it" is contradicted by measurement.** `0206`'s complete flag says *"ready to close"* and `0206` **is still in `backlog/`** — the producer never closed it. The `complete` form dies when a close actually happens, which is not reliably prompt. Both halves are the slow-rot profile, not one.
- **3 of the 4 emissions live in `ai-agents/wiki-vault/log.md` — out of my scope entirely.**

### 2. "All three" — how I enumerated, and what it missed

Method (repo-wide, hidden dirs included, `.git` excluded): `grep -rln "vault work is complete"`, cross-checked with `grep -rn "not ready to close"` and a digits-form grep for real emissions. Also checked `claude/agents/`, `.claude/agents/`, `claude/scaffold/` (**no `SKILL.md` under scaffold at all** — it holds `AGENTS.md`, `CLAUDE.md`, `ai-agents`, `universal-rules.md`), and `test/`.

**The count of three holds for tracked source.** Two carriers the brief does not name:

1. **`.claude/skills/fkit-wiki-{ingest,lint,sync}/SKILL.md`** — the gitignored mirrors (`git check-ignore` → `.gitignore:.claude/skills/fkit-*/`). Currently `diff`-clean against `claude/`. Regenerated by `claude/fkit-claude-init.sh`; **I will not edit them**, and they will carry the old template until init reruns.
2. **Non-source prose carriers** (report, sprint plan, `0125`/`0153`/`0148` task folders, vault pages) — history and specification, not generators. Not edited.

No test file carries the flag string.

### 3. The fix

**Two textual changes, same text in all three files, under the anchor heading above.**

**(1) Drop the path from both template lines** — owner-ruled candidate (i), folder ID only:

```
- complete → `Task <NNNN>'s vault work is complete — ready to close`
- partial or uncertain → `Task <NNNN>: partial — not ready to close`
```

**(2) Add the `:NNN` prohibition** as a new sentence appended to the existing `<NNNN>` paragraph — beside the rank ban, **not** restructuring it:

> It is also **never** a line number — write no `:NNN` coordinate in a flag.

**Preserve indentation exactly. Measured this turn: ingest = 3, lint = 3, sync = 0.** Ingest and lint blocks `diff` **IDENTICAL**; sync `diff`s **IDENTICAL once 3 spaces are stripped**. Every claim in the brief's indentation warning verified.

**A third edit is REQUIRED and the brief forbids it — see Q1.** The same paragraph reads:

> **`<NNNN>` is the task folder name's four-digit prefix** (equivalently the brief's `## ID`) — **the same four digits that open the path you emit**, and the task's only identity.

Once no path is emitted, *"the path you emit"* is a false clause instructing the reader to emit one. It sits inside the sentence the brief says *"do not re-add it, and do not reword it."* Present in all three files. **Owner call.**

**Existing emissions: LEFT FROZEN. Nothing repaired, nothing annotated.** `0148`'s ledger is closed and frozen by the 2026-07-29 ruling; the other three are in `ai-agents/wiki-vault/log.md`, which only `fkit-wiki` may write. **The fix applies going forward only.** Any vault cleanup is a separate `fkit-wiki` task I will not start.

**Note for the record:** this reverts the *brief-path* half of task `0153` ("wiki-flag-carries-folder-id-and-brief-path") while keeping its folder-ID half. Deliberate, per the owner's ruling — not a regression.

### 4. Citation form

**The new template emits no path at all** — the most durable answer available. The folder-ID `NNNN` is assigned once and never reused (ADR-029 Decision 3), so nothing in the flag can rot. Per `0160`/ADR-035 and the form `0143` established, this plan and all task artifacts cite **file + heading + quoted phrase**; no `:NNN` anywhere.

### 5. Blast radius

- **Blocks `0154` — CONFIRMED.** `ai-agents/tasks/backlog/0154-build-wiki-flag-convention-test/brief.md` requires the guard to assert *"the **complete-flag** line, verbatim, in all three files"* and *"the **partial-flag** line, verbatim, in all three files."* Landing `0154` first pins the ruled-out form in a test.
- **Blocks `0165` — CONFIRMED** (`0165` is *"Decide where a check on the wiki flag's emitted form can live"*; content ruling must precede the check's location).
- **`test/wiki-flag-convention.test.js` does NOT exist yet** — so verification step 7's red-test scenario does not fire today.
- **No test reads a skill body — still true.** `test/skill-frontmatter.test.js`'s `splitFrontmatter` slices `lines.slice(1, i)` and discards everything after the closing `---`. `test/prove-red.sh`'s only `SKILL.md` mutations target synthetic temp trees (`$m8_tree`, `$m9_tree`), not the live wiki skills. `test/dual-home-parity.test.js` compares `ai-agents/` against `claude/scaffold/ai-agents/` — **not** `claude/skills/` vs `.claude/skills/`, so the mirror drift does not turn it red. `test/rules-block-budget.test.js` measures `claude/scaffold/universal-rules.md`.
- **What that means for verification: `npm test` is a no-op signal for this change.** It cannot go red and cannot go green on the edit's correctness. **All real verification is grep + diff.** I did **not** run `npm test` this turn (it writes temp trees; the write-nothing constraint) — **baseline unmeasured**, I will run it at build time and report the result.

### 6. Scope boundary — what I will NOT do

No guard, no test (`0154`/`0136`/`0152` own the `SKILL.md` walk). No ruling on where an emitted-form check lives (`0165`). **No write to `ai-agents/wiki-vault/` — including the three rotting `log.md` emissions**, which are `fkit-wiki`'s exclusively. No edit to `0148`'s frozen ledger or any other ledger. No edit to `ai-agents/sprints/sprint-2.md` or any `backlog/` folder other than `0173`'s own (producer active there). No `.claude/` mirror edit. No renumbering of board ranks. **No commit.**

### Verification steps I will run at build time

1. `grep -n 'tasks/backlog/' ` on all three → **expect exactly the surviving legitimate hits** (the consideration-set line *"any brief under `ai-agents/tasks/backlog/*/brief.md` whose `## Owner` is `fkit-wiki`"* in all three, plus sync's `**Skip:**` line) and **zero** inside either template line. I will list every surviving hit and name it.
2. Both template lines, all three files, carry the folder-ID-only form — **`partial or uncertain` checked explicitly**.
3. `grep` for the `:NNN` prohibition in each of the three.
4. `git diff HEAD` on the rank / `P<n>` sentence → **unchanged**, except the one clause Q1 rules on.
5. Indent report: ingest / lint / sync = **3 / 3 / 0**; ingest-vs-lint `diff` clean; sync clean once stripped.
6. `git diff --stat` → exactly 3 files, all `claude/skills/fkit-wiki-*/`, nothing under `ai-agents/`.
7. `npm test` — result reported verbatim, with the caveat above that it does not cover this change.
8. Recorded on the task record: the form used is **owner-ruled candidate (i)**, and **the ruling came from the brief, not the report** (report §11 still reads *"⏳ Awaits the owner"*).

---

## Open questions for the owner

**Q1 — the dangling *"the path you emit"* clause. (Rec: strike it.)**
The `<NNNN>` paragraph in all three files says *"the same four digits that **open the path you emit**, and the task's only identity."* With the path gone, that clause instructs the reader to emit a path. The brief says do not reword this sentence.
- **(A) Strike only the dangling clause (Rec)** — becomes *"— and the task's only identity."* Minimal; the rank ban itself is untouched; removes an instruction that contradicts the ruling.
- **(B) Leave it** — honors the brief literally, ships a false instruction into the spec an LLM reads.

**Q2 — does the `complete` line keep naming the mover? (Rec: follow the ruling literally.)**
The ruled example is *"Task 0148's vault work is complete — ready to close"* — nothing after. That drops the whole parenthetical, including *"producer runs /fkit-task-done"*, not just the path.
- **(A) Drop the entire parenthetical (Rec)** — literal ruling; the routing instruction is not lost, it survives two lines below in the same block: *"`@fkit-producer Run /fkit-task-done on <brief path>`"*.
- **(B) Keep a pathless mover hint** — `… — ready to close (producer runs /fkit-task-done)`. Preserves the ADR-033 routing cue in the flag itself; deviates from the ruled example's exact text.

**Q3 — the rank conflict. (Rec: promote `0173` above `0154`.)**
`0173` is append-rank **P152**; `0154` sits at **P129** and `0165` at **P130**, both above it, so the board's reading order contradicts the `Blocks:` links. Verified in `ai-agents/sprints/sprint-2.md`.
- **(A) Promote `0173` immediately above `0154` (Rec)** — board order matches the dependency; a producer edit, not mine.
- **(B) Leave ranks, rely on the `Depends on` / `Blocks` links** — accepted today; risks someone pulling `0154` first and pinning the ruled-out form in a test.

---

## Owner rulings — `AskUserQuestion`, live lead session, 2026-08-03

- **Plan approved as written.**
- **Q1 → (A) STRIKE the dangling clause.** It becomes *"— and the task's only identity."* The rank ban in the same sentence is untouched. Rationale accepted: leaving it ships a false instruction into a spec an LLM reads as authoritative.
- **Q2 → (A) DROP the entire parenthetical.** Follows the ruled example literally. The routing cue is not lost — it survives two lines below as *"`@fkit-producer Run /fkit-task-done on <brief path>`"*.
- **Q3 → (B) LEAVE RANKS.** Rely on the `Blocks` links. `0154` and `0165` are both in the 45 rows headed for `backlog.md` once the sprint→backlog move is unblocked, so the exposure is temporary and nobody is driving them. Avoids a re-rank, which ADR-035 exists to restrict.
