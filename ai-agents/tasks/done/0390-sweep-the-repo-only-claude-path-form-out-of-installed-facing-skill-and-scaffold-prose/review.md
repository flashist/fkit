# Review — 0390

Task: ai-agents/tasks/done/0390-sweep-the-repo-only-claude-path-form-out-of-installed-facing-skill-and-scaffold-prose/brief.md
File(s) under review: claude/skills/fkit-sprint-ship-loop/SKILL.md, claude/skills/fkit-sprint-done/SKILL.md, claude/skills/fkit-sprint-cancelled/SKILL.md, claude/scaffold/ai-agents/knowledge-base/conventions/{priority-is-rank-not-identity,task-status-vocabulary,sprint-status-vocabulary}.md, ai-agents/knowledge-base/conventions/priority-is-rank-not-identity.md, claude/structure-manifest.tsv, and only the `E2_PROSE` line of test/dashboard-contract.test.js (working tree vs HEAD)
Status: closed-out
Coverage: both reviewers measured — round 1: Codex (codex-cli 0.152.0, exit 0) ran `node --test` over dashboard-contract / dual-home-parity / structure-manifest (structure-manifest 5/5 pass; most dashboard fixtures blocked by sandbox `mkdtemp` EPERM, E2's prose half passed before its fixture step) plus E2-needle and mutation-39 count scripts (E2 count 1 per mover; m39 old 0 / new 1). Claude reviewer ran `node --test --test-name-pattern="emitter map"` (E1–E4 4/4 pass) and dual-home-parity + structure-manifest + reference-integrity (36 pass, 0 fail, "0 broken, 7 named-exempt"), and `cmp` of the two priority-page copies (identical). prove-red.sh not run by either (concurrent Verify step).

## Reviewer findings
| #  | Round | Sev | Location | Claim |
|----|-------|-----|----------|-------|
| R1 | 1     | low | claude/skills/fkit-sprint-done/SKILL.md:251 | Sweep incomplete: a bare repo-only `claude/` DIRECTORY operand is still in the runnable repoint command `grep -rn --exclude-dir=wiki-vault "<basename>" ai-agents/ claude/ test/ CLAUDE.md README.md AGENTS.md` — here and in `claude/skills/fkit-sprint-cancelled/SKILL.md:239`. In a consuming project `claude/` does not exist, so grep prints "No such file or directory" and exits 2 even when it found hits (a step an agent may read as failed). Missed by construction: the plan's site grep `claude/[A-Za-z0-9_./-]+` needs at least one character after the slash, so it cannot see a bare `claude/`; the worklog's "Zero untriaged" rests on that grep. Same blind spot, untriaged, explanatory prose: sprint-done :86 / :259 / :406, sprint-cancelled :99 / :247 / :417 ("in `claude/`, `test/`, …", "5 in `claude/`"), and `claude/skills/fkit-stateful-review/SKILL.md:72` + `claude/skills/fkit-process-stateful-review/SKILL.md:78` ("files under `claude/`"). ⚠️ NOT a mechanical `.claude/` rewrite: in the fkit repo `claude/` is the correct operand and `.claude/` holds gitignored managed copies that must never be repointed — this is a dual-audience site (the `fkit-heal/SKILL.md:51` "In this repo's own checkout" pattern is the precedent), so the fix shape is a decision. Pre-existing, not introduced by 0390. Raised by Codex (rated medium; lowered — every hit in the directories that do exist is still printed, and nothing in a consumer's `.claude/` should be repointed anyway). |
| R2 | 1     | low | claude/scaffold/ai-agents/knowledge-base/conventions/task-status-vocabulary.md:80 | The new D2(b) qualifier "fkit's own `claude/skill-ownership-hook.sh` (in the fkit install, not your project's `.claude/`)" is accurate about location (share `claude/`, wired by `claude/fkit-claude.sh:331`; not copied by init) but, as the only guidance for "the fix", it now points a team at a non-durable place: `fkit update` re-runs the installer (`claude/fkit-claude.sh:100-104`), which does `rm -rf "$SHARE/claude"` (`install.sh:42`), so a precondition added in the install is silently wiped on the next update. Same sentence at `claude/scaffold/ai-agents/knowledge-base/conventions/sprint-status-vocabulary.md:98`. Borders owner ruling D2(b) (path + note, approved with this example wording) — the wipe-on-update fact was not in the plan's option text, which is why it is raised rather than suppressed; owner may treat it as settled. Raised by Claude reviewer only. |

Suppressed as settled (not rows): priority page still names `test/dashboard-contract.test.js` beside the new `.claude/…` paths — D1(a) ("the `test/…` entries are left alone"); the one-token `E2_PROSE` edit relaxing "no test" and the wider file list — D4(a); keeping `claude/skill-ownership-hook.sh` as a path at all — D2(b); `claude/scaffold/` mentions in priority-is-rank-not-identity.md:127 and durable-citation-anchors.md:227, `fkit-heal/SKILL.md:25-26,51`, `fkit-heal/check.sh:11` — plan.md § "Triage: left unchanged", confirmed legitimate on reading (fkit-meta explanation of why the byte-identical copies cannot link; heal :51 explicitly scoped to the fkit checkout; :25-26 name share files); manifest regeneration making existing projects' untouched copies `untouched-stale` — plan.md § "Pre-existing premise corrections".

Verified sound, no row: all 8 Class A + 2 Class A′ edits are token-only (`claude/` → `.claude/`), no rewrap; both rewritten targets exist under init's copy target (`claude/fkit-claude-init.sh:615-619` copies `claude/skills/fkit-*` to `.claude/skills/`; `fkit-status/dashboard.sh` and `fkit-task-brief/SKILL.md` present); hook confirmed not installed into `.claude/` (no `skill-ownership-hook` in init; `install.sh:43` copies all of `claude/` to the share); priority page copies byte-identical (`cmp`); parity exceptions for both vocabulary pages still describe the path accurately; manifest diff is exactly +3 hash lines (priority, sprint-status, task-status), 0 removed; `E2_PROSE` still carries the full command line ("render the board — `bash .claude/skills/fkit-status/dashboard.sh ai-agents/sprints/<basename>` — … which names every other claimant.") and the old `bash claude/` form is not a substring of it, so the needle pins the new form; mutation 39's anchor ("for `drift ambiguous-plan-identity`, which names") is untouched by the edit and lies inside the needle; no other test or prove-red mutation quotes any rewritten line; wider re-grep of `claude/skills`, `claude/agents`, `claude/scaffold` for `claude/(skills|agents|scaffold|<hook scripts>|structure…)` finds only the triaged sites plus `$share/claude/…` / `$root/claude/…` script variables (correct).

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT — all cited sites confirmed. Runnable command at `claude/skills/fkit-sprint-done/SKILL.md:251` and `claude/skills/fkit-sprint-cancelled/SKILL.md:239`; prose at sprint-done :86 / :259 / :406, sprint-cancelled :99 / :247 / :417, `fkit-stateful-review/SKILL.md:72`, `fkit-process-stateful-review/SKILL.md:78`. A bare-`claude/` grep over `claude/skills`, `claude/agents`, `claude/scaffold` finds exactly these 12 plus `fkit-status/dashboard.sh:679` (a script comment; brief excludes scripts). Exit status reproduced in a temp dir holding only `ai-agents/`: `/usr/bin/grep` printed the hit, warned "claude/: No such file or directory", exit=2. Severity derived: **low** — output stays complete (every hit in a directory that exists still prints) and a consumer has no `claude/` to repoint. ⚠️ But exit 2 is deterministic in every consuming project, because `claude/` never exists there. ⚠️ New fact: the same command also names `test/`, which many consuming projects lack, so a `claude/`-only fix would not clear exit 2 everywhere. The follow-up must cover the whole operand list. | Defect, pre-existing (not introduced by 0390), outside the approved plan's site list. For 0390 it is a scope boundary: the fix shape is a two-audience decision (the `fkit-heal/SKILL.md:51` precedent). | none in 0390 — owner ruling R1 "Follow-up brief (Rec)": close 0390 as built, `0396` (the R1 follow-up brief filed 2026-09-14) designs the both-audiences fix, and residual "R1 bare `claude/` operand and prose" below records it. No source touched. | won't fix (frontier) — why this token: the owner ruled that R1 is recorded as an accepted residual, and Step 6 maps that to `won't fix (frontier)`. Under ADR-045 limit 3 the token is terminal only once the residual entry exists, and it does. The alternatives do not fit. `blocked` is for an approved fix that could not be completed; it is non-terminal and contradicts "close as built". `✅ done` is untrue. `disproven` and `closeout` do not apply. `pending approval` is barred. The six-value vocabulary has no "deferred to a named successor" value (backlog task 0386 proposes one), so "frontier" here means 0390's scope line, not a claim that the defect is intended. ADR-045 §4 point 2 is respected: a brief is filed, so the residual is not parking a defect to dodge both a fix and a brief. |
| R2 | CORRECT — `claude/fkit-claude.sh:100-104` (`_fkit_reinstall` pipes `install.sh` to `sh`), `install.sh:42` `rm -rf "$SHARE/claude"` then `:43` `cp -R` fresh. The hook is wired from the share at `claude/fkit-claude.sh:331` (`bash "$here/skill-ownership-hook.sh"`). The D2(b) qualifier is at `task-status-vocabulary.md:80-81` and `sprint-status-vocabulary.md:98-99` (scaffold copies). An edit made in the install copy is wiped by the next `fkit update`. Severity derived: **low** — this is advisory prose for a hypothetical team extension, and the location it names is now accurate. Before 0390 it named a path that does not exist in a consuming project at all. | Frontier-move: D2(b) (path + note) was chosen, and wipe-on-update is that choice's cost. The durable route (change fkit upstream or in a fork) is outside what a convention page can promise. | none — owner ruling R2 "Accept as residual (Rec)": keep the note as ruled, no page changes. Residual "R2 D2(b) hook note names a non-durable install copy" below records it. No source touched. | won't fix (frontier) |

Regression / oscillation check: no prior *Coder response* rows exist, and no fix is applied, so nothing can regress or recreate a prior finding. Loop check (Step 2): neither finding matches an existing residual (the section was empty) or an ADR whose "Re-raise only if" bars it, so both were treated as novel. R2 borders ruling D2(b), but its wipe-on-update fact was not in that ruling's option text; the owner has now ruled on it directly.

## Accepted residuals (shared, do-not-re-litigate)
- R1 bare `claude/` operand and prose — What: 0390 closes as built. The runnable repoint grep in `fkit-sprint-done` / `fkit-sprint-cancelled` step 6 keeps its bare `claude/` (and `test/`) operands, and the prose mentions of "`claude/`" in those two skills and in the two stateful-review skills are unchanged. In a consuming project the grep warns "No such file or directory" and exits 2, while still printing every hit in the directories that exist. · Why (structural): in the fkit repo `claude/` is the correct operand, and `.claude/` holds gitignored managed copies that must never be repointed. A mechanical `.claude/` rewrite would therefore break the fkit repo's own sprint close, so the fix must serve both audiences (`fkit-heal/SKILL.md:51` shows the split). That is a design decision beyond 0390's approved token sweep. Owner ruling 2026-09-14, verbatim label "Follow-up brief (Rec)". Rejected: a `.claude/` rewrite inside 0390 (wrong for the fkit repo); leaving it untracked (the defect is real). Tracked by `0396` (the R1 follow-up brief filed 2026-09-14). · Re-raise only if: that follow-up brief is cancelled or closes without resolving the operand list, or a sprint close in a consuming project misses a repoint or halts because of the exit-2 warning.
- R2 D2(b) hook note names a non-durable install copy — What: both scaffold vocabulary pages keep "fkit's own `claude/skill-ownership-hook.sh` (in the fkit install, not your project's `.claude/`)", although `fkit update` re-runs the installer and `install.sh:42` wipes `$SHARE/claude`, so an edit made in the install copy does not survive an update. · Why (structural): owner ruling D2(b) ("Keep path + note (Rec)") put the location note there, and on 2026-09-14 the owner kept it after this cost was known (verbatim label "Accept as residual (Rec)"). The note is accurate about where the hook lives. A durable-extension instruction (fork or upstream change) is a larger doc change the owner declined. Rejected: adding a wipe-on-update warning or removing the path (both are page changes, declined). · Re-raise only if: someone reports editing the install copy of the hook (and losing the edit on update).
