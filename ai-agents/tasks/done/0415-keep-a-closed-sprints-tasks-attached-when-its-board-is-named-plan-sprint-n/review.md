# Review — 0415

Task: ai-agents/tasks/done/0415-keep-a-closed-sprints-tasks-attached-when-its-board-is-named-plan-sprint-n/brief.md
File(s) under review: bin/fkit-board.mjs (`boardIdFromFile()` + its comment), test/board-archived-id.test.js (new)
Status: closed-out
Coverage: reasoning-only second opinion — Round 2: Codex (`codex-cli 0.152.0`, exit 0, 300 s cap) only read the diff, the test file and ADR-040 (git diff / sed / rg) and executed no test or probe; its one claim (unanchored mutant not equivalent, counterexample stem `Sprint sprint-2`) falls inside the coder's stated carve-out (stems already starting `Sprint` + whitespace) and was disproven as a refutation. Measurement is the Claude reviewer's: 9 regex mutants of `boardIdFromFile()` against the new test file in a scratch mirror (`/^.*?sprint-/` and `/^(?:[a-z]+-)*sprint-/` now T4-red 4/5; unanchored 5/5; shipped 5/5; pre-fix `/^sprint-/` 1/5), a 19-stem anchored-vs-unanchored probe via the exported `boardId()` (differ only on `Sprint 4sprint-2` and `Sprint\t4-sprint-2`), and the three board test files (30/30).

## Reviewer findings
| #  | Round | Sev  | Location | Claim |
|----|-------|------|----------|-------|
| R1 | 1     | low  | test/board-archived-id.test.js:163-182 (T4), header :13-15 | T4 does not catch the over-reach it claims to guard. Its only "sprint-in-the-middle" fixture is `hotfix-post-sprint2.md` (no hyphen after `sprint`), so an open prefix rule (`/^.*?sprint-/`, `/^(?:[a-z]+-)*sprint-/`) or an unanchored `/(?:plan-)?sprint-/` passes all five tests (measured: 3 mutants, 5/5 green each). The header line "it can only go red if the fix over-reaches" (:14-15) overclaims. `dashboard.sh:141-143` names the exact hazard: an open rule "would claim `hotfix-post-sprint-2.md`". Direction: add a hyphenated `done/hotfix-post-sprint-2.md` (expect `S-hotfix-post-sprint-2`) to T4. Test-gap defect; the shipped regex is correct. |
| R2 | 1     | nit  | bin/fkit-board.mjs:218-220 | The comment says an archived `plan-sprint-4.md` "keeps the `S-004` it had while open". That holds only when the board's H1 agrees with its file name: an open board's id comes from dashboard.sh's H1 rung first (`dashboard.sh:98`, ladder at :148). Plan §2 states this caveat ("What (b)/(c) would catch that (a) does not"), but the code comment drops it. Direction: add "when its H1 and file name agree". Doc-accuracy only; no behaviour at stake. |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | PARTIALLY CORRECT | Defect (test gap, low) | Added `done/hotfix-post-sprint-2.md` → `S-hotfix-post-sprint-2` to T4 (test/board-archived-id.test.js:175, :188) and rewrote the T4 header (:13-19). Mutants, scratch mirror, new T4: `/^.*?sprint-/` and `/^(?:[a-z]+-)*sprint-/` now 4/5 (T4 red; were 5/5); shipped 5/5; pre-0415 reader T4 still green. Partial: unanchored `/(?:plan-)?sprint-/` stays 5/5 — not a test gap, a near-equivalent mutant: `boardId()` anchors on `Sprint`, so ids differ only for a stem already starting `Sprint ` (probe: same id on 12 of 14 stems incl. both hotfix names; differs only on `Sprint 4sprint-2`, `Sprint 4-sprint-2`). Header now says so instead of claiming it. Owner-approved 2026-09-26 via lead relay | ✅ done |
| R2 | CORRECT | Defect (doc accuracy, nit) | Comment now reads "keeps the `S-004` it had while open, when its H1 title and file name agree (an open board's id comes from its H1 first)" (bin/fkit-board.mjs:218-221). Comment only; no banned banner token added. Owner-approved 2026-09-26 via lead relay | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- Archived-board id from the file name, not the H1 — What: `boardIdFromFile()` accepts `plan-` only before `sprint-`, and an archived board whose H1 and file name disagree changes id on close · Why (structural): owner-approved plan (derivation (a)); (b) is a second copy of the identity grammar (ADR-041 §5); (c) adds ~270–320 ms per full re-read. Measured zero disagreeing boards in both trees · Re-raise only if: a real board is found whose H1 and file name resolve to different identities, or the archived-board re-read cost stops mattering.
- Suffix looseness after the prefix — What: `plan-sprint-4-old.md` → `S-4-old`, the same `\S+` looseness `sprint-` stems already have · Why (structural): owner-approved plan §2 ("A detail of (a)"); tightening to dashboard.sh's `N[a-z]?` would change fkit's existing `sprint-` behaviour, out of scope · Re-raise only if: a real board with such a stem appears, or a task takes up tightening `sprint-` stems too.
- `Sprint backlog — …` task values (12 sibling tasks → `S-backlog`, no board) — What: left unattached, visible only under "All tasks" · Why (structural): owner ruling 2026-09-26, selected option "Leave them — No task"; fixing it changes `boardId()`, which maps every fkit task · Re-raise only if: the owner asks for it.
