# Worklog — task 0178, record the canonical merit-statement form

**Worker:** fkit-architect, spawned as the BUILD step of `/fkit-sprint-ship-loop`.
**Date:** 2026-08-21.
**Plan:** `plan.md` in this folder (driver-written, owner-approved 2026-08-21 via `AskUserQuestion`,
verbatim option label *"Approve (Recommended)"*). Not re-authored here.

## What was done

Implemented plan §D into **both homes** of `priority-is-rank-not-identity.md`, byte-identical, plus the
regenerated manifest. Three files, exactly as plan §C scoped.

| # | Path | Change | Lines |
|---|---|---|---|
| 1 | `ai-agents/knowledge-base/conventions/priority-is-rank-not-identity.md` | new `## The merit statement` section after the `## What to write` table; new item 4 under `## Where this is enforced` | +58, −0 |
| 2 | `claude/scaffold/ai-agents/knowledge-base/conventions/priority-is-rank-not-identity.md` | byte-identical copy of the same edit (`cp`, not retyped) | +58, −0 |
| 3 | `claude/structure-manifest.tsv` | regenerated by `npm run generate:manifest`; never hand-edited | +1 attributable to 0178 |

**Total: 119 insertions, 0 deletions**, of which 2 manifest insertions are pre-existing in-flight work
(see the baseline note below), so **117 are 0178's**.

### Owner rulings honoured

- **OQ1** — content stays in `priority-is-rank-not-identity.md`. Nothing written to
  `dependency-declaration-form.md` (only *linked*, which is permitted: it is dual-homed and present in
  both trees, the exact test the page states).
- **OQ3** — item 4 ships **with** its *"Specified, not built yet"* flag intact, verified present at line
  109 in **both** homes. No enforcement claimed that does not exist.
- **OQ4** — neither copy of `conventions/README.md` was touched.
- **OQ2** — `0180` was not edited. Its stale cost figure is a producer's separate correction.

### Constraints honoured

- ⛔ No relative link escapes `conventions/` into `decisions/` or `reports/` — ADR-035 and the decision
  report are cited **bare by name**, per the page's own closing rule. Verified by step 7.
- ⛔ No `path:NNN` citations in the new text. Verified by step 6.
- ⛔ Nothing written under `ai-agents/wiki-vault/`.
- ⛔ Nothing committed, nothing pushed, no task file moved, no `## Status` changed.

## Baseline captured before editing (working tree was already dirty)

`claude/structure-manifest.tsv` was **already modified** by unrelated in-flight work before this task
began — 2 added rows, 0 removed:

```
+03eaaac6…	ai-agents/knowledge-base/conventions/README.md
+2ef1f155…	ai-agents/knowledge-base/conventions/durable-citation-anchors.md
```

This baseline is what makes the plan's *"exactly one line added"* pass condition checkable: after
regeneration the file carries **3** added rows, so **exactly one** is 0178's. Every diff/grep command
below was path-scoped, per the driver's warning about ~97 unrelated dirty paths.

## Verification — plan §E's table, with raw output

Order of operations followed exactly as §E prescribes: edit live → `cp` to scaffold → `diff` →
`generate:manifest` → `npm test` → greps.

| §E step | Command | Pass condition | Result |
|---|---|---|---|
| 4 | `diff` live vs scaffold | exit 0, no output | ✅ **empty, exit 0**; `md5` identical both sides: `092b25026c45d39f9c567eabd1e5f45f` |
| 4b | `node --test test/dual-home-parity.test.js` | 9/9 pass | ✅ **tests 9, pass 9, fail 0** |
| 5a | `npm run generate:manifest`, then `git diff --stat -- claude/structure-manifest.tsv` | exactly one line added for this page, none removed | ✅ `wrote claude/structure-manifest.tsv — 69 entries`; stat `3 +++`, **0 removed**; the one new row is `8f9ad2aa…  ai-agents/knowledge-base/conventions/priority-is-rank-not-identity.md`. Rows for this page **2 → 3**, exactly as §B predicted (table is append-only) |
| 5a | `node --test test/structure-manifest.test.js` | 5/5 pass | ✅ **tests 5, pass 5, fail 0** — assertions A–E all green |
| 5 | `npm test` | green | ✅ **exit 0**. `tests 730, suites 17, pass 730, fail 0, skipped 0, todo 0`. Prove-red hard gate: 11 baseline checks green (0a–0k), **22/22 mutations red their named assertion**, `✓ hard gate PASSED`. No `✖` / `not ok` / `FAIL` line anywhere in the log |
| 1 | grep both canonical shapes | both returned, inside the fenced block | ✅ line 43 `- **On merit:** immediately above 0154 — <reason>`; line 44 `- **On merit:** as ranked` |
| 2 | grep `contains no \`P<n>\` token` | one hit | ✅ line 55 |
| 3 | grep `required, not optional` | one hit, followed by the *absence detectable* reason | ✅ line 61 |
| 6 | grep `\.md:[0-9]` in **both** homes | no output | ✅ **no output, exit 1** |
| 7 | grep `](\.\./` | no output | ✅ **no output, exit 1** |

`npm test` full log kept out of the repo (session scratchpad only); the counts above are read from it
verbatim.

**What `npm test` does and does not prove.** It proves **no regression** — in particular that §B's
predicted manifest staleness was actually cured. It does **not** prove the change: no test asserts on
this page's body today. The brief says this correctly, and §E repeats it. The body is verified by the
greps in steps 1, 2, 3, 6, 7 instead.

## Decision log

**None.** Every judgement call in this build was already settled by the approved plan or by an explicit
owner ruling on OQ1–OQ4. Nothing was decided here that the plan did not decide.

Three things were **checked rather than assumed** before writing, and all confirmed the plan:

- `dual-home-parity-exceptions.mjs` does **not** list this path, so byte-parity is machine-enforced
  (plan §A). Confirmed.
- Report §3.1, §3.7 and §5.4, and ADR-035's title line, were read first-hand this turn; the §D wording
  and the `Authority.` sentence match them. Confirmed.
- `dependency-declaration-form.md` is present in **both** homes and its canonical form really is
  `- **Depends on:**`, so the link and the sentence pointing at it are both live. Confirmed — the
  brief's contrary claim was stale, as plan §A already flagged.

---

## Round 1 review response — 2026-08-21 (architect worker)

Verdict was 🛑 **BLOCKED**, 5 findings. All five re-verified first-hand against the files before any
edit; **none was found wrong**. Owner dispositions of 2026-08-21 applied as relayed. Full ledger:
`review.md` → *Coder response*.

### Decision log — Round 1

| # | Answers | What changed | Why it qualified as a decision |
|---|---|---|---|
| D1 | **R1** | Regenerated `claude/structure-manifest.tsv` inside a **`git clone` of this repo into a scratch dir outside the checkout**, with only 0178's two files applied, then copied the result back. | The plan said "run `npm run generate:manifest`" and said nothing about a dirty tree. Doing that literally is what produced R1. Choosing the isolation **method** — clone rather than stash/revert — was a genuine call, forced by the hard constraint that 0171's uncommitted work must not be touched. A clone carries only committed content, so 0171's files were *physically absent* from the generator's walk rather than merely avoided. |
| D2 | **R1** | Accepted a **red `npm test` in the live working tree** instead of regenerating to green. | The reviewer flagged this trap explicitly. Proved by probe: with 0178's manifest fix fully reverted to HEAD, `structure-manifest.test.js` A and `structure-spec.test.js` F are **still red** — they are 0171's, cleared when 0171 commits and regenerates. Regenerating here would restore the R1 defect. Choosing a correct-but-red tree over a green-but-wrong manifest is the decision. |
| D3 | **R2** | Split R2. **R2a** (presence condition) fixed on the page, tightened to report §5.1's two-part condition with §5.1 now cited by name in item 4. **R2b** (scan domain; whether a fenced example counts) **not** fixed — routed to `0179` as accepted residual **AR-1**. | R2 bundles two different things. §5.1 rules the presence condition, so fixing that is applying ruled authority. §5.1 is **silent** on scan domain and fenced examples — writing them onto the page would widen it past the authority, which the owner's R2/R3 disposition forbids. Splitting rather than half-applying or over-applying was the call; the unfixed half is recorded, not dropped. |
| D4 | **R3** | Recorded only. Page text left verbatim-faithful to report §3.1. | Owner ruled R3 an accepted residual for `0179`. Recorded with its evidence so `0179` need not rediscover it. |
| D5 | **R4** | Line 47 restated from description to **rule**, and made to say plainly that the corpus does not yet meet it. | Owner's instruction was "state it as the rule". Adding the explicit *"much of the existing corpus does not yet meet it"* goes slightly beyond the literal instruction: normative wording alone removes the falsehood but leaves the reader with no idea the corpus is non-conforming, which item 4's caveat only reveals 60 lines later. Verified the trigger first-hand — `0259`/`0260`/`0261` each carry `P<n>` inside a canonical merit line. |
| D6 | **R5** | Moved **"Specified, not built yet."** to the head of item 4, wording byte-unchanged. | Placement, not word count — the flag has to be readable by someone skimming the numbered list heading *"Where this is enforced"*. Keeping the phrase byte-identical was deliberate: the owner ruled item 4 ships **with** its flag, so moving it must not become an occasion to reword it. |
| D7 | **AR-1** | Re-measured the R3 corpus counts instead of copying them. Recorded **10** briefs using the `below` form, against the reviewer's 9, with the ten IDs listed. | An accepted residual is a durable record `0179` will build from; inheriting an unverified count would hand it a stale number. The re-measure also surfaced a hybrid (`below … in importance but above it in readiness`) that neither shape covers — evidence the reviewer's finding understated. |

### Files changed in Round 1

Three, the same three the plan names — **no new file, no widened surface**:
`ai-agents/knowledge-base/conventions/priority-is-rank-not-identity.md`, its byte-identical scaffold
twin, and `claude/structure-manifest.tsv` (now **+1 row, −0**, down from the wrong +3).

### 0171 integrity proof

Hashed before and after; **byte-identical**, never opened for writing:
`README.md` `03eaaac6…c6b01e697`, `durable-citation-anchors.md` `2ef1f155…4466199c7933`.

## Follow-ups this task deliberately did not do

1. **Wiki ingest** (plan §F). `priority-is-rank-not-identity.md` should be ingested into
   `ai-agents/wiki-vault/`, but that is **fkit-wiki's exclusively** — routed, never done here. Owner runs
   `/fkit-wiki-ingest`, or the driver routes an ingest consult.
2. **Task `0180` inherits a duty**: when the `brief-missing-merit` guard lands, it must edit item 4 under
   `## Where this is enforced` to drop *"Specified, not built yet"* — in **both** homes, and regenerate
   the manifest again. `0180`'s brief should carry this.
3. **`0180`'s stale cost figure** — report §5.4b's "29 of 29 open briefs red, 0 canonical" no longer
   matches the corpus (plan §A re-measured: 91 briefs carry `On merit`, 40 already canonical, 38 legacy).
   The owner ruled this a producer's separate correction. Not touched here.
4. **Nothing committed.** The three changed files sit in the working tree.
