# Review — 0188

Task: 0188 — [brief](./brief.md)
File(s) under review: `ai-agents/knowledge-base/architecture.md`, `claude/skills/fkit-team/SKILL.md`, `claude/scaffold/CLAUDE.md`, `claude/structure-manifest.tsv`, `claude/skills-for-role.sh`, `claude/fkit-claude.sh` (D4 hunk `:259-273` only), `CLAUDE.md`, `README.md`, `ai-agents/knowledge-base/PROJECT.md` — working tree vs HEAD `493cecd`, this task's hunks only
Status: closed-out

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | low | `CLAUDE.md:43` | *"Each skill's `⛔ Owner:` banner is a courtesy"* — D2-class falsehood at a site this task rewrote (ND1): `fkit-query` and `fkit-team` carry no banner (24 of 26 do; verified by opening all 26). The task corrected the same phrase at `claude/scaffold/CLAUDE.md:50-51` ("each **role-owned** skill") and left it here. Raised by both reviewers. |
| R2 | 1 | low | `CLAUDE.md:40-43`, `README.md:81-82` | *"against `skills_for_role()` on every `Skill` call, and denies what that role does not own"* / *"checks the spawned agent's own role on every skill call"* — literal over-claim: `claude/skill-ownership-hook.sh:101-109` allows every non-`fkit-*` skill with no identity or ownership check; only `fkit-*` skills are gated. PARTIALLY CORRECT: the wording is verbatim the live mirrors' (`claude/skills/fkit-team/SKILL.md:39-42`, `claude/README.md:58`) and the paragraph is scoped to fkit skills. Narrowing here alone breaks wording parity; narrowing everywhere is wider than 0188. Raised by Codex. |
| R3 | 1 | low | `ai-agents/knowledge-base/PROJECT.md:93` (+ `:68`, `:47-48`), `README.md:75-76`, `claude/fkit-claude.sh:245-246` | Stale mechanism facts left standing beside the ND1 rewrites, same "skillOverrides-era" class: `PROJECT.md:93` *"a `skillOverrides` lockdown"* and `:68` *"generates the per-role `skillOverrides` settings"* — retired by ADR-018 (`claude/fkit-claude.sh:286`, `claude/README.md:66-69`); the new `:96` sentence *"gated the same way"* now points at that wrong session mechanism. `README.md:75-76` *"pins the session to that role's … tool allowlist"* — only `fkit-adversarial-reviewer` carries `tools:` (ADR-022). `PROJECT.md:47-48` *"every other `/fkit-*` skill is turned off"* — ADR-018 §Context: *"Leave every `fkit-*` skill technically enabled everywhere"*; the plan's "not shown false" rested on `fkit-claude.sh:245-246`, a comment that is itself pre-hook. Build flagged the first two (worklog decision log 4). Outside the ruled ND1 sites — owner's call: fold in or file. Raised by both reviewers (the `:47-48`/`:245-246` pair by Codex). |
| R4 | 1 | low | `ai-agents/knowledge-base/architecture.md:144-145` | *"a `⛔ Owner:` banner naming the one role allowed to execute it"* — pre-existing phrase on the edited line; `fkit-open-questions-interview/SKILL.md:11` and `fkit-dumb-down/SKILL.md:12` name *"the six Claude-side roles"*, as the table's own `:159` row says. PARTIALLY CORRECT (imprecise, D2-adjacent, not introduced by this diff). Raised by Codex. |
| R5 | 1 | low | `claude/skills-for-role.sh:26`, `claude/fkit-claude.sh:273` | *"It said "FOUR" until 2026-08-26"* — HEAD `493cecd` (2026-08-25) still says FOUR; the edit landed 2026-08-27 (file mtime) and is uncommitted, so on 2026-08-26 the block still said FOUR. Off by one day. This is the plan-approved block verbatim (ND3), so changing it is a plan deviation — driver/owner call. |

### Checked, no finding (Round 1)
- Every "before" the plan quoted matched HEAD (`git diff -U0` per hunk); every hunk is the plan's "after", except the ruled adaptations (D3 `:48`→`:51`, D4 ranges 14→15 lines).
- D2: 26 skills, 24 banners (lines 11–15), none in `fkit-query`/`fkit-team` (only prose at `fkit-team:48`); the three sentences read true.
- D3: `sed -n 51p claude/skills-for-role.sh` = `skills_for_role() {`; `:51` + fragment satisfies `conventions/durable-citation-anchors.md` row 1 + the rider. ND4 `:12` is correct (`fkit-review/SKILL.md:12`); its fragment is the `⛔ Owner:` named in the same sentence.
- D4: `diff <(sed -n 12,26p skills-for-role.sh) <(sed -n 259,273p fkit-claude.sh)` empty; `bash -n` both OK; `skills_for_role` byte-identical HEAD vs tree for 7 roles + unknown; `"Task 70"` untouched; six mirrors exist and are mirrors (`test/skill-ownership-hook.test.js:289-290,317`; `fkit-claude-init.sh:1011-1014`). "SIX" is the only count added by ruling; "the two universal skills" is a named pair, not a floating count.
- D5/ND1: wording matches ADR-018 §Decision 5 and the four live docs; ADR files untouched; `grep invisible` clean; root `CLAUDE.md` hunks are outside the rules block (`:51-102`).
- `PROJECT.md:91` bullet-head reword (decision log 3) — same claim as the ruled `:93-96` body; within ND1's intent, not creep.
- Manifest: fresh (`generate-structure-manifest.mjs --stdout | cmp` clean); +1 row is the only change. `package.json` unchanged. `node --test test/*.test.js` → 774 pass / 0 fail. Wiki-vault and decisions untouched.
- D1 not reordered (no hunk at `claude/scaffold/CLAUDE.md:23`).

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect | `CLAUDE.md:43` "Each skill's" → "Each role-owned skill's" (D2-class, in-scope ND1 site; standing approval) | ✅ done |
| R2 | PARTIALLY CORRECT | Frontier | none — owner ruled "Keep, record residual" (2026-08-27). Verified: `claude/skill-ownership-hook.sh:101-109` allows every non-`fkit-*` skill with no identity check; the two root sentences are verbatim the live mirrors' (`fkit-team/SKILL.md:39-42`, `claude/README.md:58`) and read as scoped to fkit skills. Recorded below. | won't fix (frontier) |
| R3 | CORRECT | Defect | owner ruled "Fold into 0188 now" (2026-08-27); each site re-measured, then: `PROJECT.md:47-49` "turned off" → "stays technically enabled but is denied on invocation: visible in the `/` menu, unrunnable (ADR-018 §Decision 5)"; `PROJECT.md:68-69` "generates the per-role `skillOverrides` settings" → "writes the per-role settings that wire the `PreToolUse` skill-ownership hook … (ADR-018)"; `PROJECT.md:93-94` "a `skillOverrides` lockdown that makes … unrunnable" → "a `PreToolUse` skill-ownership hook that denies every non-owned `/fkit-*` skill on invocation, visible in the menu but unrunnable" (so `:96` "gated the same way" now points at the hook); `README.md:75-77` "system prompt, tool allowlist, and" → "system prompt and … (a `tools:` allowlist too, for the adversarial reviewer alone — ADR-022, linked)"; `fkit-claude.sh:245-247` comment "sees ONLY these; every other fkit-* skill is turned off" → "may RUN only these — every other fkit-* skill stays enabled (visible in the menu) but is denied on invocation (ADR-018)", kept at 3 lines so the D4 block stays at `:259-273`; `bash -n` OK; the other task's `:316+` hunk untouched. **Extension, owner-ruled "Fold in now" (2026-08-27):** the two same-class sites the coder surfaced — `claude/fkit-claude.sh:17` and `claude/README.md:29` "system prompt and tool allowlist (harness-enforced)" → system prompt (harness-enforced), plus a `tools:` allowlist for the adversarial reviewer alone (ADR-022; linked in the README); one line kept in the script so D4 stays at `:259-273`; `bash -n` OK. Before/after in `worklog.md`. | ✅ done |
| R4 | CORRECT | Defect | owner ruled "Fix in-task": `architecture.md:145` "the one role allowed to execute it" → "the role (or roles) allowed to execute it" | ✅ done |
| R5 | CORRECT | Defect | owner ruled "Change to 2026-08-27": one token in both copies (`skills-for-role.sh:26`, `fkit-claude.sh:273`); `diff <(sed -n 12,26p …) <(sed -n 259,273p …)` empty after; plan deviation logged in `worklog.md` as owner-ruled | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- **Hook scope is fkit-only (R2)** — What: root `CLAUDE.md:40-43` and `README.md:81-82` (with their mirrors `claude/skills/fkit-team/SKILL.md:39-42`, `claude/README.md:58`) say the `PreToolUse` hook checks "every `Skill` call" and "denies what that role does not own"; the hook allows every non-`fkit-*` skill with no identity or ownership check (`claude/skill-ownership-hook.sh:101-109`). The sentences stand, read as scoped to fkit skills (the paragraph's subject). · Why (structural): the fkit-only scope is by design — a project's own or the user's own skills are never touched, the same scope the retired off-list had (`skill-ownership-hook.sh:101-103`). Narrowing the two root sentences alone breaks wording parity with the live mirrors; narrowing all four is wider than 0188. Rejected alternatives: edit root only (parity break); edit all four (scope creep). Owner ruled "Keep, record residual", 2026-08-27. · Re-raise only if: a doc claims the hook governs non-fkit skills, or the mirrors' wording is narrowed (then narrow root `CLAUDE.md` / `README.md` in step).

