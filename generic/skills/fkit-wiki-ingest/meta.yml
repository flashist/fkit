# Per-CLI packaging for the `fkit-wiki-ingest` skill (owned by Codex by default; see ai-agents/config.json).

codex:
  interface:
    display_name: "Wiki Ingest"
    short_description: "Ingest sources into the {{project_slug}} wiki"
    default_prompt: "Run the fkit-wiki-ingest skill: ingest the following source(s) into {{wiki_path}}/: "
