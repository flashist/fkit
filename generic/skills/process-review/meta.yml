# Per-CLI packaging for the `process-review` skill.
# The skill body + shared frontmatter (name, description) live in skill.md.
# This file carries only the per-CLI extras the compiler needs.

claude:
  user_invocable: true

codex:
  interface:
    display_name: "Process Review"
    short_description: "Evaluate external PR review feedback"
    default_prompt: "Use $process-review to evaluate this external PR review before making changes."
